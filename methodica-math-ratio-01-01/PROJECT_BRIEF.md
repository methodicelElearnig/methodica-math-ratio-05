# PROJECT_BRIEF.md — לומדת "מתמטיקה יעד 1.5" (720 | מתמטיקה | יחס | סיין 1)

## לקוח והקשר
תכנית 720, מתמטיקה, יעד 1.5, נושא **"יחס"**. לומדה **חדשה** — אין מסכי תוכן
קיימים לפני הפרומפט הזה. תסריט הפקה:
`מתמטיקה_יחס_יעד 1.5_090826_מוכן להפקה (1).pptx` (בתיקיית הפרויקט הראשית,
מחוץ לתיקיית הסיין הזו — משותף לכל סיני היחידה). לפי בקשת המשתמשת המפורשת:
"אין הבדלים של ממש בין הלומדה בנושא מדעים לנושא המתמטיקה" — אותה מתודולוגיה,
אותם רכיבים/סקילים כמו לומדות המדעים שכבר הופקו; ההבדל היחיד (מלבד תוכן)
הוא ערכת הצבע (Subject theme **Math/סגול**, לא Science/כחול).

שם התיקייה: `methodica-math-ratio-01-01` — סיין 1 מתוך 6 סינים מתוכננים
(אושר ע"י המשתמשת ב-16.08.2026); סינים נוספים ייפתחו כתיקיות אחיות
(`methodica-math-ratio-01-02` וכו') לפי הצורך, כל אחד עם פרומפט-תשתית משלו.

## מטרת הפרומפט הזה (Prompt 1 — תשתית בלבד, ללא תוכן)
בניית **תשתית טכנית בלבד** לסיין 1: מנוע ניווט מלא (canvas/scaleApp/goTo/
resetScreenState), כל הרכיבים הגלובליים המשותפים (flag button, image zoom,
feedback popup, hint overlay, בר-ניווט תחתון), מערכת הדמות המלווה (מפתח
אחסון + פונקציות resolve), RTL אמיתי מלא, וערכת הצבע Math — הכול מוכן
לשימוש, **בלי תוכן לימודי אמיתי**. מסך יחיד placeholder בלבד. תוכן אמיתי
(מסך 1 בפועל, מסכי המשך) ייבנה בפרומפטים הבאים, לפי בריף/Figma/תסריט
שיסופקו.

## מקור למתודולוגיה — הועתק/הותאם במפורש מ:
`methodica-science-mass-measure-03-01`
(`c:\Users\user\OneDrive - methodic.co.il\קבצי עבודה\720 מדעים\יעד 3\methodica-science-mass-measure-03\methodica-science-mass-measure-03-01`)
— לפי בקשת המשתמשת (סיכום-תהליך-בניית-הלומדה.md §0.2, "לחפש לפני שממציאים")
וההחלטה המתועדת ביומן הפרויקט עצמו שפרויקט המדעים יעד 11/יעד 3 הוא מקור
האמת המתודולוגי (לא `methodica-math-scale-01-vadimr-1`, שנבדק ונפסל — ראו
סיכום-תהליך-בניית-הלומדה.md §0.5). הועתקו/הותאמו **רק הרכיבים הגלובליים/
משותפים**: canvas + scaleApp, goTo/resetScreenState/TOTAL_SCREENS, ניווט
מקלדת, dev postMessage bridge, flag-btn, img-zoom-btn/modal, feedback popup
system (CSS+JS), hint overlay CSS, בר-ניווט תחתון (btn-continue/btn-back/
btn-hint), resolveCharBubbleImg/resolveCharBubbleVideo. **28 מסכי התוכן
עצמם של פרויקט המדעים לא הועתקו** — אינם רלוונטיים לתשתית.

## היקף השלב הנוכחי
נבנתה **תשתית בלבד** (`TOTAL_SCREENS = 1`, מסך placeholder יחיד). אין עדיין
תוכן לימודי, אין מסך בחירת דמות, אין שאלות. ראו `ARCHITECTURE.md` לפירוט
טכני מלא.

## שינוי מרכזי ביחס למקור — ערכת הצבע (Math, לא Science)
המקור (`methodica-science-mass-measure-03-01`) הגדיר רק שני טוקנים
(`--subject-500`/`--subject-700`, Science). כאן הוגדרו **11 הטוקנים המלאים**
של ערכת ה-Math, לפי `720-design-guidelines` skill →
`references/colors-and-gradients.md` (הטבלה, עמודת Math) — אושרו במפורש
ע"י המשתמשת בשיחה הנוכחית. ⚠️ **הערכים האלה מחליפים את סעיף 0.6 שכבר קיים
ב-`סיכום-תהליך-בניית-הלומדה.md`, שהתברר stale/שגוי** (ראה השוואה מלאה
ב-`ARCHITECTURE.md` § "תיקון טוקני ה-Math"). קובץ ה-summary ברמת הפרויקט
עצמו טרם עודכן על ידי — ראו "פעולות המשך נדרשות" למטה.

## הנחות/החלטות שדורשות בדיקה אנושית לפני שהתשתית הזו תשמש כבסיס לשכפול
1. **גרדיאנט Math לכפתור Continue** — נלקח מ-`colors-and-gradients.md`
   ("Math gradient": `#6F6AD5 8.654% → #4B35A5 38.462% → #270075 100%`),
   מומר מ-`to bottom` ל-`180deg` (אותה תוצאה חזותית). הסקיל עצמו מסמן את
   שאלת השימוש המדויק בגרדיאנט הזה כ"open question, unconfirmed" — לא
   אושר עדיין ע"י מעצב/ת שזה אכן הגרדיאנט המיועד ל-Continue/Submit. יש
   לאשר לפני שמסכי תוכן אמיתיים מתחילים להסתמך על זה.
2. **קובצי פונט** — הועתקו כפי-שהם (`assistant-hebrew.woff2`,
   `assistant-latin.woff2`) מהמקור, ללא בדיקת רישיון/עדכניות מול המקור
   הרשמי של הגופן.
3. **אייקוני כפתור "מצאתם בעיה?"** (`btn-flag-default.png`/
   `btn-flag-hover.png`) הועתקו כפי-שהם מהמקור (ניטרליים, ללא צבע-נושא
   אפוי — לא דורשים ייצוא מחדש ממצב Math, לפי §0.7 בסיכום הפרויקט).

## פעולות המשך נדרשות (לא בוצעו כחלק מהפרומפט הזה)
- ~~עדכון `סיכום-תהליך-בניית-הלומדה.md` §0.6 וסעיף "מצב נוכחי"~~ — **בוצע**
  בסבב הבא (ראו למטה).
- ~~מסך בחירת דמות (TwoOptionSelection)~~ — **בוצע** (Prompt 2, ראו למטה).
- מסך תוכן 1 בפועל (המסך הבא אחרי בחירת הדמות) וכל שאר הסינים (2–6) —
  ממתינים לרשימת מסכים/מקור-שכפול לכל מסך, ולתסריט ההפקה.

---

## עדכון — Prompt 2: מסך 1 בפועל, בחירת דמות מלווה (16.08.2026)

`data-screen="0"`/`id="s0"` הוחלף מ-placeholder לגמרי: מסך בחירת דמות
(TwoOptionSelection), עיצוב+פונקציונליות מועתקים 1:1 ממסך 1 (`s0`) של
`methodica-science-mass-measure-03-01` — שגם שם משתמש בכרטיסי וידאו בלופ
(לא תמונות סטטיות), לפי בקשת המשתמשת ("בדומה למסך ראשון ביעד 11" — יעד 11
עצמו לא היה נגיש בדיסק בסביבה זו; `mass-measure-03-01` הוא המקור הזמין
בפועל מאותה משפחת מוסכמות, כולל אותו דפוס וידאו-בכרטיס). שני נכסי הוידאו
(`boy-avatar-v-fingers.mp4`, `yellow-avatar-eats-popcorn.mp4`) כבר היו
מונחים ב-`assets/videos/` (סופקו מראש ע"י המשתמשת) — לא הוזזו/שוכפלו.

**התאמה מכוונת אחת ביחס למקור:** המקור עדיין משתמש ב-`direction: ltr`
על `.options-row` כדי לקבע סדר-פיזי — כאן זה **לא** הועתק (RTL אמיתי, לפי
כלל הפרויקט): הכרטיס הראשון ב-DOM (`character-1`) פיזית מימין, השני
(`character-2`) פיזית משמאל, מכוח ברירת המחדל של flexbox תחת `dir="rtl"`
בלבד — בלי שום `direction` override.

**סדר שרירותי, טרם אושר:** מיקום `character-1`/`character-2` (ימין/שמאל)
נבחר שרירותית, כמו במסך המקביל בכל פרויקט קודם — אין הנחיה קודמת. אם יש
העדפה, קל להחליף (החלפת סדר שני ה-`.option-card` ב-`index.html`).

**Alt text מומצא, לא סופק ע"י המפיקה:** "דמות ילד עושה סימן ניצחון
באצבעות" / "דמות צהובה אוכלת פופקורן" — תיאור תמציתי לפי שם הקובץ, לא
תוכן `Alt text:` מפורש מהתסריט (לא סופק כזה). לתקן אם יש נוסח מדויק אחר.

**אין תוויות-שם מתחת לכרטיסים** (`.option-card-label`) — עקבי עם המקור
(מסך 1 שם גם לא מציג שם), לא השמטה. קל להוסיף אם רוצים.

`CHARACTER_STORAGE_KEY`/`resolveCharBubbleImg`/`resolveCharBubbleVideo`
מהתשתית הקודמת נשארו ללא שינוי במבנה, רק הופעלו בפועל
(`selectOption`/`resetScreenState0` כותבים/קוראים דרכם). נוספה גם ולידציה
של הערך השמור מול `KNOWN_CHARACTER_IDS` (לפי החוזה המומלץ
ב-`_global-components.md`, שסימן זאת כ"טרם קיים בשתי הלומדות שנבדקו שם") —
שיפור לעומת המקור, לא רק העתקה.

`TOTAL_SCREENS` נשאר `1` — "בחרתי" קורא ל-`goTo(1)`, no-op בטוח עד שמסך
2 ייבנה בפועל.

---

## עדכון — Prompt 3: מסך 2, אולימפיאדת המדעים (מסך גלילה מותאם) (16.08.2026)

`TOTAL_SCREENS` עלה מ-1 ל-2. תוכן חולץ **בפועל מה-pptx** (שקפים 4-5,
כולל טקסטים בתוך קבוצות-צורה מקוננות — הקובץ היה נעול תחילה, המשתמשת
סגרה את PowerPoint ואישרה המשך). מסך **"לא בתבנית"** (תיוג מפורש של
המפיקה עצמה בהערות השקף) — שתי שאלות חד-ברירה עצמאיות (2 ו-3 מסיחים
בהתאמה) בתוך אזור גלילה יחיד, עם כותרת+שתי תמונות-משלחת קבועות מעל
אזור הגלילה. פירוט טכני מלא: `ARCHITECTURE.md` § "מסך 2".

**החלטות עיצוב שאושרו מראש בשיחה עם המשתמשת** (לא הומצאו): קפיצת-עמוד
בגלילה (בהשראת מסך 17 ב-mass-measure-03, מותאם לעמודים שנחשפים
דינמית), "צף" = fixed/sticky אמיתי (לא חזרה-לכל-שאלה), מסיחים בשורה
אופקית אחת (וריאנט מקומי חדש — לא קיים בספריית המדעים).

**⚠️ הנחה יחידה שנותרה פתוחה לאישור:** טקסט המשוב לשאלה ב' (התסריט
נתן רק את פידבק שאלה א', שכבר חושף את המספר "40") — נכתב מחדש על סמך
אותו הסבר-יחס, לא צוטט. ראו `ARCHITECTURE.md` לפירוט.

**תמונות המשלחות** — placeholder בלבד, לפי בקשה מפורשת ("במקום התמונות
תשאיר בינתיים פלייסהולדר"). ממתינות לנכס אמיתי.

**לא אומת בדפדפן** (אין Playwright בסביבה): קפיצת-העמוד בפועל, שבירת
שורת-המסיחים ב-3 פילים, מיקום מדויק של אזור הגלילה מתחת לכותרת הצפה.
יש לבדוק ידנית לפני שהמסך נחשב סופי.

**עדכון (16.08.2026) א':** מיקום שתי תמונות-המשלחת תוקן מ-שורה ממורכזת
ל-עמודה מוערמת בצד שמאל (`position:absolute; left:40px`, לא
flex+centering) — לפי רפרנס מפורש "מסך 16 סיין 1 יעד 11", ותוך שימוש
בדפוס absolute-positioning מאומת שכבר קיים ב-`mass-measure-03-01`
(`#s11 .scq-img`), לא בניסיון flex+DOM-order שנדחה חוזר-ונשנה באותו
מסך-רפרנס לפי היומן שלו. גודל התמונה: 150×150px (ערך מדויק מאותו
רפרנס). פירוט מלא: `ARCHITECTURE.md` § "מסך 2" → העדכון.

**עדכון (16.08.2026) ב' — שני פערי-תוכן אמיתיים + באג-גלילה, כולם
תוקנו:**
1. **פסקת-הקדמה שלמה (נתוני-היחס: 1:4/1:8) הייתה חסרה** — קיימת
   בתסריט (שקף 4) אבל עטופה ב-`<mc:AlternateContent>` ש-python-pptx
   משמיט בשקט מ-`slide.shapes`; אותרה רק בחיפוש XML ישיר. נוספה
   ל-`.oly-sticky-header` (כולל כיתוב-יחס קומפקטי ליד כל תמונה,
   מאותו מקור). **לקח מתודולוגי לכל חילוץ pptx עתידי** — ראו
   `ARCHITECTURE.md`.
2. **שאלה ג' (שקף 6) נוספה** — לא נבנתה קודם. וריאנט חדש: כל מסיח
   כולל label+נימוק (`.oly-opt-rich`).
3. **תוקן באג ממשי:** `s1InitScrollJump()` קרא `e.preventDefault()`
   על **כל** אירוע גלגלת ללא-תנאי — חסם גלילה טבעית לגמרי. הוסר;
   הגלילה עכשיו טבעית (`overflow-y:auto` רגיל), קפיצת-עמוד/מקלדת
   נשארה רק כתוספת-נוחות. **לא אומת בפועל בדפדפן** (אין Playwright) —
   יש לבדוק את זה ספציפית.

פירוט מלא של כל השלושה: `ARCHITECTURE.md` § "מסך 2" → "עדכון".

**עדכון (16.08.2026) ג' — תיקון מהותי נוסף לפי משוב חד:**
1. פורש מחדש "צף" — **רק התמונות** (העמודה השמאלית) קבועות; הכותרת
   ופסקת-ההקדמה עברו לתוך אזור הגלילה עצמו (זרימה רגילה, מיד לפני
   שאלה א').
2. **כל גודלי הפונט הוחלפו** — הגדלים הקודמים (16/14/13/11/18px) לא
   היו בטבלת הטוקנים המאושרת (`720-design-guidelines/typography.md`)
   כלל. תוקן לטוקנים אמיתיים בלבד (20/24/40px — הטווח המאושר הקטן
   ביותר הוא 20px, אין קטן יותר).
3. אזור הגלילה קיבל שטח אמיתי (604px, לא 166px) — הבעיה המקורית
   ("שטח תחתון קטן") נבעה מכך שכותרת+הקדמה תפסו header קבוע מעל אזור
   הגלילה; עכשיו הן חלק מהתוכן הגולל עצמו.

פירוט מלא: `ARCHITECTURE.md` § "מסך 2" → "עדכון (סבב שני)".

**עדכון (16.08.2026) ד' — שני תיקונים נוספים לפי משוב ישיר:**
1. **כפתור "צדקתי?" הוחלף מפיל-מתאר מומצא ל-`.btn-continue` הגלובלי**
   (גרדיאנט מלא, disabled=אפור מלא) — לפי `SELF-QA-lomda.md` §6, שכבר
   מתעד את זה במפורש כאותו רכיב. לא היה צריך Figma כדי לגלות את זה.
2. **המשוב הוחלף מתיבה מוטבעת/סטטית לפופ-אפ `.scq-fb-box` גריר אמיתי**
   — הסטייה הקודמת (משוב לא-גריר "כי לא מתאים למסך-גלילה") לא הייתה
   מאושרת. שלוש התיבות עכשיו ילדים ישירים של `#s1` (לא בתוך אזור
   הגלילה), גרירות דרך `scqFbMakeDraggable` שכבר היה קיים ומוכן
   מהתשתית. גם קו-הפרדה אפור מומצא בין השאלות הוסר.

פירוט מלא: `ARCHITECTURE.md` § "מסך 2".

**עדכון (17.08.2026) ה':**
1. משוב-סעיף מוסתר ברגע שנבחר מסיח בסעיף הבא (לא רק בניווט-מסך) —
   `olyHideFeedback()` חדש, נקרא מ-`olyQ2Select`/`olyQ3Select`.
2. מסיחים חזרו לעמודה מוערמת (לא שורה) — עקביות עם שאר שאלות
   חד-הברירה בפרויקט; `oly-answers-row` הוסר לגמרי.

**עדכון (17.08.2026) ו':**
1. עמודת התמונות הקבועה הוגדלה (150→170px, כולל התמונות עצמן).
2. נוסף שקף 7 (פסקת-מעבר + דמות מלווה+בועית "נשמע מאתגר ממש! בא
   לי!") מיד אחרי שאלה ג', תלוי-בחירת-דמות ממסך 1, בגודל זהה למסך 2
   סיין 1 יעד 3 (300×230px).
3. מסיחי שאלה ג' פושטו לריצת-טקסט אחת ("**משלחת X**: נימוק") במקום
   שני אלמנטים נפרדים — עקבי עם מסיחי א'/ב'.

פירוט מלא: `ARCHITECTURE.md` § "מסך 2".

**עדכון (17.08.2026) ז' — נוסף מסך 3 + Gesture Hint על שני המסכים:**
1. **Gesture Hint (Cursor Scroll)** — `SELF-QA-lomda.md` §7, הנחיה
   שלא יושמה קודם. הוחל על מסך 2 **וגם** מסך 3 (לפי בקשה מפורשת "בכל
   מסך"). צבעי-טבעת הוחלפו ל-`var(--subject-350/400)` (Math), לא
   Science המוקשח ב-SELF-QA.
2. **מסך 3 נבנה במלואו** ("יהב ויעל שותלים גינה", שקפים 8-9,11-14,
   שקף 10 דולג — מסומן בתסריט "מסך לא להפקה"). כולל: תצלום אמיתי
   (לא placeholder), placeholder ליישומון (לפי בקשה), SCQ, ווריאנט
   DragAndDropQuestion חדש ("דיאגרמת-משוואה") שהפריסה שלו שוחזרה
   מייצוא-שקף אמיתי (PowerPoint COM) כי לא נמצא node תואם בפיגמה.
3. **דמויות-בועה במסך 3 = placeholder** — אין נכסי וידאו לתוכן הזה
   עדיין (לפי אישור מפורש להמשיך כך בינתיים).

**עדכון (17.08.2026) ח' — תוקן באג מבני בהגדלת-תמונה (מסכים 2+3):**
המשתמשת דיווחה שההגדלה "מגעילה" — נמצא ומתוקן: ה-wrapper שה-JS
משכפל לתוך מודאל-הזום (660×660) היה בפריסת-px קבועה (170×170/400×267)
במקום יחסית (100%/100%) כמו ברפרנס (`methodica-science-mass-measure-03-01`),
כך שהתמונה נשארה קטנה בתוך מודאל ענק במקום להתמלא. פוצל לרמה חיצונית
(px קבוע, ללייאאוט הרגיל) + רמה פנימית חדשה (יחסי, זו שמשוכפלת בפועל)
בשני המסכים. תמונות המקור (320×255px) נשארות ברזולוציה נמוכה יחסית
להגדלה — מגבלת-נכס, לא תוקן ב-CSS.

**עדכון (17.08.2026) ט' — נוספו מסכים 4-5 (מאוחד, "מסכים 4-11" בדוקס):**
1. **מסך 4** (מסך-מעבר, שקף 15) — כותרת+דמות-מלווה יחידה (לא זוג-דמויות
   אוחזות-יד כמבוקש בהערת המפיקה — אין נכס כזה בפרויקט).
2. **מסך 5** (מאוחד — "מסכים 5-11" בדוקס/פיגמה, שקפים 16-22) — לפי
   בקשה מפורשת ("כמסך אחד, לשיקולך"): נבנה כמסך HTML **יחיד** עם
   state machine פנימי בן 7 מצבים (פתיחה + 6 שלבי-תרגול-מונחה),
   במקום 7 מסכי-ניווט נפרדים. שני-פאנלים לפי Figma (fileKey
   `eSbp4bKHgBky0rakDb8l9N`): מחברת-לבנה (הבעיה+סיכום דינמי+שלב נוכחי)
   + כרטיס "שלב מודרך" סגול (שאלה+פילי-תשובה+מונה X-מתוך-6). תוכן
   מדויק מחולץ מהתסריט (כולל ייצוא-שקפים אמיתי ל-PNG כדי לפענח סדר-
   פילים פיזי ותיקון מיפוי — שלב ב' הוא 3-way, לא 4-way כפי שהונח קודם
   בשיחה). דיאגרמת-הגולות (שלב 6) נבנתה HTML/CSS טהור, לא asset.
3. `TOTAL_SCREENS` עודכן ל-5.

**עדכון (17.08.2026) י' — תוקן באג-מיקום "צדקתי?" + עמודת תמונות שוב הוגדלה (מסכים 2+3):**
לפי צילום-מסך: כפתור "צדקתי?" ישב sibling נפרד מ-`.scq-answers`
בתוך container ברוחב מלא — `align-self:flex-end` דחף אותו רחוק
מהמסיחים, לכיוון עמודת התמונות. עטפתי scq-answers+כפתור יחד ב-
`.oly-answer-block` חדש (604px) בכל 4 המופעים (מסך 2: q1/q2/q3; מסך
3: סעיף ג') — עכשיו הכפתור צמוד לבלוק-המסיחים עצמו. עמודת התמונות
הוגדלה שוב ל-200px (מ-170px) — 240px היה שובר את הגובה הזמין מול
הבר התחתון, אז צומצמו גם המרווחים כדי להישאר בטוח.

פירוט מלא, כולל כל ההנחות/קירובים שדורשים אישור: `ARCHITECTURE.md`

**עדכון (17.08.2026) י"א — מסך 3 עבר מבנה מחדש מלא לפי 5 רפרנסים מ"יעד 3":**
כל אחד מ-5 סעיפי מסך 3 נבנה מחדש לפי מסך ספציפי בפרויקטי המדעים
("יעד 3"): חלק 1 (שורת טקסט+תמונה 380×380, כמו מסך 2/סיין 6), חלק 2
(שורת פלייסהולדר-יישומון+טקסט, כמו מסך 6/סיין 1 — הסימולציה עצמה
נשארת ריקה), חלק 3 (שאלה+דמות בשורה, תמונת-משוב חדשה — PLACEHOLDER,
מוצגת רק כשהמשוב מוצג, אומתה מהערות-המפיקה של שקף 12 עצמו), חלק 4
(תוקנה טעות: פסקה מתמטית שהוטבעה בטעות בתוך בועה הוצאה לטקסט רגיל,
ונוספה בועה חסרה — "טווווב את גאונה!"), חלק 5 (שפת-צבע/מסגרת של
הגרירה הותאמה למוסכמת מסך 18/סיין 1 — הדיאגרמה-עם-הקווים עצמה נשארה).

**⚠️ תיקון עובדתי:** "מסך 18"/"מסך 20" סיפקו תוכן רק בסיין 1, לא
בסיין 2 (סיין 2 מכיל 10 מסכים בלבד) — נבנה לפי סיין 1, לאשר שזו
הכוונה.

פירוט מלא: `ARCHITECTURE.md`
§ "מסך 3".

---

## עדכון — מסכים 6+7 (17.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 1.docx` (חולץ ישירות מה-XML הגולמי
של הקובץ — python-docx לא הותקן/נדרש). **מסך 6** (`data-screen="5"`,
`id="s5"`, שקף 23) — מסך מעבר, שכפול מבני מלא של מסך 4
(`.s3-content`/`.s3-line` וכו', אין class חדש), עם כפתור-המשך מותאם
("קחו אותי לשאלות"). **מסך 7** (`data-screen="6"`, `id="s6"`, שקפים
24-26) — מסך גלילה עם 2 שאלות ValueInputQuestion (בקבוקים, אז
זוויות), יישומון-בקבוקים כפלייסהולדר ריק בלבד (לפי בקשה מפורשת),
דיאגרמת-זווית שיוצאה בפועל מהשקף (PowerPoint COM + טרימינג), וסרגל
התקדמות מצומצם ל-2 פריטים (לא 6 כמו במוקאפ הגולמי — לפי הבהרת
המשתמשת שזו ספירה גלובלית על מסכים עתידיים, לא לוקאלית למסך הזה).
כפתור "אפשר רמז?" (`.btn-hint`) קיבל כאן את המימוש הפעיל **הראשון**
בפרויקט הזה (הוגדר-CSS-בלבד משלב התשתית).

`TOTAL_SCREENS` עודכן מ-5 ל-**7**. תוך-כדי בנייה נמצא ותוקן ממצא-
תיעוד: `resetScreenState3()` בפועל טוען את וידאו-הדמות **דינמית**
לפי הדמות שנבחרה במסך 1 (`resolveCharBubbleVideo`+`S3_AVATAR_ASSETS`)
— לא נכס-זוג-דמויות סטטי כפי שתועד קודם ב-ARCHITECTURE.md (הערה
ישנה, לא עודכנה אחרי שינוי-קוד מאוחר יותר). מסך 6 נבנה לפי ההתנהגות
האמיתית, לא לפי ההערה הישנה — ראו ARCHITECTURE.md § "מסך 6" לפירוט.

**בדיקה סטטית בלבד** (אין Playwright/דפדפן זמין): `node --check` PASS,
איזון-סוגריים PASS, איזון-תגיות PASS, כל הפניית-נכס נבדקה מול קבצים
בפועל בדיסק PASS. לא בוצעו `responsiveness`/`animation-system`/בדיקת
דפדפן בפועל — ראו ARCHITECTURE.md § "מסך 6+7" לרשימת ההנחות שדורשות
אישור ולפירוט המלא.

פירוט מלא: `ARCHITECTURE.md` § "מסך 6" ו-§ "מסך 7".
