# ARCHITECTURE.md — לומדת "מתמטיקה יעד 1.5" | יחס | סיין 4 (תשתית)

## הערה מתודולוגית — מקור האמת בפרויקט הזה
מסמך זה מתעד את **הקוד בפועל** בתיקייה הזו (`index.html`/`styles.css`/
`script.js`, נבדק ישירות), לא תיאור תיאורטי.

## רקע — תיקייה חדשה מאפס
בניגוד לסיין 2 (שנמצא כבר קיים על הדיסק כעותק גולמי של סיין 1), תיקיית
`methodica-math-ratio-01-04` **לא הייתה קיימת בכלל** לפני הבנייה הזו —
נבדק במפורש (`Glob`) לפני יצירת התיקייה. אין סיכון של תוכן-ישן שצריך
לנקות.

## מקור לרכיבי-התשתית
כל הרכיבים הגלובליים/המשותפים הועתקו/הותאמו מ-
`methodica-math-ratio-01-02` (**סיין 2 של אותו פרויקט**) — לא מסיין 1
ולא ישירות מ-`methodica-science-mass-measure-03-01` — לפי "לחפש לפני
שממציאים": סיין 2 הוא כרגע מקור-האמת המתודולוגי המאומת/מתוקן ביותר
בפרויקט (עבר סבבי-QA נוספים אחרי סיין 1). ראו
`methodica-math-ratio-01-02/ARCHITECTURE.md` לפירוט המלא של התיקונים
שכבר בוצעו במנוע הזה. מסכי-התוכן של סיינים 1+2 **לא הועתקו** — אינם
רלוונטיים לתשתית.

## Canvas ומנוע
זהה 1:1 לסיינים 1+2: קנבס **1280×710px**, `scaleApp()`
(`Math.min(innerWidth/1280, innerHeight/710)`),
`html,body{direction:rtl;overflow:hidden;font-family:'Assistant'}`,
ניווט מסכים דרך `goTo(n)`/`resetScreenState(n)`/`TOTAL_SCREENS=1`,
ניווט מקלדת (`Ctrl+←`/`Ctrl+→`), dev postMessage bridge ל-`index_dev.html`.

## closeAllPopupsAndHints() — מועתק כפי-שהוא מהגרסה המתוקנת של סיין 1/2
```js
function closeAllPopupsAndHints() {
  document.querySelectorAll('[id$="-feedbox"]').forEach(function (el) {
    el.classList.remove('visible');
  });
  document.querySelectorAll('[id$="-hint-overlay"]').forEach(function (el) {
    el.hidden = true;
  });
}
```
`goTo(n)` קורא לפונקציה הזו לפני כל החלפת מסך.

## מוסכמת ID לפופ-אפים
`<prefix>-feedbox` לפופ-אפ משוב, `<prefix>-hint-overlay` לפופ-אפ רמז —
זהה לסיינים 1+2.

## Subject theme — Math (11 טוקנים מלאים)
זהה **בדיוק** לסיינים 1+2 (אותו יעד/נושא — "יחס" — אותה ערכת-צבע):

| טוקן | ערך Math |
|---|---|
| `--subject-100` | `#EAD9FF` |
| `--subject-200` | `#CEC7FF` |
| `--subject-300` | `#E1D9FF` |
| `--subject-350` | `#B3B0FF` |
| `--subject-400` | `#BDB3FC` |
| `--subject-500` | `#3B2FC2` |
| `--subject-600` | `#383896` |
| `--subject-700` | `#5656CC` |
| `--subject-800` | `#4B35A5` |
| `--subject-900` | `#2F2478` |
| `--subject-alpha-100` | `rgba(165, 28, 255, 0)` |
| `--subject-alpha-200` | `rgba(165, 28, 255, 0.3)` |

### גרדיאנט Continue — עדיין הנחה, לא אישור מלא (ירושה מסיינים 1+2)
`.btn-continue` משתמש ב-`linear-gradient(180deg, #6F6AD5 8.654%, #4B35A5
38.462%, #270075 100%)`. עדיין לא סופית מאומת מול node Figma של וריאציית
Math בפועל (ראו סיין 1/2 ARCHITECTURE.md § "ביקורת התאמה לפיגמה") —
משותף לכל 6 סיני היעד, לא רק לסיין הזה.

## בר-ניווט תחתון (`.btn-continue`/`.btn-back`/`.btn-hint`)
זהה לסיינים 1+2 (מקור: `SELF-QA-lomda.md` §6, "Reference CSS", אומת
מול Figma) — `height:39px` מפורש, `min-width:140px`, `var(--subject-700)`
(לא `--subject-500`), גרדיאנט Math לכפתור Continue. `.btn-hint
.scq-hint-icon` דרך CSS `mask` על `icon-bulb-outline-white.svg` (עדיין
לא הועתק לתיקייה הזו — **חסר**, ראו § "פערים ידועים" למטה).

## מסך 1 — Placeholder (`data-screen="0"`, `id="s0"`)
אין תוכן לימודי. קופסה ניטרלית (`.placeholder-box`, מסגרת מקווקוות
`var(--subject-350)`, רקע `var(--subject-100)`) עם כותרת ותת-כותרת
גנריות, ממורכזת בקנבס — **נבנתה מחדש** לפי התיאור ההיסטורי המתועד
ב-`methodica-math-ratio-01-02/ARCHITECTURE.md` (הגרסה המקורית שם כבר
נמחקה כקוד-מת), בגדלי-גופן מהסולם המאושר (32px/700 לכותרת, 24px/400
לתת-כותרת — לא 28px/700 כמו הגרסה המקורית ההיא שכבר נמצאה שגויה). בר
תחתון:
- `.btn-back` (`id="s0-back"`) — **`hidden`**, לפי המוסכמה "Never shown
  on the first screen".
- `.btn-continue` (`id="s0-continue"`) — **`disabled`**, `onclick` טרם
  מחובר: אין עדיין יעד ניווט אמיתי (`TOTAL_SCREENS=1`).
- **אין** מופע `.btn-hint` במסך הזה — אין שאלה. ה-CSS מוכן ב-`styles.css`
  למסך השאלה הראשון.

## רכיבים גלובליים — מוכנים, טרם מחוברים לתוכן
- **`.flag-btn`** — מופע יחיד, מחוץ לכל `.screen`, ישירות ב-`#app`.
  ויזואלי בלבד. נכסים: `assets/images/btn-flag-default.png`/
  `btn-flag-hover.png` — הועתקו מסיין 2 (ניטרליים, ללא צבע-נושא אפוי).
  מיקום `top:32/left:32` — כבר מתוקן לפי Figma (לא 16/16 הישן).
- **`.img-zoom-btn`/`#img-zoom-modal`** — מופע-משותף יחיד, מחוץ לכל
  `.screen`. CSS+JS (`imgZoomOpen`/`imgZoomClose`, delegated click
  handler, `Escape`-to-close) זהים לסיינים 1+2. אין עדיין שום תמונת-תוכן
  שמפעילה אותו.
- **`.scq-fb-box`** (Feedback popup) — CSS + JS (`clampPopupPosition`/
  `scqFbResetPosition`/`scqFbMakeDraggable`) מוכנים, בלי מופע HTML —
  `scqFbMakeDraggable(boxId)` no-ops בבטחה אם ה-id לא קיים עדיין.
- **`.scq-hint-overlay`/`.scq-hint-popup`** — CSS מוכן, בלי מופע HTML.
  **⚠️ תוקן ביחס למקור:** צבע ה-backdrop (`rgba(47, 36, 120, 0.35)`,
  נגזר מ-`--subject-900`) — הועתק כבר-מתוקן. הגרסה בסיין 2 (מקור-ההעתקה)
  עצמה עדיין הכילה `rgba(0,32,96,0.35)` (Science, דלף) עד לתיקון בו-זמני
  שבוצע יחד עם הבנייה הזו (ראו סיין 2 ARCHITECTURE.md).
- **Companion character system** — `CHARACTER_STORAGE_KEY =
  'math-ratio-01_selectedCharacter'` (משותף לכל הסינים ברמת-היעד, לא
  ברמת-הסיין), `resolveCharBubbleImg`/`resolveCharBubbleVideo` מוכנים,
  לא נקראים עדיין.

## פערים ידועים (לתיקון ב-Prompt 2, כשמסך-שאלה ראשון נבנה)
- `assets/images/icon-bulb-outline-white.svg` (נדרש ל-`.btn-hint`),
  `assets/images/icon-check-white.svg`/`icon-x-white.svg` (לתגי נכון/
  שגוי), `assets/images/gesture-hand-cursor.svg` (ל-Gesture Hint אם
  ייבנה מסך-גלילה) — **לא הועתקו לתיקייה הזו עדיין**, כי אין עוד שום
  שימוש בהם (לא נמצא-שימוש = לא מעתיקים מראש, לפי עקרון "לא לבנות
  תוכן/נכסים מראש בלי צורך מוכח" של שלב-התשתית). יועתקו מסיין 2 ברגע
  שהמסך הראשון שבאמת צריך אותם ייבנה.

## בדיקה סטטית
`node --check script.js` → PASS. איזון `{`/`}` ב-`styles.css`: 74/74
PASS. איזון-תגיות ב-`index.html`: 9/9 `<div>`, 1/1 `<section>` PASS.
**לא ניתן לאמת חזותית בסביבה הזו** (אין דפדפן/Playwright זמין) — יש
לפתוח `index_dev.html` בדפדפן ולוודא שהקנבס נטען תקין (placeholder ריק,
בלי שגיאות בקונסול) לפני מעבר ל-Prompt 2.

---

# Prompt 2 — 3 מסכי תוכן (18.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 4.docx`. גבולות-התוכן (שקפים 51-53)
אומתו דרך שקפי-מחיצה בפועל: שקף 50 = "רכיב 4 / methodica-math-ratio-01-04",
שקף 54 = "רכיב 5 / methodica-math-ratio-01-05" — לא הונח מספור. תוכן
כל שקף נבדק ישירות (חילוץ-XML + רינדור בפועל דרך PowerPoint COM
automation, לא רק טקסט-שטוח) לפני הבנייה.

## מסך 1 — מסך מעבר, דמות+בועית (`data-screen="0"`, `id="s0"`, שקף 51)
הדוקס הפנה במפורש ל-"מסך 13 סיין 1 יעד 3" —
`methodica-science-mass-measure-03-01`, `data-screen="12"` — נמצא
ונקרא במלואו (כבר תועד גם בסיין 3 עבור מסך-מעבר-דומה שם). **הבדל
מהותי מסיינים 2/3 של הפרויקט הזה:** מסכי-המעבר של סיינים 2/3 השתמשו
בתבנית הפשוטה יותר `.s0-content` (בלי בועית-דיבור); המסך הזה משתמש
בתבנית **עם** בועית (`.transition-content`/`.transition-title-block`/
`.transition-avatar-wrap`/`.transition-bubble`) — כי זה מה שהרפרנס
המצוטט בדוקס בפועל מכיל, ואומת ישירות מול השקף המרונדר (רואים בועית
בפועל במוקאפ). שני class-families שונים קיימים עכשיו בפרויקט הזה
למסכי-מעבר — לא טעות, תלוי-רפרנס.

**⚠️ הדמות במוקאפ הגולמי היא מפלצת-טקסטית גנרית (תבנית-פלייסהולדר של
המפיק, לא נכס-פרויקט אמיתי)** — נראתה בדיוק אותה מפלצת בשני מוקאפי-
המעבר של הפרויקט (שקפים 51 ו-55/רכיב הבא). הוחלפה במערכת-הדמות-
הנלווית האמיתית של הפרויקט הזה (`character-1`/`character-2`, וידאו).
אין וידאו ייעודי לתוכן הספציפי הזה ("סיימנו תרגול בהצלחה, זמן ללמוד
אחרת") בנכסים הקיימים — נבחרו אותן שתי פוזות ששימשו כבר למסכי-מעבר
קודמים בפרויקט (`boy-avatar-thinking.mp4`/`yellow-avatr-asking.mp4`),
**לא אושר מול המפיקה**.

טקסט (מהשקף בפועל, verbatim): "השלמת בהצלחה את התרגול – כל הכבוד!"
(`.transition-sub`) / "זמן טוב ללמוד גם בצורה אחרת," (`.transition-title`,
הכי-בולט חזותית בשקף) / "כי למידה מתרחשת בכל מיני צורות." (`.transition-sub`)
/ בועית: "את המשימה הבאה נבצע בלי מחשב, במרחב הכיתה או בית הספר!".
כפתור "המשך" → `goTo(1)`.

## מסך 2 — משימת כיתה, הוראות בלבד (`data-screen="1"`, `id="s1"`, שקף 52)
רפרנס: `methodica-science-mass-measure-03-03`, מסך 2
(`data-screen="1"`) — אותר ונקרא במלואו ע"י סוכן-חיפוש ייעודי.
**אומת במפורש: אין שום בדיקת נכון/שגוי במסך הזה** — הערת-קוד מקורית
ברפרנס עצמו אומרת זאת מילה-במילה ("בלי בדיקת נכון/שגוי... כפתור
ההמשך תמיד פעיל"). מוסכמת-class מועתקת 1:1: `.s2-content`/`.s2-title`/
`.s2-group`/`.s2-steps` (רשימה ממוספרת עם `counter-reset`/
`counter-increment`, לא `<ol>` דיפולטיבי של הדפדפן) + `.char-bubble-wrap`
(רכיב-דמות-נלווה **שונה** מ-`.transition-avatar-wrap`: ממוקם קבוע
בפינה, לא ממורכז בתוך בלוק-תוכן — שני רכיבי-דמות נפרדים בפרויקט הזה,
לפי הקשר).

**תוספת מקומית לא-קיימת ברפרנס:** הרפרנס השתמש רק ב-`<ol>` ממוספר;
השקף שלנו מכיל גם רשימת-נקודות רגילה (לא ממוספרת) אחרי הפסקה השנייה
— נוסף class חדש נפרד `.s2-bullets` (לא הרחבה של `.s2-steps`, כי
הסמנטיקה שונה: שלבים-לביצוע מול שאלות-להרהור).

כפתור-ההמשך: "חזרתי.<br>אפשר להמשיך" (טקסט דו-שורתי אמיתי בשקף, לא
עטיפה-אוטומטית — `<br>` מפורש ב-HTML, לא `white-space` בלבד) —
`class="btn-continue s2-btn-continue-wide"`, **תמיד מופעל** (בלי
`disabled`, בלי שום gate-function), `onclick="s1Continue()"` → `goTo(2)`.
בועית-הדמות: "כמה חלונות יש בקומה שלנו?" (טקסט-דוגמה מהשקף, לא
אינטראקטיבי — פלט/הרהור של הדמות, לא שאלה שהלומד/ת עונה עליה).

## מסך 3 — המשך משימת הכיתה: קלט פתוח (`data-screen="2"`, `id="s2"`, שקף 53)
**⚠️ אין תקדים לצירוף הזה (הוראות-פיזיות + שדות-קלט-אמיתיים) באף
פרויקט-אחות שנבדק.** סוכן-החיפוש הייעודי בדק את `methodica-science-
mass-measure-03-03` **וגם** את הרפרנס-המקור שלו
(`Methodica-science-mass-measure-02-03`, שתי גרסאות — הועדפה הגרסה
העדכנית יותר, "linked", `script.js` מ-13.08.2026) — בשני המקרים
"משימת כיתה" היא מסך-הוראות-בלבד, בלי שום `<input>` אמיתי (רק קווים-
ריקים ויזואליים למילוי-על-נייר, `.s2-fill-line`). **נבנה חדש**, לא
הועתק, לפי בדיקה ישירה מול השקף המרונדר בפועל (לא הדוקס בלבד, שרק
תיאר "אינפוטים כמו במסך 2 בסיין 2" באופן כללי):

- 6 שדות פתוחים: 2 טקסט חופשי ("אילו עצמים או חפצים בחרת?" — שם-
  הסוג-הראשון/שם-הסוג-השני, למשל "חלונות"/"עצים") + 2 זוגות-יחס
  מספריים ("היחס הראשון ביניהם היה: _:_", "היחס השני... (לאחר הוספת
  3 יחידות) הוא: _:_").
- **אין תשובה-נכונה יחידה** — אלה נתוני-אמת אישיים שהלומד/ת אספו
  בפועל בכיתה/בית-הספר. לכן: **אין** כפתור "צדקתי?", **אין** פופ-אפ-
  משוב, **אין** `.correct`/`.wrong` בשום מקום. שער-ההפעלה היחיד
  לכפתור "המשך" הוא "כל 6 השדות מלאים" (`s2OnInput()`, פונקציה קטנה
  ומקומית — לא `viqOnInput`/`viqCheck` הגנריים, כי אלה תמיד מניחים
  תשובה-נכונה קבועה ב-`cfg.correct[i]`, שלא קיימת כאן).
- שדות הטקסט/היחס משתמשים ב-`.viq-input`/`.viq-input--sm`/`.viq-coord`
  **רק לעיצוב** (עקביות חזותית עם שאר הפרויקט) — הועתקו כ-CSS בסיסי
  בלבד מסיין 2/3, **בכוונה בלי** ה-state rules `.correct`/`.wrong`
  שקיימים שם, כי שום קוד כאן לא מוסיף classes כאלה בכלל.

**⚠️ באג אמיתי שנתפס בבדיקה-עצמית (לא בסבב-QA נפרד):** אחרי כתיבת
ה-HTML הראשונית, גרסת ה-CSS לא הכילה בכלל את `.viq-input`/
`.viq-input--sm`/`.viq-coord` (רק אוזכרו בהערה, לא הוגדרו בפועל) —
כי תשתית-הבסיס של הסיין הזה (Prompt 1) לא כללה CSS של תבניות-שאלה
כלל, ונשכח להוסיף אותו כשנבנה מסך 3. נתפס ע"י סריקה פרוגרמטית של כל
class בכל ה-HTML מול קיומו בפועל ב-CSS (לא הסתמכות על "זה בטח כבר
שם") — תוקן: שלושת ה-rules נוספו (בלי `.correct`/`.wrong`, כמתועד
למעלה).

בועית-הדמות: "הופה! אילו דברים מגניבים בחרת!" (טקסט-עידוד מהשקף, לא
אינטראקטיבי).

**⚠️ goTo(3): TOTAL_SCREENS=3 (אינדקסים תקפים 0-2) — no-op בטוח** לפי
ה-bounds guard של `goTo()`, לא קריסה. זהה למוסכמה המתועדת כבר בסיינים
1-3 עבור מסכן האחרון — יעד-ניווט אמיתי דורש אישור-מוצר.

## בדיקה סטטית (חוזרת, אחרי כל התוכן למעלה)
`node --check script.js` → PASS. טעינה אמיתית ב-Node עם DOM מדומה
(לא רק `node --check`, שרק בודק תחביר) → PASS, ללא ReferenceError/TDZ.
איזון `{`/`}` ב-`styles.css`: 111/111 PASS. איזון-תגיות ב-`index.html`:
36/36 `<div>`, 3/3 `<section>`, 8/8 `<button>`, 3/3 `<video>` PASS.
כל `onclick`/`oninput` נבדק מול קיום הפונקציה ב-JS בפועל. כל class
שמופיע ב-HTML נבדק מול קיומו ב-`styles.css` (סריקה פרוגרמטית מלאה,
לא דגימה) — זה בדיוק מה שתפס את הבאג המתועד למעלה. כל נתיב-נכס
(`assets/...`) שמופיע ב-HTML/JS נבדק מול קיום בפועל בדיסק. **לא ניתן
לאמת חזותית בדפדפן בסביבה הזו.**

## עדכון (2026-08-19) — QA צולב על כל הסינים: פס-גלילה תמיד מימין

**ממצא אמיתי**: `.s2-content` (מסך 2, הוראות-בלבד) ו-`.s3open-content`
(מסך 3, קלט-פתוח) היו `overflow-y:auto` ישירות על אלמנט עם `direction`
שלא הוגדר-מפורש (יורש `rtl` מ-`html`) — **בלי** טכניקת עוטף-חיצוני-ltr
+ פנימי-rtl שכל שאר מסכי-הגלילה בפרויקט (סיינים 1-3, 5) משתמשים בה כבר.
משמעות בפועל: אם התוכן עולה על גובה-המסך, פס-הגלילה של הדפדפן היה
עלול לרנדר בצד **שמאל** (ההתנהגות הידועה/המתועדת-כבר של דפדפני-Chromium
ל-`overflow` בהקשר RTL), בניגוד למוסכמה "הגלילה תמיד בצד ימין" של
הפרויקט. מסך זה נבנה לפני שהדרישה הזו נבדקה במפורש כאן (ה-QA המקורי
של הסיין לא כלל בדיקת-מרווח/צד לפס-גלילה).

**תוקן**: שני המסכים עודכנו לאותו דפוס בדיוק כמו `.s1-scroll-area`
(סיינים 1/3/5): `.s2-content`/`.s3open-content` עצמם הפכו לעוטף-החיצוני
(`direction:ltr; padding-right:20px; box-sizing:border-box;
overflow-x:hidden`) עם `::-webkit-scrollbar` מותאם (8px, אותם צבעים),
והתוכן-בפועל עבר לתוך `<div class="s2-content-inner">`/
`<div class="s3open-content-inner">` חדש עם `direction:rtl` (משחזר RTL
אמיתי) ו-`display:flex; flex-direction:column` + אותו `gap` שהיה קודם
על העוטף. איזון תגיות `<div>` נבדק מחדש: 38/38 PASS. אין תלות-JS
במבנה-הפנימי (`querySelector`/`children`/`:nth-child`) שהייתה עלולה
להישבר משינוי-הקינון — נבדק, אין כזו.
