# PROJECT_BRIEF.md — לומדת "מתמטיקה יעד 1.5" (720 | מתמטיקה | יחס | סיין 6)

## לקוח והקשר
תכנית 720, מתמטיקה, יעד 1.5, נושא **"יחס"**. זהו **סיין 6 מתוך 6**
המתוכננים ליעד הזה — האחרון בסדרה. אחיו של `methodica-math-ratio-01-01`
(סיין 1, 7 מסכים בנויים), `methodica-math-ratio-01-02` (סיין 2, 5
מסכים בנויים), ו-`methodica-math-ratio-01-03`/`-04`/`-05` (סינים 3-5,
תשתית-בלבד, נבנו באותו סבב). **⚠️ לא נמצא קובץ `פרומפטים לבניית מסכים
של סיין 6.docx`** בתיקיית הפרויקט בזמן בניית התשתית הזו (קיימים קבצים
לסינים 3/4/5 בלבד) — התשתית עצמה לא תלויה בו, אבל יידרש לפני Prompt 2.

## מטרת הפרומפט הזה (Prompt 1 — תשתית בלבד, ללא תוכן)
תיקייה חדשה מאפס (לא נמצאה תיקייה קיימת מוקדם יותר, בניגוד למה שקרה
בסיין 2). נבנה: מנוע ניווט מלא (canvas/scaleApp/goTo/resetScreenState),
כל הרכיבים הגלובליים המשותפים (flag button, image zoom, feedback popup
CSS/JS, hint overlay CSS, בר-ניווט תחתון), מערכת הדמות המלווה (מפתח
אחסון + פונקציות resolve), RTL אמיתי מלא, וערכת הצבע Math — הכול מוכן
לשימוש, **בלי תוכן לימודי אמיתי**. מסך יחיד placeholder בלבד. תוכן
אמיתי ייבנה בפרומפטים הבאים לפי `פרומפטים לבניית מסכים של סיין 6.docx`.

## מקור למתודולוגיה — הועתק/הותאם במפורש מ:
`methodica-math-ratio-01-02` (סיין 2 של **אותו פרויקט**) — לא סיין 1
ולא מ-`methodica-science-mass-measure-03-01` — לפי החלטת "לחפש לפני
שממציאים": סיין 2 הוא כרגע מקור-האמת המתודולוגי המאומת/מתוקן ביותר
בפרויקט (עבר סבבי-QA נוספים אחרי סיין 1 — תיקון בעיית-הגלילה ב-RTL,
תיקון צבע Progress Question, ניגודיות-תוויות, מרווח פס-גלילה מול
Figma). הועתקו/הותאמו **רק הרכיבים הגלובליים/משותפים**: canvas +
scaleApp, goTo/resetScreenState/TOTAL_SCREENS, ניווט מקלדת, dev
postMessage bridge, flag-btn, img-zoom-btn/modal, feedback popup system
(CSS+JS: `clampPopupPosition`/`scqFbResetPosition`/`scqFbMakeDraggable`),
hint overlay CSS, בר-ניווט תחתון, Companion character system
(resolveCharBubbleImg/Video + storage key). **תוכן-הלימוד של סיינים 1+2
לא הועתק** — אינו רלוונטי לתשתית.

**ממצא אגבי במהלך ההעתקה:** `.scq-hint-overlay` בסיין 2 עדיין הכיל את
צבע ה-Science הדלוף (`rgba(0,32,96,0.35)`) שכבר תוקן בסיין 1 — ביקורת
העיצוב על סיין 2 החמיצה את זה כי בדקה רק את `.scq-hint-popup` עצמו, לא
את ה-backdrop. תוקן בו-זמנית גם בסיין 2 עצמו וגם כאן (הועתק כבר-מתוקן).

## מפתח אחסון הדמות — משותף במכוון לסיינים 1+2
`CHARACTER_STORAGE_KEY = 'math-ratio-01_selectedCharacter'` — **זהה**
לסיינים הקודמים, **לא** `'math-ratio-01-06_selectedCharacter'` חדש.
המפתח מתויג ברמת ה-**יעד/יחידה** ("math-ratio-01"), לא ברמת הסיין
הבודד — כדי שבחירת-הדמות שהלומד/ת עשה/תה בסיין קודם תישמר ב-`localStorage`
ותחול גם כאן. אם יתברר שכל סיין צריך לבקש בחירה עצמאית — יש לשנות את
זה במפורש, לא להניח.

## היקף השלב הנוכחי
נבנתה **תשתית בלבד** (`TOTAL_SCREENS = 1`, מסך placeholder יחיד). אין
עדיין תוכן לימודי, אין מסך בחירת דמות (ייתכן שלא יידרש כלל כאן, אם
הבחירה כבר נעשתה בסיין קודם — לאשר/להחליט כשהמסך הראשון בפועל ייבנה).
ראו `ARCHITECTURE.md` לפירוט טכני מלא.

## פעולות המשך נדרשות (Prompt 1 — היסטורי, בוצע ב-Prompt 2)
- לחלץ תוכן מ-`פרומפטים לבניית מסכים של סיין 6.docx` + מהתסריט
  (`מתמטיקה_יחס_יעד 1.5_090826_מוכן להפקה (1).pptx`) לבניית מסך 1
  בפועל. **חובה להשתמש בסקיל `pptx-script-extraction`** לשליפת התוכן
  מהתסריט (ראו `MUST-USE-skills.md` §8) — לא python-pptx רגיל/סקרינשוט.
- להחליט: האם סיין 6 פותח במסך בחירת-דמות משלו, או מניח שהבחירה כבר
  קיימת מ-localStorage — עדיין פתוח, לא הוכרע (זהה לשאלה הפתוחה בסיין 2).

## עדכון (2026-08-19) — Prompt 2: שלושה מסכי-תוכן אמיתיים

**היקף**: תוכן אמיתי הוזן לתוך התשתית (Prompt 1). `TOTAL_SCREENS`
עודכן מ-1 ל-**3**. התוכן (טקסטים/מפתחות-תשובה/משוב/רמזים) סופק
כבר-מחולץ על-ידי המשתמש עצמו — נלקח כפי-שסופק, לא נגזר-מחדש ולא
"שופר", כולל במקום שבו נמצאה אי-עקביות ניסוחית לכאורה בהסבר סעיף ב'
(ראה למטה). זהו הסיין ה-6 מתוך 6 ביעד הזה — **האחרון** — ולכן כפתור-
ההמשך הראשי במסך האחרון הוא no-op בטוח (`goTo(TOTAL_SCREENS)`, אין
עדיין יעד-אמיתי לסוף-היחידה). פירוט טכני מלא ב-`ARCHITECTURE.md`.

**המסכים שנבנו:**
1. `s0` — מסך-מעבר עם בועיית-דיבור: "כל הכבוד! הסתיים שלב התרגול" →
   "עכשיו מגיעה שאלת השיא" → "יש בה 4 סעיפים...". מבנה מועתק 1:1
   מ-`methodica-math-ratio-01-04` מסך 1, לפי הנחיה מפורשת.
2. `s1` — תמונה ממלאת-מסך (`family-home.jpg`) עם תגית-מידע בפינה
   השמאלית-עליונה — **סוג-מסך חדש בפרויקט הזה**, אין תקדים ישיר; נבנה
   לפי דפוס גנרי + גיאומטריה שנמדדה מה-pptx.
3. `s2` — "שאלת השיא", מסך-גלילה עם דיאגרמת-מגרש קבועה (זהה בארבעת
   הסעיפים) + 4 שאלות ValueInputQuestion (סעיפים א/ב/ג/ד: שטח-מגרש,
   שטח-גינה, העברת-שטח פעמיים ביחסים שונים) + חלק-ביניים תמונתי לא-
   מנוקד באמצע (תמונת-מהנדסים, דיאגרמה מוסתרת זמנית).

**החלטות/פערים מפורשים שתועדו (ראו `ARCHITECTURE.md` לפירוט מלא של
כל אחד):**
- מסך `s0`: חלוקת-הטקסט בין תת-כותרת/כותרת/בועית-דיבור היא שיקול-
  דעת — המקור לא מסמן זאת בעצמו (רק שורה אחת מודגשת-בבולד).
- מסך `s1`: אין תקדים בפרויקט למסך "תמונה ממלאת-מסך + כרטיס-מידע" —
  class גנרי חדש (`.info-banner-card`) נבנה כדי שישרת גם את מסך `s1`
  וגם את חלק-הביניים במסך `s2`.
- מסך `s2`: הכללת מנגנון ה-Progress Question מ"שאלה אחת למסך" (התקדים
  היחיד הקיים) ל"4 שאלות על מסך פיזי יחיד" — `resetScreenState2`
  מחשב איזו שאלה "נוכחית" בעת חזרה, לפי הכללה של דפוס `resetScreenState5`
  הקיים בסיין 3.
- מסך `s2`, חלק-הביניים: נבחר להרחיב זמנית את כל עמודת-הגלילה (ולא
  bleed עם margin שלילי) כדי שתמונת-חלק-הביניים תיראה רחבה — נבדק
  ונמצא ש-margin שלילי היה נחתך על-ידי `overflow-x:hidden` של האב.
  טרייד-אוף מתועד: חלקים א'/ב' (שכבר נענו) עשויים לזוז מעט חזותית
  לרגע ההרחבה.
- מסך `s2`, סעיף ב': טקסט-ההסבר שסופק מכיל אי-עקביות ניסוחית לכאורה
  ("3:2" מול המקדם "2/5") — הועתק כפי-שהוא, לא תוקן, לפי כלל-הפרויקט
  הקבוע ("תוכן כפי-שסופק") — תקדים זהה בדיוק בשאלת-ערבוב-הצבעים
  בסיין 5.
- זום-תמונה לא הופעל על דיאגרמת-המגרש — עקביות עם התקדים הקיים
  בסיינים 3/5 (שגם הם לא הפעילו זום על הדיאגרמות-הקבועות שלהם), על
  פני המדיניות הגנרית של הסקיל.

**אימות (Prompt 2)**: `node --check` PASS; load-and-execute עם DOM
מדומה (`vm.createContext`) — כל 4 הזרימות (נכון/שגוי-סופי/נכון/נכון)
תואמות למפתחות-התשובה, ה-resume אידמפוטנטי; סריקה מלאה (לא מדגמית)
של classes/handlers/asset-paths — הכול PASS. **לא בוצעה בדיקה חזותית
בדפדפן** (אין דפדפן/Playwright זמין בסביבה הזו) — נדרש מעבר ידני על
`index_dev.html` לפני אישור סופי.
