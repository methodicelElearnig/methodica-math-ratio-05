# PROJECT_BRIEF.md — לומדת "מתמטיקה יעד 1.5" (720 | מתמטיקה | יחס | סיין 5)

## לקוח והקשר
תכנית 720, מתמטיקה, יעד 1.5, נושא **"יחס"**. זהו **סיין 5 מתוך 6**
המתוכננים ליעד הזה — אחיו של `methodica-math-ratio-01-01` (סיין 1, 7
מסכים בנויים), `methodica-math-ratio-01-02` (סיין 2, 5 מסכים בנויים),
ו-`methodica-math-ratio-01-03`/`-04` (סינים 3-4, תשתית-בלבד, נבנו
באותו סבב).

## מטרת הפרומפט הזה (Prompt 1 — תשתית בלבד, ללא תוכן)
תיקייה חדשה מאפס (לא נמצאה תיקייה קיימת מוקדם יותר, בניגוד למה שקרה
בסיין 2). נבנה: מנוע ניווט מלא (canvas/scaleApp/goTo/resetScreenState),
כל הרכיבים הגלובליים המשותפים (flag button, image zoom, feedback popup
CSS/JS, hint overlay CSS, בר-ניווט תחתון), מערכת הדמות המלווה (מפתח
אחסון + פונקציות resolve), RTL אמיתי מלא, וערכת הצבע Math — הכול מוכן
לשימוש, **בלי תוכן לימודי אמיתי**. מסך יחיד placeholder בלבד. תוכן
אמיתי ייבנה בפרומפטים הבאים לפי `פרומפטים לבניית מסכים של סיין 5.docx`.

## מקור למתודולוגיה — הועתק/הותאם במפורש מ:
`methodica-math-ratio-01-02` (סיין 2 של **אותו פרויקט**) — לא סיין 1
ולא מ-`methodica-science-mass-measure-03-01` — לפי החלטת "לחפש לפני
שממציאים": סיין 2 הוא כרגע מקור-האמת המתודולוגי המאומת/מתוקן ביותר
בפרויקט (עבר סבבי-QA נוספים אחרי סיין 1 — תיקון בעיית-הגלילה ב-RTL,
תיקון צבע Progress Question, ניגודיות-תוויות, מרווח פס-גלילה מול
Figma). הועתקו/הותאמו **רק הרכיבים הגלובליים/משותפים**: canvas +
scaleApp, goTo/resetScreenState/TOTAL_SCREENS, ניווט מקלדת, dev
postMessage bridge, flag-btn, img-zoom-btn/modal, feedback popup system
(CSS+JS: `clampPopupPosition`/`scqFbResetPosition`/`scqFbMakeDraggable`),
hint overlay CSS, בר-ניווט תחתון, Companion character system
(resolveCharBubbleImg/Video + storage key). **תוכן-הלימוד של סיינים 1+2
לא הועתק** — אינו רלוונטי לתשתית.

**ממצא אגבי במהלך ההעתקה:** `.scq-hint-overlay` בסיין 2 עדיין הכיל את
צבע ה-Science הדלוף (`rgba(0,32,96,0.35)`) שכבר תוקן בסיין 1 — ביקורת
העיצוב על סיין 2 החמיצה את זה כי בדקה רק את `.scq-hint-popup` עצמו, לא
את ה-backdrop. תוקן בו-זמנית גם בסיין 2 עצמו וגם כאן (הועתק כבר-מתוקן).

## מפתח אחסון הדמות — משותף במכוון לסיינים 1+2
`CHARACTER_STORAGE_KEY = 'math-ratio-01_selectedCharacter'` — **זהה**
לסיינים הקודמים, **לא** `'math-ratio-01-05_selectedCharacter'` חדש.
המפתח מתויג ברמת ה-**יעד/יחידה** ("math-ratio-01"), לא ברמת הסיין
הבודד — כדי שבחירת-הדמות שהלומד/ת עשה/תה בסיין קודם תישמר ב-`localStorage`
ותחול גם כאן. אם יתברר שכל סיין צריך לבקש בחירה עצמאית — יש לשנות את
זה במפורש, לא להניח.

## היקף השלב הנוכחי
נבנתה **תשתית בלבד** (`TOTAL_SCREENS = 1`, מסך placeholder יחיד). אין
עדיין תוכן לימודי, אין מסך בחירת דמות (ייתכן שלא יידרש כלל כאן, אם
הבחירה כבר נעשתה בסיין קודם — לאשר/להחליט כשהמסך הראשון בפועל ייבנה).
ראו `ARCHITECTURE.md` לפירוט טכני מלא.

## פעולות המשך נדרשות (Prompt 1)
- לחלץ תוכן מ-`פרומפטים לבניית מסכים של סיין 5.docx` + מהתסריט
  (`מתמטיקה_יחס_יעד 1.5_090826_מוכן להפקה (1).pptx`) לבניית מסך 1
  בפועל. **חובה להשתמש בסקיל `pptx-script-extraction`** לשליפת התוכן
  מהתסריט (ראו `MUST-USE-skills.md` §8) — לא python-pptx רגיל/סקרינשוט.
- להחליט: האם סיין 5 פותח במסך בחירת-דמות משלו, או מניח שהבחירה כבר
  קיימת מ-localStorage — עדיין פתוח, לא הוכרע (זהה לשאלה הפתוחה בסיין 2).

---

## עדכון (2026-08-18) — Prompt 2: ארבעה מסכי-תוכן אמיתיים

**היקף**: תוכן אמיתי הוזן לתוך התשתית (Prompt 1). `TOTAL_SCREENS`
עודכן ל-**4**. משימת-תמלול (transcription) — כל התוכן/מפתחות-התשובה/
דפוסי-ה-UI נלקחו כפי-שסופקו (משקפים מעובדים של התסריט המקורי), לא
נגזרו-מחדש ולא "שופרו". פירוט טכני מלא ב-`ARCHITECTURE.md`.

**המסכים שנבנו:**
1. `s0` — מסך-מעבר עם בועיית-דיבור ("זמן ל – 3 שאלות מתקדמות" /
   "זה אומר שעלינו רמה!").
2. `s1` — שאלה 1/3, מסך-גלילה: משולש שווה-שוקיים ABC (3 זוויות VIQ) →
   טענת-אדריכל (SCQ, 3 אפשרויות). תמונה קבועה בצד שמאל.
3. `s2` — שאלה 2/3, **מסך סטטי** (לא גלילה, לפי הנחיה מפורשת): יחס-
   שטחים במשולש, שני קלטים ממוקמים כ-overlay אבסולוטי ישירות מתחת
   לדיאגרמה (28%/67% מרוחב-הדיאגרמה).
4. `s3` — שאלה 3/3 (אחרון), מסך-גלילה: ערבוב-צבעים ביחס 2:3:5 (VIQ,
   3 קלטים) → שינוי-יחס אחרי הכפלת השחור (SCQ, 4 אפשרויות). תמונה
   קבועה בצד שמאל.

**החלטות/פערים מפורשים שתועדו (ראו `ARCHITECTURE.md` לפירוט מלא של
כל אחד):**
- מסך-המעבר תוכנו נלקח משקף 55 בפועל, לא שקף 54 שהמסמך-המקור ציין —
  שקף 54 הוא שקף-מפריד-רכיב בלבד. הוכרע לפי תוכן, לא לפי היסט-מספור.
- מסך `s2`: תבנית-layout חדשה (דיאגרמה + overlay-קלטים אבסולוטי) —
  לא הייתה קיימת קודם בפרויקט. אחוזי-המיקום (28%/67%) נלקחו כפי שסופקו,
  לא נגזרו-מחדש. נמצא ותוקן תוך-כדי-בנייה: מעבר מ-`top:100%` (יחסי,
  שגוי בגלל padding-bottom שנוסף לשריון-מקום) ל-`top:193px` (קבוע).
- מסך `s3` (צבעים): אי-בהירות סדר-הספרות ב-"2:3:5" נחקרה ונפתרה
  *במקור* (לא כאן) — מפתח-התשובה (60/30/18, נתון 12) שימש כפי-שסופק,
  בלי חישוב-מחדש עצמאי.
- הבחנה מפורשת יושמה בין הדגשה-מלאה-של-פסקה (`s1` חלק ב') להדגשה-
  חלקית-בתוך-פסקה (`s3` חלק א') — בדיוק כפי שהתסריט ביקש לשים-לב אליה.
- סימון-זוויות: הותאם לתו ∢ (U+2222) הקיים כבר בפרויקט (סיין 1), לא
  לתו ∡ (U+2221) שהוזכר כדוגמה בהנחיה — לשם עקביות-פרויקט.
- מדיניות-רמז לשני מופעי-הרמז החדשים (`s1` חלק ב', `s3` חלק א'):
  "גלוי/פעיל מלכתחילה", פתיחה/סגירה עצמאיות — לא "אחרי-ניסיון-ראשון-
  שגוי" כמו המופע היחיד-הקיים בפרויקט (סיין 1) — כדי לא לגעת בפונקציות
  הגנריות `scqCheck`/`viqCheck` (שהתבקשו מפורשות להישאר "כפי-שהן").
- כותרת `wrongFinal.title` הוכרעה כ-`'לא נכון.'` לפי-רוב מבין שני
  ניסוחים קיימים בפרויקט (לא ניחוש).
- אין זום-תמונה על אף אחת משלוש התמונות החדשות — עקבי עם התקדים
  הקיים בפועל בסיין 2 (המקור-המדויק שהועתקה ממנו התבנית), לא סטייה
  חדשה.

**בדיקה סטטית**: `node --check` PASS, איזון-סוגריים/תגיות PASS,
הרצה-בפועל ב-Node עם DOM מדומה PASS (בלי ReferenceError/TDZ), בדיקה-
צולבת ממוקדת-קוד (onclick/getElementById/CSS classes/asset paths) —
**הכל OK, אין ממצא חסר**. פירוט מלא ב-`ARCHITECTURE.md` § "בדיקה
סטטית (Prompt 2)". **לא בוצעה בדיקה חזותית בדפדפן** (אין Playwright
זמין בסביבה הזו) — מומלץ לפתוח `index_dev.html` ולעבור ידנית על כל
ארבעת המסכים לפני מסירה, בפרט תקציב-הגובה של מסך `s2` הסטטי.
