# ARCHITECTURE.md — לומדת "מתמטיקה יעד 1.5" | יחס | סיין 6 (תשתית)

## הערה מתודולוגית — מקור האמת בפרויקט הזה
מסמך זה מתעד את **הקוד בפועל** בתיקייה הזו (`index.html`/`styles.css`/
`script.js`, נבדק ישירות), לא תיאור תיאורטי.

## רקע — תיקייה חדשה מאפס
בניגוד לסיין 2 (שנמצא כבר קיים על הדיסק כעותק גולמי של סיין 1), תיקיית
`methodica-math-ratio-01-06` **לא הייתה קיימת בכלל** לפני הבנייה הזו —
נבדק במפורש (`Glob`) לפני יצירת התיקייה. אין סיכון של תוכן-ישן שצריך
לנקות.

## מקור לרכיבי-התשתית
כל הרכיבים הגלובליים/המשותפים הועתקו/הותאמו מ-
`methodica-math-ratio-01-02` (**סיין 2 של אותו פרויקט**) — לא מסיין 1
ולא ישירות מ-`methodica-science-mass-measure-03-01` — לפי "לחפש לפני
שממציאים": סיין 2 הוא כרגע מקור-האמת המתודולוגי המאומת/מתוקן ביותר
בפרויקט (עבר סבבי-QA נוספים אחרי סיין 1). ראו
`methodica-math-ratio-01-02/ARCHITECTURE.md` לפירוט המלא של התיקונים
שכבר בוצעו במנוע הזה. מסכי-התוכן של סיינים 1+2 **לא הועתקו** — אינם
רלוונטיים לתשתית.

## Canvas ומנוע
זהה 1:1 לסיינים 1+2: קנבס **1280×710px**, `scaleApp()`
(`Math.min(innerWidth/1280, innerHeight/710)`),
`html,body{direction:rtl;overflow:hidden;font-family:'Assistant'}`,
ניווט מסכים דרך `goTo(n)`/`resetScreenState(n)`/`TOTAL_SCREENS=1`,
ניווט מקלדת (`Ctrl+←`/`Ctrl+→`), dev postMessage bridge ל-`index_dev.html`.

## closeAllPopupsAndHints() — מועתק כפי-שהוא מהגרסה המתוקנת של סיין 1/2
```js
function closeAllPopupsAndHints() {
  document.querySelectorAll('[id$="-feedbox"]').forEach(function (el) {
    el.classList.remove('visible');
  });
  document.querySelectorAll('[id$="-hint-overlay"]').forEach(function (el) {
    el.hidden = true;
  });
}
```
`goTo(n)` קורא לפונקציה הזו לפני כל החלפת מסך.

## מוסכמת ID לפופ-אפים
`<prefix>-feedbox` לפופ-אפ משוב, `<prefix>-hint-overlay` לפופ-אפ רמז —
זהה לסיינים 1+2.

## Subject theme — Math (11 טוקנים מלאים)
זהה **בדיוק** לסיינים 1+2 (אותו יעד/נושא — "יחס" — אותה ערכת-צבע):

| טוקן | ערך Math |
|---|---|
| `--subject-100` | `#EAD9FF` |
| `--subject-200` | `#CEC7FF` |
| `--subject-300` | `#E1D9FF` |
| `--subject-350` | `#B3B0FF` |
| `--subject-400` | `#BDB3FC` |
| `--subject-500` | `#3B2FC2` |
| `--subject-600` | `#383896` |
| `--subject-700` | `#5656CC` |
| `--subject-800` | `#4B35A5` |
| `--subject-900` | `#2F2478` |
| `--subject-alpha-100` | `rgba(165, 28, 255, 0)` |
| `--subject-alpha-200` | `rgba(165, 28, 255, 0.3)` |

### גרדיאנט Continue — עדיין הנחה, לא אישור מלא (ירושה מסיינים 1+2)
`.btn-continue` משתמש ב-`linear-gradient(180deg, #6F6AD5 8.654%, #4B35A5
38.462%, #270075 100%)`. עדיין לא סופית מאומת מול node Figma של וריאציית
Math בפועל (ראו סיין 1/2 ARCHITECTURE.md § "ביקורת התאמה לפיגמה") —
משותף לכל 6 סיני היעד, לא רק לסיין הזה.

## בר-ניווט תחתון (`.btn-continue`/`.btn-back`/`.btn-hint`)
זהה לסיינים 1+2 (מקור: `SELF-QA-lomda.md` §6, "Reference CSS", אומת
מול Figma) — `height:39px` מפורש, `min-width:140px`, `var(--subject-700)`
(לא `--subject-500`), גרדיאנט Math לכפתור Continue. `.btn-hint
.scq-hint-icon` דרך CSS `mask` על `icon-bulb-outline-white.svg` (עדיין
לא הועתק לתיקייה הזו — **חסר**, ראו § "פערים ידועים" למטה).

## מסך 1 — Placeholder (`data-screen="0"`, `id="s0"`)
אין תוכן לימודי. קופסה ניטרלית (`.placeholder-box`, מסגרת מקווקוות
`var(--subject-350)`, רקע `var(--subject-100)`) עם כותרת ותת-כותרת
גנריות, ממורכזת בקנבס — **נבנתה מחדש** לפי התיאור ההיסטורי המתועד
ב-`methodica-math-ratio-01-02/ARCHITECTURE.md` (הגרסה המקורית שם כבר
נמחקה כקוד-מת), בגדלי-גופן מהסולם המאושר (32px/700 לכותרת, 24px/400
לתת-כותרת — לא 28px/700 כמו הגרסה המקורית ההיא שכבר נמצאה שגויה). בר
תחתון:
- `.btn-back` (`id="s0-back"`) — **`hidden`**, לפי המוסכמה "Never shown
  on the first screen".
- `.btn-continue` (`id="s0-continue"`) — **`disabled`**, `onclick` טרם
  מחובר: אין עדיין יעד ניווט אמיתי (`TOTAL_SCREENS=1`).
- **אין** מופע `.btn-hint` במסך הזה — אין שאלה. ה-CSS מוכן ב-`styles.css`
  למסך השאלה הראשון.

## רכיבים גלובליים — מוכנים, טרם מחוברים לתוכן
- **`.flag-btn`** — מופע יחיד, מחוץ לכל `.screen`, ישירות ב-`#app`.
  ויזואלי בלבד. נכסים: `assets/images/btn-flag-default.png`/
  `btn-flag-hover.png` — הועתקו מסיין 2 (ניטרליים, ללא צבע-נושא אפוי).
  מיקום `top:32/left:32` — כבר מתוקן לפי Figma (לא 16/16 הישן).
- **`.img-zoom-btn`/`#img-zoom-modal`** — מופע-משותף יחיד, מחוץ לכל
  `.screen`. CSS+JS (`imgZoomOpen`/`imgZoomClose`, delegated click
  handler, `Escape`-to-close) זהים לסיינים 1+2. אין עדיין שום תמונת-תוכן
  שמפעילה אותו.
- **`.scq-fb-box`** (Feedback popup) — CSS + JS (`clampPopupPosition`/
  `scqFbResetPosition`/`scqFbMakeDraggable`) מוכנים, בלי מופע HTML —
  `scqFbMakeDraggable(boxId)` no-ops בבטחה אם ה-id לא קיים עדיין.
- **`.scq-hint-overlay`/`.scq-hint-popup`** — CSS מוכן, בלי מופע HTML.
  **⚠️ תוקן ביחס למקור:** צבע ה-backdrop (`rgba(47, 36, 120, 0.35)`,
  נגזר מ-`--subject-900`) — הועתק כבר-מתוקן. הגרסה בסיין 2 (מקור-ההעתקה)
  עצמה עדיין הכילה `rgba(0,32,96,0.35)` (Science, דלף) עד לתיקון בו-זמני
  שבוצע יחד עם הבנייה הזו (ראו סיין 2 ARCHITECTURE.md).
- **Companion character system** — `CHARACTER_STORAGE_KEY =
  'math-ratio-01_selectedCharacter'` (משותף לכל הסינים ברמת-היעד, לא
  ברמת-הסיין), `resolveCharBubbleImg`/`resolveCharBubbleVideo` מוכנים,
  לא נקראים עדיין.

## פערים ידועים (Prompt 1, תשתית בלבד — היסטורי, נסגר ב-Prompt 2)
- `assets/images/icon-bulb-outline-white.svg` (נדרש ל-`.btn-hint`),
  `assets/images/icon-check-white.svg`/`icon-x-white.svg` (לתגי נכון/
  שגוי), `assets/images/gesture-hand-cursor.svg` (ל-Gesture Hint אם
  ייבנה מסך-גלילה) — **לא הועתקו לתיקייה הזו עדיין**, כי אין עוד שום
  שימוש בהם (לא נמצא-שימוש = לא מעתיקים מראש, לפי עקרון "לא לבנות
  תוכן/נכסים מראש בלי צורך מוכח" של שלב-התשתית). יועתקו מסיין 2 ברגע
  שהמסך הראשון שבאמת צריך אותם ייבנה.

## בדיקה סטטית (Prompt 1, תשתית בלבד — היסטורי)
`node --check script.js` → PASS. איזון `{`/`}` ב-`styles.css`: 74/74
PASS. איזון-תגיות ב-`index.html`: 9/9 `<div>`, 1/1 `<section>` PASS.
**לא ניתן לאמת חזותית בסביבה הזו** (אין דפדפן/Playwright זמין) — יש
לפתוח `index_dev.html` בדפדפן ולוודא שהקנבס נטען תקין (placeholder ריק,
בלי שגיאות בקונסול) לפני מעבר ל-Prompt 2.

---

# Prompt 2 — 3 מסכי תוכן (19.08.2026)

## הערה מתודולוגית
כל התוכן (טקסטים, מפתחות-תשובה, טקסטי-משוב ורמז) הועבר **כפי-שסופק**
על-ידי המשתמש (כבר חולץ מה-pptx בהפקה מראש) — לא הומצא ולא נגזר-מחדש
כאן, כולל במקום שבו ניתן היה לחשב תוצאה מעט שונה בעצמי (ראו § "אי-
עקביות ידועה בהסבר סעיף ב'" למטה). איפה שההוראות היו עמומות (חלוקת
הטקסט במסך 1, גיאומטריית "כרטיס-מידע מעל תמונה" במסך 2, ומנגנון
ה-bleed במסך 3), ההחלטה מתועדת למטה כשיקול-דעת מפורש, לא כניחוש-שקט.

## מקור לרכיבים גנריים
`.progress-question`/`updateProgressQuestion`/`syncPracticeProgressNav`/
`setCurrentQuestion`, `.viq-*`/`viqOnInput`/`viqCheck`/`viqFinish`/
`VIQ_CFG`/`VIQ_CFG_REGISTER`, `.scq-qtext-em`, `.btn-hint-row`,
`.s3-fixed-images`/`.s3-img-frame`(+`--diagram`)/`.s3-scroll-area`/
`.s3-scroll-inner`/`.s3-part`/`.s3-body`/`.s3-inline-btn`,
`.gesture-hint-*` (כולל שני ה-keyframes), `.scq-fb-box`/`.scq-hint-
overlay`/`.scq-hint-popup`, `scqFbResetPosition`/`scqFbMakeDraggable`
— כולם הועתקו **כפי-שהם** ממסך-הגלילה עם דיאגרמה-קבועה בסיין 3
(`methodica-math-ratio-01-03`, מסך 4/`id="s3"`), בלי שינוי ערך. מסך 1
(מסך-מעבר, `.transition-*`) הועתק כפי-שהוא מ-`methodica-math-ratio-01-04`
מסך 1, לפי הנחיית-הדוקס המפורשת. `VIQ_CFG`/`viqOnInput`/`viqCheck`/
`viqFinish` **לא שונו כלל** — רק `VIQ_CFG` מכיל מפתחות חדשים (`s2p1`/
`s2p2`/`s2p4`/`s2p5`). אין `SCQ_CFG`/`MCQ_CFG` בסיין הזה — 4/4 השאלות
האמיתיות הן ValueInputQuestion בלבד, לפי תוכן התסריט בפועל (אין שאלת-
בחירה בסיין הזה כלל).

## מסך 1 — מסך מעבר (`data-screen="0"`, `id="s0"`)
מבנה מועתק 1:1 מ-`methodica-math-ratio-01-04` (`.transition-content`/
`.transition-title-block`/`.transition-avatar-wrap`/`.transition-bubble`),
לפי הנחיית-הדוקס המפורשת. **שיקול-דעת מתועד — חלוקת הטקסט**: המקור
(שקף 62) לא מסמן בעצמו איזו שורה היא כותרת/תת-כותרת/בועית-דיבור —
רק שורה אחת מודגשת-בבולד ("עכשיו מגיעה שאלת השיא"). מיפוי שנבחר:
`.transition-sub` #1 = "כל הכבוד! הסתיים שלב התרגול." (פתיחה קצרה),
`.transition-title` (הבולט היחיד במקור) = "עכשיו מגיעה שאלת השיא",
`.transition-sub` #2 = "יש בה 4 סעיפים. עליכם לענות נכון על 3 לפחות."
(המשך-הנחיה), `.transition-bubble` (טקסט-בקול-הדמות) = "אם לא הצלחת
לענות נכון על חלק מהשאלות, חשוב להבין מה עדיין לא ברור ולפנות לצ'אט
או למורה להסבר נוסף." (הפנייה האישית ביותר, מתאימה-בטבעיות לקול-דמות).
כל מילה מהמקור נשמרה, בלי הוספה/השמטה. דמות: `resolveCharBubbleVideo`
+ `S0_AVATAR_ASSETS` (character-1→boy-avatar-thinking.mp4, character-2→
yellow-avatr-asking.mp4) — אותן שתי פוזות (חשיבה/שאילה) ששימשו כבר
במסכי-המעבר של סיינים 3/4/5, לא נכס-ייעודי (אין כזה בנכסים) — אותה
מוסכמה בדיוק, טרם אושרה מול המפיקה. נכסי-הווידאו הועתקו מ-
`methodica-math-ratio-01-05/assets/videos/` (זהים בשני הסינים).

**⚠️ עודכן (30.08.2026) — נסגר.** נכס-וידאו ייעודי סופק במפורש:
`character-1→boy-avatar-climbing.mp4`, `character-2→
yellow-avatar-climbing.mp4` (שני הקבצים כבר קיימים ב-`assets/videos/`).
`S0_AVATAR_ASSETS` עודכן בהתאם.

## מסך 2 — תמונה ממלאת-מסך + תגית-מידע (`data-screen="1"`, `id="s1"`)
**סוג-מסך חדש בפרויקט הזה — אין תקדים ישיר** (בניגוד לרוב המסכים
האחרים בפרויקט, שהועתקו ממסך קיים). נבנה לפי דפוס גנרי "כרטיס-מידע
לבן מעל תמונה" (`.info-banner-card`, class גנרי בכוונה — נעשה בו שימוש
חוזר גם בחלק-הביניים במסך 3) + הגיאומטריה שנמדדה ישירות מה-pptx בסלייד
63 (יחסים באחוזים, לא ערכי-pt מילוליים): `top:2.5%`/`left:1.1%`/
`width:38%` מתוך תיבת-התמונה (1280×636, לא כל הקנבס — התיבה עצמה
מסתיימת ב-`bottom:74px`, מעל בר-הניווט). שתי שכבות-הטקסט (label
קטן/מודגש + body רגיל) ממופות לשתי הפסקאות שסופקו במפורש בתסריט
(שקף 63) — לא הומצא טקסט נוסף. `family-home.jpg` (1281×639px בפועל)
ממולא ב-`object-fit:cover` לתוך תיבה 1280×636 — יחס-רוחב-גובה כמעט
זהה (2.00 מול 2.01), כך שה-crop מינימלי. **בלי כפתור-זום** — עקביות
עם החלטה זהה במסכי-הגלילה עם דיאגרמה-קבועה בסיינים 3/5 (ראו § "זום-
תמונה" למטה).

## מסך 3 — "שאלת השיא", 4 סעיפים + חלק-ביניים תמונתי (`data-screen="2"`,
`id="s2"`)
מבנה-גלילה מועתק מסיין 3 (`.s3-fixed-images`/`.s3-scroll-area`/
`.s3-scroll-inner`/`.s3-part`). דיאגרמת המגרש (`land-plot-diagram.png`,
971×802px, כבר בנכסים) קבועה וזהה בארבעת הסעיפים (א/ב/ג/ד) — לא
מתחלפת per-part כמו `s5SetPhoto` בסיין 3 — ולכן פשוט `<img>` יחיד
קבוע בתוך `.s3-img-frame.s3-img-frame--diagram` (מודיפייר-הרקע-הבהיר
הקיים, מתאים לדיאגרמה טכנית).

### הכללה נדרשת ל-Progress Question — 4 שאלות על מסך פיזי אחד
בניגוד לתקדימים בסיינים 1/3 (שם כל שאלה חיה על `data-screen` נפרד
משלה), כאן **ארבע** השאלות הלוגיות (סעיפים א/ב/ג/ד) חיות כולן על אותו
מסך פיזי יחיד (`data-screen="2"`) — `practiceProgress.questions[i].screen`
מוגדר `2` בעבור כל ארבעתן (לא `null` — הן כן "חיות" על מסך קיים, רק
שהוא לא-שונה מ-`currentScreen` בזמן שהלומד/ת נמצא/ת עליו, ולכן אף
פריט לא הופך ל-clickable דרך ה-nav בפועל — זו התנהגות תקינה, לא ברירת-
מחדל לא-מכוונת: אין "מסך אחר" לנווט אליו). **`resetScreenState2()`
מכליל את דפוס `resetScreenState5` של סיין 3** (שם היה תנאי בודד —
"שאלה 1 נפתרה? כן/לא") ללולאה על 4 השאלות: מוצא את השאלה
הראשונה-שטרם-נפתרה (או את האחרונה, אם כולן נפתרו) ומסמן אותה
`current` — כך שחזרה למסך (למשל דרך "חזרה") ממשיכה בדיוק מהנקודה
הנכונה, בלי לאפס דבר מ-DOM/state (חשיפת-חלקים/`disabled`/`viqState`
נשארים כפי שהם לכל אורך חיי העמוד, לפי אותה מוסכמה בדיוק כמו כל
מסכי-הגלילה מרובי-החלקים בפרויקט הזה — לא נדרש state נוסף מעבר לזה).
נבדק ישירות (סימולציית stubbed-DOM): פתרון עד סעיף ד', חזרה למסך 1
וחזרה למסך 2 — מצב ה-progress נשאר זהה (אידמפוטנטי), כמצופה.

### חלק-ביניים תמונתי (חלק 3 מתוך 5, לא-מנוקד)
`s2-part-3` — בלי VIQ, בלי `progress-question` slot משלו (לפי הנחיה
מפורשת: "לא לתת סלוט-שאלה לחלק הזה"). לפי בקשה מפורשת בתסריט (שקף 66),
דיאגרמת-המגרש מוסתרת בזמן החלק הזה (לא רלוונטית לתוכנו) ומוחזרת עם
המעבר לסעיף ג'. מנגנון-החשיפה עצמו (`s2ShowPart`) מועתק כפי-שהוא מ-
`s3ShowPart`/`s5ShowPart` בסיין 3 (כולל התיקון הכפול `s2ProgrammaticScroll`
נגד "בליעת" מאזין ה-Gesture Hint).

**שיקול-דעת מתועד — "רוחב-מלא" לתמונת-חלק-הביניים, בלי `bleed`
ב-margin שלילי:** ההנחיה אפשרה שתי גישות — (א) `margin-left` שלילי על
`.s2-part-3` עצמו, או (ב) "restructure so this part isn't nested under
the same width constraint". נבדק ונשלל (א): `.s3-scroll-area` (האב
הגולל) מגדיר `overflow-x:hidden` (חלק הכרחי מדפוס "פס-גלילה תמיד
בצד ימין" של הפרויקט) — `overflow-x:hidden` **קוצץ** כל תוכן שחורג
משטח-האב, כך ש-`margin-left` שלילי החורג מעבר לגבול-האב (x=360)
היה נחתך (בלתי-נראה), לא "יוצא" ממנו. לכן נבחרה גישה (ב), בגרסה
פשוטה יחסית: כש-`s2HideDiagramForInterlude()` מוסתרת הדיאגרמה, גם
`.s3-scroll-area` עצמה מתרחבת זמנית (`.s2-full-width`, `left:40px`
במקום `left:360px`) — כי השטח שהדיאגרמה תפסה ממילא פנוי. זו **הרחבת
כל העמודה המשותפת**, לא רק חלק 3 עצמו — כלומר חלקים א'/ב' (שכבר
נענו וגלויים מעל חלק 3 באותה נקודה) עשויים לזוז מעט שמאלה חזותית
לרגע-אחד עם ההרחבה (טרייד-אוף מתועד, לא באג-נסתר). לא נבחר `width:
calc(100vw)`/רוחב-קנבס-מלא-פיזי (0-1280) במדויק — התמונה משתרעת עד
`left:40/right:40` (1200px, זהה למרווחי-40px הסטנדרטיים כבר קיימים
בפרויקט הזה, למשל `.progress-question`), לא עד קצוות-הקנבס הפיזיים
ממש — נבחר במכוון לביטחון מבני (בלי סיכון-חיתוך/גלילה-אופקית) על
פני התאמה פיקסלית-מדויקת להנחיה, כפי שההנחיה עצמה הרשתה במפורש
("doesn't need to be pixel-exact").

### שיקולי-דעת נוספים (לא ניחוש שקט — מתועד)
- **פורמט תשובה "תווית + קלט + יחידה"**: "שטח המגרש הוא ___ מ\"ר"
  מומש כ-`.viq-answer-row` (label+input+unit), אותו דפוס בדיוק כמו
  שאלות-הזווית של סיין 5 ומסך 6-השאלות-האחרון של סיין 1 (`.viq-
  answer-unit`, כבר מוגדר גנרית בסיין 5 — הועתק לכאן ולא הומצא שם-
  class חדש).
- **סדר-DOM בכל 4 שורות-הרמז**: רמז ראשון, "צדקתי?" שני (`.btn-hint-
  row`) — לפי המוסכמה המתוקנת-בפועל (19.08.2026) בכל שאר הסינים.
  מדיניות-הרמז: גלוי/פעיל מלכתחילה (ברירת-המחדל של התבנית — אין
  הנחיה מפורשת בתסריט לסטות ממנה, בניגוד לסעיף-הזווית של מסך 6 בסיין 1
  ששם נבחרה מדיניות "מוסתר-עד-ניסיון-שגוי").
- **אי-עקביות ידועה בהסבר סעיף ב' (לא תוקנה בכוונה)**: טקסט-ההסבר
  שסופק לסעיף ב' קובע "היחס בין AT ל-TB הוא 3 : 2. נחשב את AT:
  2/5⋅20=8" — יש כאן חוסר-עקביות ניסוחי לכאורה (3:2 מרמז-מקדם 3/5,
  לא 2/5). לפי כלל-הפרויקט הקבוע "תוכן כפי-שסופק, לא נגזר-מחדש", לא
  תוקן/נגזר-מחדש כאן — הועתק מילה-במילה, בדיוק כמו תקדים-זהה בשאלת
  ערבוב-הצבעים ב-`methodica-math-ratio-01-05/ARCHITECTURE.md`.
- **זום-תמונה (עדכון 06.09.2026)**: כפתור-הזום (`.img-zoom-btn`) הופעל
  על דיאגרמת-המגרש (`land-plot-diagram.png`) וגם על `family-home.jpg`
  ו-`engineers.jpg`, לפי בקשה מפורשת ("על כל תמונה בלומדה חוץ מדמויות").
  מבטל את ההחלטה הקודמת (ראו היסטוריית git) לשמור עקביות עם סיינים
  3/5 שלא הפעילו זום על הדיאגרמות שלהם — סטייה מכוונת מהתקדים הישן,
  לא רטרו-תיקון של סיינים 3/5 עצמם (לא נגעו בהם כאן; טופלו בנפרד).
- **בסיס-ניווט-סופי**: כפתור "המשך" הראשי במסך 3 (האחרון בסיין
  ובכל היעד) הוא `onclick="goTo(3)"` — no-op בטוח (`TOTAL_SCREENS=3`
  → אינדקסים תקפים 0-2 בלבד), בדיוק לפי המוסכמה המתועדת כבר בסיינים
  1/3. יעד-ניווט אמיתי לסוף-היחידה דורש אישור-מוצר, לא הומצא כאן.

## נכסים שהועתקו לתיקייה הזו (Prompt 2)
- וידאו: `boy-avatar-thinking.mp4`, `yellow-avatr-asking.mp4` (ממסך 1)
  — הועתקו מ-`methodica-math-ratio-01-05/assets/videos/`.
- אייקונים: `icon-bulb-outline-white.svg`, `icon-check-white.svg`,
  `icon-x-white.svg`, `icon-check-green-16.svg`, `icon-x-red-16.svg`,
  `gesture-hand-cursor.svg` — הועתקו מ-`methodica-math-ratio-01-03/
  assets/images/`, סוגר את כל הפערים שתועדו ב-Prompt 1 (§ "פערים
  ידועים" למעלה).
- `family-home.jpg`/`engineers.jpg`/`land-plot-diagram.png` כבר היו
  בתיקייה מראש (לא נדרש חילוץ/הורדה).

## בדיקה סטטית (Prompt 2)
- `node --check script.js` → **PASS**.
- **Load-and-execute עם DOM מדומה** (סקריפט Node ייעודי, `vm.createContext`,
  לא רק `--check`): הסקריפט נטען ורץ בלי `ReferenceError`
  מ-temporal-dead-zone; סימולציה מלאה של הזרימה — סעיף א' (216, נכון),
  סעיף ב' (שגוי פעמיים → חושף 80), חלק-ביניים, סעיף ג' (26, נכון),
  סעיף ד' (28, נכון) — כל התוצאות תואמות למפתחות-התשובה שסופקו; כפתור
  "המשך" הראשי מתאפשר בסיום; חזרה למסך 1 וחזרה למסך 2 משמרת את מצב
  ה-progress (אידמפוטנטי); `goTo(3)` (יעד-הניווט הסופי) נשאר בבטחה על
  מסך 2 (no-op) — **PASS**.
- **סריקה מלאה (לא מדגמית) של כל class ב-`index.html` מול `styles.css`**
  (סקריפט Node ייעודי, לא בדיקה ידנית/מדגמית): 74 classes ייחודיים,
  כולם מוגדרים — **PASS**.
- **סריקה מלאה של כל `onclick`/`oninput` מול `script.js`**: 18 שמות-
  פונקציה ייחודיים, כולם מוגדרים בפועל — **PASS**.
- **סריקה מלאה של כל נתיב-נכס** (`src=`/`url()` ב-HTML/CSS, ומחרוזות
  `assets/...` ב-JS) מול הדיסק בפועל: 14 (HTML/CSS) + 2 (JS) נתיבים,
  כולם קיימים — **PASS**.
- **איזון-תגיות** (`<div>`/`<section>`/`<nav>`/`<button>`/`<span>`/`<p>`):
  כולם תואמים (80/80, 3/3, 1/1, 21/21, 22/22, 25/25) — **PASS**.
- **איזון `{`/`}`** ב-`styles.css`: 156/156 — **PASS**.
- **צילום-מסך/QA חזותי — לא בוצע.** אין דפדפן/Playwright זמין בסביבה
  הזו (זהה למגבלת Prompt 1) — יש לפתוח `index_dev.html` בדפדפן ולעבור
  ידנית על שלושת המסכים (מעבר → תמונה → 4 סעיפים + חלק-ביניים) לפני
  אישור סופי, כולל בדיקת: מיקום/רוחב תגית-המידע במסך 2 מול ה-pptx,
  הרחבת עמודת-הגלילה בזמן חלק-הביניים במסך 3 (בלי גלילה אופקית),
  והתנהגות ה-Gesture Hint בכניסה למסך 3.

## עדכון (2026-08-19) — הנחיית-מוצר מפורשת: פופאפ-משוב סטטי מתחת למסיחים

זהה בעקרון לתיקון שבוצע באותו יום בסיינים 1/2/3/5 (ראו
`methodica-math-ratio-01-01/ARCHITECTURE.md` לרקע המלא). מסך `s2`
("שאלת השיא") הוא היחיד עם שאלות בסיין הזה, והוא מסך-גלילה — **כל**
ארבעת תיבות-המשוב (`s2-p1/p2/p4/p5-feedbox`) הפכו ל-`.scq-fb-box.is-static`,
זורמות מיד אחרי כפתור-הבדיקה בתוך `.btn-hint-row`/`.s3-part`
הרלוונטי (חלק-הביניים התמונתי, `s2-part-3`, אינו מנוקד ואין לו
תיבת-משוב כלל — לא רלוונטי). הוסרה דריסת-מיקום ישנה
(`#s2-p1/p2/p4/p5-feedbox{left:360px}`). מערך-האתחול היחיד
ב-`script.js` הוסר לגמרי — אין יותר אף תיבת-משוב גרירה בסיין הזה.

**אימות**: איזון `<div>` (80/80 PASS), איזון `{`/`}` (156/156 PASS),
`node --check` PASS, טעינה-אמיתית ב-Node/vm PASS. לא ניתן לאמת חזותית
בסביבה הזו — נוסף לרשימת-הבדיקה הידנית שכבר קיימת למעלה.

## עדכון (2026-08-19) — Gesture Hint (Cursor Scroll): מיקום + תזמון-הצגה

זהה לתיקון שבוצע באותו יום בסיינים 1/2/3/5 — ראו
`methodica-math-ratio-01-01/ARCHITECTURE.md` לרקע/הצדקה המלאים. מסך
`s2` הוא היחיד עם Gesture Hint בסיין הזה. **מיקום**:
`.gesture-hint-scroll-wrap .gesture-hint { left: 92%; }` (אין
`.gesture-hint-drag-wrap` בסיין הזה). **תזמון**:
`s2MaybeShowScrollGesture()` בודקת `scrollHeight>clientHeight` לפני
הצגה; נקראת שוב מ-`s2ShowPart` בכל חשיפת-חלק (כולל דרך
`s2Part3Continue`, שכבר קוראת ל-`s2ShowPart(4)`).

**אימות**: איזון `{`/`}` נספר מחדש, `node --check` PASS, טעינה-אמיתית
ב-Node/vm PASS. לא ניתן לאמת חזותית בסביבה הזו — חשוב לוודא בדפדפן
שהמיקום `left:92%` לא מתנגש עם ההרחבה הזמנית `.s2-full-width` של
עמודת-הגלילה בחלק-הביניים (סעיף ד' לא מגיע — הרמז אמור כבר להיות
מוסתר עד אז בכל מקרה).

## עדכון (2026-08-19) — משוב נשאר מוצג גם אחרי מעבר לשאלה הבאה

זהה לתיקון שבוצע באותו יום בשאר הסינים — ראו
`methodica-math-ratio-01-01/ARCHITECTURE.md` לפירוט מלא + בדיקה
התנהגותית מאומתת (סימולציה מוודאת בפועל שהגלילה נכונה אל תיבת-המשוב,
לא אל החלק החדש). חל על `s2ShowPart` (המסך היחיד עם שאלות בסיין הזה):
גוללים עכשיו אל תיבת-המשוב-הגלויה של החלק הקודם, לא אל החלק החדש
עצמו — כולל דרך `s2Part3Continue`, שכבר קוראת ל-`s2ShowPart(4)`.

**אימות**: `node --check` PASS, בדיקה התנהגותית ממוקדת (stub-מבנה
מדמה `part1`+`feedbox`+`part2`) מוודאת שהגלילה מתמקדת בתיבת-המשוב, לא
בחלק החדש. לא ניתן לאמת חזותית בסביבה הזו.

## עדכון (2026-08-20) — Gesture Hint: תוקן שוב, לא היה קרוב מספיק בפועל

זהה לתיקון שבוצע באותו יום בשאר הסינים — `left:92%` (19.08.2026)
הוחלף ב-`right:6px` קבוע-בפיקסלים. ראו
`methodica-math-ratio-01-01/ARCHITECTURE.md` להסבר המלא.

## עדכון (30.08.2026) — ניווט בין-סיינים (חוצה יעד)

כפתור "חזרה" במסך הראשון (`s0`) הופעל (הוסר `hidden`, או נוסף
מאפס אם לא היה קיים כלל) — מוביל ל-`../methodica-math-ratio-01-05/index.html
?screen=last`. נוסף `if (new URLSearchParams(location.search).
get('screen') === 'last') goTo(TOTAL_SCREENS - 1);` מיד אחרי
הצהרת `TOTAL_SCREENS`/`currentScreen`, כדי שהניווט-חזרה הזה
יפתח כאן ישר במסך האחרון (לא בברירת-המחדל, מסך 0).

אין שינוי בכפתור "המשך" של המסך האחרון — זהו הסיין האחרון
ביעד, לפי בקשה מפורשת ("חוץ מסיין אחרון").

כל הפרויקטים בתיקיית `methodica-math-ratio-01/` הם תיקיות-אחיות
(deploy כתיקיות-שכנות), כך ש-`../<תיקיית-סיין>/index.html` הוא
נתיב יחסי תקף גם בהפקה, לא רק מקומית — עקבי עם המוסכמה הקיימת
כבר לפריסת-חלקי-לומדה (ראו `_global-components.md` → Companion
character system → Cross-part persistence).

## עדכון (07.09.2026) — מסך 3 פוצל ל-3 מסכים נפרדים, נוסף מסך-סיום ריק

לפי בקשה מפורשת: מסך 3 המקורי (id="s2", "שאלת השיא" 4 סעיפים א/ב/ג/ד
+ חלק-ביניים תמונתי בין ב' ל-ג') פוצל ל-3 מסכים עצמאיים —
`TOTAL_SCREENS` עלה מ-3 ל-6:

- **מסך 3 (id="s2", data-screen="2")** — נשאר עם סעיפים א+ב בלבד.
  ניווט "המשך" נדלק עכשיו אחרי ב' (במקום אחרי ד' כמו קודם).
- **מסך 4 (id="s3", data-screen="3")** — חדש. חלק-הביניים התמונתי
  (engineers.jpg) עבר לכאן, כ"מסך תמונה ממלאת-מסך" עצמאי — **אותו
  דפוס בדיוק כמו מסך 2** (id="s1", family-home.jpg): אותם classes
  ממש (`.s1-photo-wrap`/`.s1-photo-img-frame`/`.s1-photo-img`/
  `.s1-info-banner`), לא class נפרד. טקסט-הכרטיס (label+body) הועתק
  כלשונו מ-`.s2-interlude-banner` הישן. אין progress-nav (כמו מסך 2).
  ⚠️ עודכן (07.09.2026, לפי בקשה מפורשת נוספת: "לא צריך הגדלת תמונה
  לשתיהן [מסך 2+4], הן מאוד גדולות") — `.img-zoom-btn` הוסר גם ממסך 2
  וגם ממסך 4 (שתיהן תמונות ממלאות-מסך-מלא, כפתור-הגדלה מיותר).
- **מסך 5 (id="s4", data-screen="4")** — חדש. סעיפים ג+ד עברו לכאן,
  עם דיאגרמת-מגרש עצמאית משלו (מופע נפרד, לא משותף עם מסך 3). id-ים
  פנימיים (`s2-part-4`/`s2-p4-*`/`s2-part-5`/`s2-p5-*`) לא שונו כלל —
  כל הפונקציות הקיימות ב-script.js (`s2P4Check` וכו') ממשיכות לעבוד
  בלי שינוי, למרות ש"החיים" עכשיו בתוך `<section id="s4">`, לא `s2`.
  ⚠️ תוקן (07.09.2026, לפי בקשה מפורשת: "יש 4 סעיפים של שאלה, צריך
  שיהיו 4 שאלות על סרגל ההתקדמות, לא משנה שיש שבירה במסך 4") —
  ניסיון-ראשון (אותה קומיט) יצר `practiceProgress2` נפרד (2 שאלות,
  מתחיל-מחדש) לכל מסך — **נדחה מיד** לפי הבקשה הזו: `practiceProgress`
  חזר להיות מונה-יחיד-משותף של 4 שאלות (א/ב/ג/ד), עם **שני** מופעי-
  nav (`#s2-progress`/`#s4-progress`) שמסונכרנים אליו יחד
  (`syncBothProgressNavs()`) — לא שני state-ים נפרדים.
- **מסך 6 (id="s5", data-screen="5")** — חדש, פלייסהולדר ריק לגמרי
  ("מסך סיום של שאלת-השיא") — לפי בקשה מפורשת: "כרגע המסך יהיה ריק
  עד שנחליט מה לשים בתוכו". אותו דפוס-פלייסהולדר-סטנדרטי שכבר קיים
  בפרויקט (מסגרת מקווקוות subject-350, רקע subject-100, טקסט
  subject-700, ראו `.s5-placeholder-box`).

**קוד שהוסר (לא רלוונטי יותר)**: `s2HideDiagramForInterlude`/
`s2RestoreDiagram`/`s2MaybeRestoreDiagram`/`s2WireDiagramScroll`/
`s2DiagramRestored`/`s2DiagramScrollWired` (script.js) ו-
`.s3-scroll-area.s2-full-width`/`.s2-interlude-frame`/
`.s2-interlude-img-frame`/`.s2-interlude-img`/`.s2-interlude-banner`
(styles.css) — היו קיימים אך ורק כי חלק-הביניים התמונתי חי באותה
עמודת-גלילה כמו הדיאגרמה-הקבועה; עכשיו שהתמונה מסך-נפרד משלה, אין
יותר צורך בהם.

**אימות**: `node --check` על script.js → PASS. איזון-תגיות מלא
ב-index.html (`div`/`section`/`button`/`nav`/`span`/`p`) → PASS.
כל `onclick`/`oninput` נבדק מול `function` declarations בפועל,
וכל `getElementById`/מפתח-VIQ_CFG נבדק מול ה-`id`-ים הקיימים
בפועל → PASS. **לא ניתן לאמת חזותית/פונקציונלית בסביבה זו** (אין
דפדפן/Playwright זמין) — יש לבדוק בדפדפן: זרימת הניווט המלאה
(1→2→3→4→5→6, ו"חזרה" בכל כיוון), שהמעבר בין מסך 3 למסך 5 לא
"שוכח" תשובות-שכבר-נענו (resume-state), ושהתמונה במסך 4 נראית
כמצופה (גדולה, כמו מסך 2).

## עדכון (07.09.2026) — `s2p5` (מסך 5, סעיף ד'): יחידה-אחרי-משוואה מחוץ ל-dir="ltr"

דיווח: "אותה טעות חוזרת על עצמה במשוב". `"מ"ר"` ישב כטקסט-רגיל
*מחוץ* ל-`dir="ltr"` שעוטף כל משוואה (`216 : 2 = 108`, `108 - 80 =
28`) — אותה בעיה בדיוק שתועדה ותוקנה אותו יום ב-`methodica-math-
ratio-05-03/script.js` § `s5p2` וב-`methodica-math-ratio-05-05/
script.js` § `VIQ_CFG_S2_BODY` (ראו שם להסבר ה-bidi המלא). תוקן:
`dir="rtl"` מקונן סביב "108 מ"ר"/"28 מ"ר" (סדר-מקור מספר-ואז-יחידה),
בתוך ה-`dir="ltr"` החיצוני שנשאר כמו שהוא.

## עדכון (07.09.2026, המשך) — מסכים 3+5 (`s2`/`s4`): הגדלת התמונה המלווה

דיווח: "מסכים 3+5 צריך להגדיל את התמונה המלווה". `.s3-fixed-images`/
`.s3-img-frame` (המשמשים אך-ורק את שתי הדיאגרמות ב-`#s2-fixed-images`
ו-`#s4-fixed-images` — שני המקומות היחידים בקובץ הזה, אומת לפני
השינוי כדי לוודא שאין השפעה על מסך שלישי) הוגדלו **300px → 400px**.
`.s3-scroll-area`, `#s2-scroll-gesture` ו-`#s4-scroll-gesture` הוזזו
בהתאם **left:360px → 460px** (40 שוליים + 400 רוחב-תמונה + 20 רווח),
כדי שאזור-הגלילה/רמז-הגלילה הסמוכים לא ייחתכו או יחפפו את התמונה
המורחבת.

## עדכון (07.09.2026, המשך) — מסך 6 (`s5`): מהפלייסהולדר-הריק למסך-מעבר אמיתי

בקשה מפורשת: "מסך אחרון בסיין 6 הוא צריך להיות מסך מעבר, אפשר להעתיק
ממסך ראשון באותו סיין". הוחלף `.s5-placeholder-box` (§ למעלה, מ-07.09.2026
המוקדם יותר, שהוצהר-בכוונה "ריק עד שנחליט") במבנה זהה למסך 1 (`s0`):
`.transition-content`/`.transition-title-block`/`.transition-avatar-wrap` —
**בלי** `.transition-bubble` (לא סופק טקסט-בועית בבקשה). טקסט:

- `.transition-sub` ("כל הכבוד!") — הגלובלי כבר בדיוק 28px/400-רגיל,
  לפי הבקשה, בלי צורך בדריסה.
- `.transition-title` ("השלמת את היחידה בהצלחה!") — הגלובלי כבר בדיוק
  40px/700-בולד, לפי הבקשה, בלי צורך בדריסה.

**דמות-נלווית**: `character-1` → `boy-avatar-jumping-happily.mp4`,
`character-2` → `yellow-avatar-jumping (1).mp4` (שם-קובץ עם רווח
וסוגריים, כפי-שסופק). אלמנט `<video id="s5-avatar">` יחיד +
`resolveCharBubbleVideo('s5-avatar', S5_AVATAR_ASSETS)`, אותו דפוס
בדיוק כמו `S0_AVATAR_ASSETS`/`resetScreenState0` למעלה.

⚠️ **תוקן (07.09.2026, עוד באותו יום)** — הניסיון הראשון סופק בטעות
עם תמונה סטטית (`Yellow_happy.png`, לא וידאו) לדמות הצהובה, מה שחייב
מנגנון `<video>`+`<img>` חופפים חריג (שני אלמנטים, `hidden` מתחלף
לפי-דמות) כי ההלפר הגלובלי `resolveCharBubbleVideo` מניח `<video>`
יחיד לשתי הדמויות. אחרי שסופק וידאו אמיתי חלופי לדמות הצהובה, המנגנון
הכפול הוסר לגמרי וחזר לדפוס הסטנדרטי-והפשוט (וידאו יחיד) — **לא
להשאיר** את מנגנון ה-video+img הכפול כ"גיבוי", הוא לא רלוונטי יותר.

**נוקה**: `.s5-placeholder-box`/`.s5-placeholder-box span` (styles.css)
הוסרו — לא בשימוש יותר בקובץ הזה.

## עדכון (07.09.2026, המשך) — `s2p1` (מסך 3, סעיף א'): יחידה-אחרי-משוואה מחוץ ל-dir="ltr"

דיווח: "מסך 3 סעיף א' סיין 6 צריך לתקן את המסומן בתמונה" (סומן: "216"
ו-"מ"ר" בשני ריבועים נפרדים). אותה בעיה בדיוק שתועדה כבר היום פעמיים
(`methodica-math-ratio-05-03/script.js` § `s5p2`, ו-`methodica-math-
ratio-05-06/script.js` § `s2p5` למעלה) — ועכשיו מתועדת גם כתבנית-א'
הרשמית ב-`ניסוח מתמטי.md` (בתיקיית-השורש של היעד): "מ"ר" ישב כטקסט-
רגיל *מחוץ* ל-`dir="ltr"` שעוטף את המשוואה (`200 + 16 = 216`). תוקן:
`dir="rtl"` מקונן סביב "216 מ"ר" (סדר-מקור מספר-ואז-יחידה), בתוך
ה-`dir="ltr"` החיצוני שנשאר כמו שהוא. הטקסט המוביל ("שטח החלקה כולה
הוא") לא שונה — לא סומן בדיווח.
