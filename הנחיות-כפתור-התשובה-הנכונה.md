# הנחיות: כפתור "התשובה הנכונה" בפופ-אפ המשוב (שאלות גרירה עם מכסת ניסיונות)

## מתי זה רלוונטי

בכל שאלת גרירה (Drag & Drop) עם מכסת ניסיונות מוגבלת (בד"כ 2), שבניסיון האחרון
הכושל **לא** גוללת אוטומטית את הפתרון הנכון. במקום זאת, הלומד/ת צריכים לבחור
בעצמם אם לראות אותו, ולהיות מסוגלים לעבור הלוך-חזור בין הפתרון הנכון לבין
התשובה שהם עצמם נתנו.

**התנהגות שגויה נפוצה (ברירת המחדל של רוב המימושים):** בניסיון השני השגוי,
קוראים אוטומטית לפונקציית `revealCorrect()` וזהו — הלומד/ת רואים את הפתרון
הנכון ותו לא, בלי אפשרות לחזור ולראות מה הם עצמם ענו.

**ההתנהגות הנכונה:**
1. בניסיון האחרון השגוי: מסמנים את המשבצות כשגויות (כרגיל), אבל **לא** קוראים
   ל-`revealCorrect()`. מציגים הודעת ביניים קצרה ("רוצים לראות את הפתרון
   הנכון?") וחושפים כפתור.
2. לחיצה ראשונה על הכפתור ("התשובה הנכונה") → **עכשיו** קוראים ל-`revealCorrect()`,
   מציגים את המשוב הסופי הרגיל (הטקסט שכבר קיים, לא לשנות אותו), והכפתור
   מתחלף לתווית "התשובה שלי".
3. לחיצה שנייה ("התשובה שלי") → משחזרים את המיקום המדויק שהלומד/ת עצמם הניחו
   (מ-snapshot, ראו למטה), מסמנים כל משבצת נכון/שגוי ביחס לתשובה שלהם, וחוזרים
   להודעת הביניים. הכפתור חוזר לתווית "התשובה הנכונה".
4. וחוזר חלילה — זה טוגל, לא חד-פעמי.

---

## עיצוב הכפתור

פיל סקנדרי (pill), **לא** הכפתור המלא/גרדיאנט של "המשך":

```css
.scq-fb-reveal-btn {
  align-self: flex-start;
  background: #fff;
  color: #019de5;
  border: 1.5px solid #019de5;
  border-radius: 1000px;
  font-family: 'Assistant', sans-serif;
  font-size: 18px;
  font-weight: 400;
  padding: 10px 24px;
  cursor: pointer;
}
.scq-fb-reveal-btn[hidden] { display: none; }
.scq-fb-reveal-btn:hover { background: #f0f8ff; }
```

מיקום: **בתוך** קופסת המשוב (`.scq-fb-box`), אחרי `.scq-fb-body`, מוסתר כברירת
מחדל (`hidden`):

```html
<div id="sX-feedbox" class="scq-fb-box">
  <div class="scq-fb-title">
    <span class="scq-fb-title-text"></span>
    <button class="scq-fb-toggle" type="button" onclick="scqFbClose('sX-feedbox')" aria-label="סגירת משוב"></button>
  </div>
  <p class="scq-fb-body"></p>
  <button class="scq-fb-reveal-btn" id="sX-reveal-btn" type="button" hidden onclick="sXToggleReveal()">התשובה הנכונה</button>
</div>
```

---

## תיקון קריטי: גרירת הפופ-אפ

אם קופסת המשוב ניתנת לגרירה (`scqFbMakeDraggable`-style, `mousedown` על כל
הקופסה), חובה להוסיף את הכפתור החדש לרשימת האלמנטים שמחריגים גרירה —
אחרת לחיצה עליו "נחטפת" כתחילת גרירה ולא עושה כלום:

```js
box.addEventListener('mousedown', function (e) {
  if (e.target.closest('.scq-fb-toggle') || e.target.closest('.scq-fb-reveal-btn')) return;
  // ... המשך לוגיקת הגרירה
});
```

---

## טקסטים

**אין לשנות שום טקסט משוב קיים** (`correct`, `wrong1`, `wrongFinal`/`wrong2`
וכו') כדי לממש את זה — הטקסט היחיד החדש הוא הודעת הביניים, לשימוש חוזר גם
בלחיצה הראשונה על "ניסיון אחרון שגוי" וגם בכל טוגל חזרה אליה:

```js
pending: {
  title: 'התשובה אינה נכונה.',
  body: 'רוצים לראות את הפתרון הנכון?'
}
```

---

## מימוש — וריאנט 1: פקטוריה משותפת (`makeDragQuestion`-style)

אם השאלה בנויה על פקטוריה משותפת (כמו ברוב הפרויקטים במשפחה הזו), מוסיפים
`cfg.revealBtnId` והלוגיקה חיה כולה בתוך הפקטוריה:

```js
// state נוסף בראש הפקטוריה, יחד עם שאר המשתנים:
let answerSnapshot = null; // הפלייסמנט של הלומד/ת ברגע הניסיון האחרון הכושל
let revealed = false;

function toggleReveal() {
  const revealBtn = cfg.revealBtnId ? document.getElementById(cfg.revealBtnId) : null;
  if (!revealed) {
    revealCorrect();                 // הפונקציה הקיימת, לא לשנות
    showFeedback('wrongFinal');       // הטקסט הקיים, לא לשנות
    revealed = true;
    if (revealBtn) revealBtn.textContent = 'התשובה שלי';
  } else {
    dragIds.forEach(function (dId) { placement[dId] = answerSnapshot[dId]; });
    targetIds.forEach(function (tId) {
      const valid = correctMap[tId];
      let placed = null;
      dragIds.forEach(function (dId) { if (placement[dId] === tId) placed = dId; });
      const ok = (placed === valid) || (placed && labels[placed] === labels[valid]);
      const zone = document.getElementById(tId);
      if (zone) { zone.classList.remove('correct', 'wrong'); zone.classList.add(ok ? 'correct' : 'wrong'); }
    });
    render();
    showFeedback('pending');
    revealed = false;
    if (revealBtn) revealBtn.textContent = 'התשובה הנכונה';
  }
}

// בתוך check(), בענף "ניסיון אחרון שגוי" (attempts >= maxAttempts):
} else if (attempts >= maxAttempts) {
  done = true;
  saveResult(false);
  if (cfg.revealBtnId) {
    answerSnapshot = Object.assign({}, placement); // חובה: לפני כל reveal
    revealed = false;
    showFeedback('pending');
    const revealBtn = document.getElementById(cfg.revealBtnId);
    if (revealBtn) { revealBtn.hidden = false; revealBtn.textContent = 'התשובה הנכונה'; }
  } else {
    revealCorrect();       // נשמר כ-fallback לשאלות בלי הכפתור
    showFeedback('wrongFinal');
  }
  ...
}

// באיפוס המסך (resetInitial):
answerSnapshot = null; revealed = false;
if (cfg.revealBtnId) {
  const revealBtn = document.getElementById(cfg.revealBtnId);
  if (revealBtn) { revealBtn.hidden = true; revealBtn.textContent = 'התשובה הנכונה'; }
}

// בחזרה למסך שכבר הושלם (restoreFinal) — לשחזר את מצב הכפתור/הטוגל:
if (cfg.revealBtnId && answerSnapshot) {
  const revealBtn = document.getElementById(cfg.revealBtnId);
  if (revealBtn) { revealBtn.hidden = false; revealBtn.textContent = revealed ? 'התשובה שלי' : 'התשובה הנכונה'; }
}

// לחשוף כפונקציה גלובלית (כמו שאר הפעולות של השאלה):
window[cfg.prefix + 'ToggleReveal'] = toggleReveal;
```

בהגדרת השאלה עצמה, מוסיפים שורה אחת ל-config:

```js
const dqX = makeDragQuestion({
  prefix: 'sX',
  // ... שאר השדות הקיימים
  revealBtnId: 'sX-reveal-btn',
});
```

---

## מימוש — וריאנט 2: מסך כתוב-ידני (בלי פקטוריה משותפת)

אותה לוגיקה בדיוק, רק מוטמעת ישירות בתוך `sXCheck()` / `sXRevealCorrect()` /
`resetScreenStateX()` הספציפיים של המסך, עם משתני מודול (`let sXAnswerSnapshot`,
`let sXRevealed`) במקום state בתוך closure של פקטוריה. הרעיון זהה: snapshot
לפני reveal, טוגל בין reveal לשחזור, איפוס נקי במעבר מסך.

---

## רשימת בדיקה לפני שמסמנים "בוצע"

- [ ] הכפתור **לא** נקרא אוטומטית בניסיון האחרון השגוי — רק מוצג (hidden→visible).
- [ ] ה-snapshot נלקח *בדיוק* ברגע הניסיון השגוי האחרון, *לפני* כל קריאה ל-reveal
      (אחרת המידע על התשובה המקורית של הלומד/ת אבד לצמיתות).
- [ ] הכפתור מתויג נכון בשני הכיוונים: "התשובה הנכונה" ↔ "התשובה שלי".
- [ ] טקסט המשוב הקיים (`wrong1`/`wrongFinal`/`correct`) לא שונה — רק נוסף `pending`.
- [ ] אם קופסת המשוב ניתנת לגרירה: `.scq-fb-reveal-btn` מוחרג מה-`mousedown`
      handler של הגרירה.
- [ ] איפוס מסך (ניווט חזרה/`resetScreenState`) מנקה את ה-snapshot, מחזיר את
      הכפתור למוסתר ולתווית ברירת המחדל.
- [ ] חזרה למסך שכבר הושלם (`restoreFinal`-style) משחזרת נכון את מצב הכפתור
      (מוצג + תווית נכונה) אם היה snapshot.
