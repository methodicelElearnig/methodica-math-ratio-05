# סיכום תהליך בניית הלומדה — 720, מתמטיקה יעד 1.5 (יחס)

## 🚀 התחל כאן — תבנית פתיחה לעובד/ת חדש/ה
ראו `תבנית-פתיחה-לפרויקט-לומדה-חדש.md` באותה תיקייה — התבנית הכללית והמעודכנת
לכל לומדות 720 (שני פרומפטים: תשתית + הפקת מסך, וצ'ק-ליסט מפיק/ה). מסמך זה
(היומן הספציפי ליעד 1.5 במתמטיקה) מצטבר תוך כדי הבנייה — כללי עבודה, החלטות,
קונפליקטים בתסריט שנפתרו, באגים ולקחים — לפי אותה מוסכמה.

**קבצי רפרנס למתודולוגיה (שניים, לא לערבב עם היומן הזה):**
- `סיכום-תהליך-בניית-הלומדה-מקור-מדעים-יעד11-גוף-חומר.md` (באותה תיקייה) —
  יומן פרויקט מדעים יעד 11 (6 סינים, אותו קובץ Figma "720 - UI Templates",
  אותם רכיבים/סקילים/מוסכמות). לפי בקשה מפורשת של המשתמשת: "אין הבדלים של
  ממש בין הלומדה בנושא מדעים לנושא המתמטיקה" — זה המקור העיקרי למוסכמות
  טכניות/תיעודיות (מבנה קבצים, מנוע ניווט, מוסכמת ID לפופ-אפים, Companion
  character system, באגים ידועים ותיקוניהם).
- `720 מדעים/מדעים יעד 2/methodica-math-scale-01-vadimr-1` (פרויקט מתמטיקה
  קודם, 5 סינים) — נבדק כמועמד למתודולוגיה, אך התברר **קדם-נוכחי**: קובץ
  Figma שונה לגמרי (`AWLhneUgYE31TIDCtMsBjH`, לא "720 - UI Templates"),
  צבעים hardcoded ללא מערכת טוקנים (`--subject-*`), בלי המוסכמות שנקבעו
  מאז (7 סקילים, RTL אמיתי, מוסכמת ID לפופ-אפים וכו'). **לא שימש כמקור
  למתודולוגיה בפועל** — רלוונטי לכל היותר כדוגמת-פלואו כללית (מבנה
  לימוד→תרגול, סף-מעבר 4/5). ראו סעיף 0.5 למטה.

---

## מצב הפרויקט הנוכחי

**תיאריך פתיחה: 16.08.2026.** יחידה חדשה, מתמטיקה יעד 1.5, נושא "יחס".
תסריט הפקה: `מתמטיקה_יחס_יעד 1.5_090826_מוכן להפקה.pptx` (בתיקיית הפרויקט
הראשית). לפי דגש מפורש של המשתמשת: **הנושא הלימודי כמעט לא משנה למתודולוגיה
— אותו קישור Figma, אותם עיצובים/רכיבים כמו לומדות המדעים שכבר הופקו, ההבדל
היחיד (מלבד תוכן) הוא ערכת הצבע (Subject theme Math/סגול, לא Science/כחול)**.
הבדלי-תוכן ספציפיים נוספים, אם יעלו, יצוינו נקודתית תוך כדי בנייה — לא
מונחים מראש.

**מספר סינים/סצנות: 6** (אושר ע"י המשתמשת ב-16.08.2026). פרומפט 1 (תשתית)
רץ כרגע רק על סיין 1 (`methodica-math-ratio-01-01`); שאר הסינים ייפתחו לפי
הצורך, כל אחד עם פרומפט-1 משלו (לפי המוסכמה בתבנית הפתיחה).

**משימת השיא — מועד אחד, לא שניים.** בשונה מלומדות קודמות (שם למשימת
השיא היו שני מועדים) — ביעד הזה יש **מועד אחד בלבד** למשימת השיא, על פני
6 הסינים. יש לזכור זאת בתכנון מבנה/ניווט הסיין שמכיל את משימת השיא, ולא
להעתיק אוטומטית מבנה דו-מועדי ממסך-מקור במדעים בלי להתאים.

**מקור לשכפול מסכים:** לפי בקשת המשתמשת, ברירת המחדל היא שכפול מלומדות
המדעים הקיימות (אותו Figma, אותם רכיבים) — מקור מדויק (איזו תיקייה, איזה
מספר מסך) ייקבע לכל מסך בנפרד ברגע שתסופק הרשימה. חלק מהמסכים עשויים להיבנות
חדשים לגמרי מ-Figma, לפי הצורך.

---

## 0. כללי עבודה מחייבים

**0.1 — סקילי הבנייה — שבעה, לא ארבעה.** כל בניית מסך/סיין עוברת דרך
שבעת הסקילים הבאים, כל אחד בשלב שבו הוא רלוונטי — לא רק פעם אחת בהתחלה:

| סקיל | מתי בדיוק |
|---|---|
| `720-templates` | לכל מסך שיש לו תבנית קיימת (TwoOptionSelection/SingleChoiceQuestion/MultipleChoiceQuestion/FlipCardsReveal/DragAndDropQuestion/ValueInputQuestion/MemoryGame) — שולף תבנית קוד קיימת ומטמיע, בלי לעבור דרך Figma כשלא נדרש. |
| `figma-lomda-builder` | לכל מסך חדש-מ-Figma (לא שכפול ממקור קיים) — שומר על מבנה הפרויקט, מממש רספונסיבית. |
| `720-design-guidelines` | לכל החלטת עיצוב (צבע/טיפוגרפיה/RTL/רכיב/שם) — מקור האמת העיצובי, כולל טוקני Subject theme. |
| `animation-system` | **אחרי** שתוכן/layout/אינטראקציות של מסך כבר סגורים — מוסיף אנימציות כניסה/reveal/popups/מעברים + בדיקת קדם-טיסה. לא בתחילת בניית מסך. |
| `responsiveness` | לפני נגיעה ב-`scaleApp()`/`#app`/`.screen` sizing, ולפני כל מסירת QA — ניתוח בלבד (v0.1 אינו עורך קוד). |
| `lomda-qa-publish` | לפני מסירה/סיום סיין — דורש דו"ח טרי מ-`responsiveness` כקלט; מכין את הריפו למסירה, לא מבצע את ההעלאה בפועל. |
| `skill-maintainer` | מעבר ניתוח בלבד לאורך כל התהליך — מחליט אילו ממצאים/לקחים כדאי להוסיף/לעדכן בספריית הסקילים, בלי לערוך סקילים ללא אישור מפורש. |

**0.2 — לחפש לפני שממציאים.** כשמתבקשת התאמה למסך מועתק ("בלי תמונה",
"פחות אפשרויות" וכו') — לחפש קודם בפרויקטי-המקור (לומדות המדעים שכבר
הופקו) האם כבר קיים וריאנט מאומת שפותר את אותה התאמה. אם קיים — להעתיק
אותו 1:1 ולהתאים רק את מה שבאמת ביקשו לשנות. רק אם חיפוש יסודי לא מעלה
כלום — מותר לעצב פתרון חדש, ולסמן זאת במפורש כ"הומצא, לא הועתק" בתיעוד.

**0.3 — מוסכמת ID לפופ-אפים.** כל תיבת משוב תיקרא `<prefix>-popup` (או
`<prefix>-feedbox`/`.scq-fb-box`, לפי המוסכמה הגלובלית הקיימת ברכיב
Feedback popup), וכל הצצת-רמז `<prefix>-hint-overlay`. `goTo(n)` בכל סין
סוגר את כולם אוטומטית לפני החלפת מסך (`closeAllPopupsAndHints()`) —
**חובה להשתמש בגרסה המתוקנת** (ראו לקח 0.6 למטה): הפעולה ההפוכה שכל רכיב
עצמו משתמש בה (`classList.remove('visible')` לפופ-אפים, `el.hidden=true`
להצצות-רמז), לא class גנרי `"hidden"` משותף.

**0.4 — Companion character system: ID לוגי, לא צבע/שם.** אם/כשייבנה מסך
בחירת דמות ביחידה זו, ה-ID הפנימי יהיה `character-1`/`character-2` (לא
צבע/שם ספציפי). מפתח האחסון מתויג ללומדה:
`CHARACTER_STORAGE_KEY = 'math-ratio-01_selectedCharacter'`.

**0.5 — פרויקט math-scale-01 (יעד 1 במתמטיקה, קודם) נבדק ונמצא לא-רלוונטי
למתודולוגיה.** נבדק לבקשת המשתמשת כמקור מתודולוגי אפשרי
(`720 מדעים/מדעים יעד 2/methodica-math-scale-01-vadimr-1`) — התברר קדם
למוסכמות הנוכחיות: Figma אחר (`AWLhneUgYE31TIDCtMsBjH`), בלי מערכת טוקני
`--subject-*`, גרדיאנט כפתור בצבעים hardcoded (`#8900E1`/`#6500A5`,
מתויגים בטעות "Blue" בתיעוד הישן). **לא הועתק/הועתק ממנו דבר** — נשמר
כאן רק לתיעוד ההחלטה, כדי שלא ייבדק שוב בטעות בעתיד.

**0.6 — טוקני Subject theme (Math) — מתוקן ב-16.08.2026.** הטבלה
המקורית שהופיעה כאן (ערכי סגול-מג'נטה, `--subject-500: #7100B9`) הייתה
**stale/שגויה** — לא תאמה את הטבלה העדכנית בפועל בסקיל
`720-design-guidelines` → `colors-and-gradients.md` (מקור האמת המוצהר של
אותו סקיל לערכי HEX לכל נושא). בבדיקה, גם אין גישה חיה ל-Figma בסביבה
הנוכחית (`get_variable_defs` דורש בחירת layer פעילה ב-Figma desktop, לא
זמין בסבב headless) — **הוכרע ע"י המשתמשת** להשתמש בטבלת הסקיל העדכנית
כמקור האמת בפועל לבנייה, במקום הערכים הישנים כאן:

| טוקן | ערך Math (סגול-אינדיגו, מהסקיל העדכני) |
|---|---|
| `--subject-100` | `#EAD9FF` |
| `--subject-200` | `#CEC7FF` |
| `--subject-300` | `#E1D9FF` |
| `--subject-350` | `#B3B0FF` |
| `--subject-400` | `#BDB3FC` |
| `--subject-500` | `#3B2FC2` |
| `--subject-600` | `#383896` |
| `--subject-700` | `#5656CC` |
| `--subject-800` | `#4B35A5` |
| `--subject-900` | `#2F2478` |
| `--subject-alpha-100` | `rgba(165, 28, 255, 0)` |
| `--subject-alpha-200` | `rgba(165, 28, 255, 0.3)` |

(שימו לב: כעת **12** טוקנים, לא 11 — הסקיל העדכני כולל גם `--subject-350`
שלא היה בטבלה הישנה כאן.) גרדיאנט `.btn-continue`: Math gradient מהסקיל —
`linear-gradient(180deg, #6F6AD5 8.654%, #4B35A5 38.462%, #270075 100%)`
(הנחת-ברירת-מחדל, לא מיפוי מאושר במפורש ע"י מעצב/ת — הסקיל עצמו מסמן את
השימוש המדויק כ"open question"). נטען ישירות על `:root` (לא כ-override
`data-subject-theme`, כי הפרויקט כולו במתמטיקה בלבד — אין צורך במיתוג
ריצה בין נושאים). כל שאר הטוקנים (feedback correct/incorrect, Natural
greys, טקסט `#303030`/`#222`, טיפוגרפיה, spacing) **זהים לגמרי**
לפרויקטי המדעים — לפי הכלל שרק `Subject/*`-bound properties משתנים בין
ערכות-נושא. פירוט מלא: `methodica-math-ratio-01-01/ARCHITECTURE.md`.

**0.7 — נכסים (PNG/SVG) עם צבע-נושא אפוי (baked-in) חייבים ייצוא מחדש**
ממצב Math בפיגמה בעת שכפול ממסך-מדעים — CSS variables לא צובעים מחדש
תמונות מיוצאות. נכסים בצבעים ניטרליים/feedback (V/X, אייקוני משוב) לא
דורשים ייצוא מחדש.

---

## 1. מטרת הפרויקט ומתודולוגיה

לומדה חדשה בעברית (RTL), תכנית 720, נושא "יחס" (מתמטיקה, יעד 1.5). שיטת
עבודה זהה לזו שגובשה ואומתה בלומדות המדעים (ראו קובץ הרפרנס
`סיכום-תהליך-בניית-הלומדה-מקור-מדעים-יעד11-גוף-חומר.md`):

1. חלק מהמסכים **מועתקים/מותאמים** מלומדות מדעים קיימות — מקור מדויק
   (איזו תיקייה, איזה מספר מסך) ייקבע לכל מסך בנפרד ברגע שתסופק הרשימה.
2. חלק מהמסכים עשויים להיבנות **חדשים לגמרי** מ-Figma/בריף שיסופקו.
3. פיגמה ("720 - UI Templates", מצב Math) הוא **מקור האמת לעיצוב**.
4. שימוש בשבעת הסקילים (ראו סעיף 0.1) לכל מסך.

**טרם סופק:** רשימת מסכים מלאה (מה מועתק מאיפה, מה חדש), וקובצי Figma
ספציפיים למסכים חדשים (אם יידרשו).

---

## 2. ארכיטקטורה טכנית

זהה למוסכמות שנקבעו ואומתו בלומדות המדעים (ראו קובץ הרפרנס), עם החלפת
ערכת הצבע בלבד (סעיף 0.6):

- HTML/CSS/JS וניל (ללא framework). קבצים: `index.html`, `styles.css`,
  `script.js`, `index_dev.html` (לא תת-תיקיות `css/`/`js/`).
- קנבס קבוע **1280 × 710**, `scaleApp()`.
- ניווט בין מסכים דרך `goTo(n)` + `resetScreenState(n)`, `TOTAL_SCREENS`.
- `index_dev.html` — כלי ניווט פנימי, `postMessage` (`DEV_READY`/`DEV_GOTO`).
- **11 טוקני נושא מלאים** (`--subject-100`…`--subject-900` +
  `--subject-alpha-100/200`, Math theme — ראו 0.6).
- מבנה נכסים: כל סין **עצמאי לחלוטין**, `assets/fonts/`+`assets/images/`+
  `assets/videos/` מקומיים.
- כללי RTL אמיתי (סדר DOM + `flex-start`/`flex-end`, בלי טריק
  `direction:ltr`), ניגודיות, פוקוס מקלדת — לפי `720-templates` skill.

---

## 3. רכיבים גלובליים בתשתית (סיין 1, מוכנים לשימוש, טרם מחוברים לתוכן)

- **`.flag-btn`** ("מצאתם בעיה?") — מופע יחיד, מחוץ לכל `.screen`.
- **`.img-zoom-btn` / `#img-zoom-modal`** — הגדלת תמונה, CSS+JS מוכנים.
- **פופ-אפ משוב** (`.scq-fb-*`) — CSS+JS מוכנים, כולל תיקון
  `closeAllPopupsAndHints()` (ראו 0.3) מההתחלה — לא מחדשים את הבאג הידוע.
- **פופ-אפ רמז** (`.scq-hint-*`) — CSS מוכן, HTML/JS per-screen.
- **`.btn-continue` / `.btn-back` / `.btn-hint`** — כפתורי פס הפעולה
  התחתון, RTL אמיתי, ערכי גובה/גרדיאנט/border מדויקים מ-`SELF-QA-lomda.md`
  → סעיף 6.
- **`resolveCharBubbleImg`/`resolveCharBubbleVideo`** — פונקציות עזר
  ל-Companion character system.

**עדיין לא בתשתית:** מסך בחירת דמות (אם נדרש), כל מסך שאלה ספציפי, ולוגיקת
ניווט cross-scene בין סינים נוספים (כשייפתחו).

---

## 4. מצב נוכחי

**⚠️ תיקון רישום (16.08.2026):** הרישום הקודם בסעיף הזה טען שתשתית סיין 1
כבר "הוקמה" — אך בפועל תיקיית `methodica-math-ratio-01-01` **לא הייתה
קיימת בדיסק** בתחילת סבב העבודה הנוכחי (נבדק ישירות, `Glob` לא מצא
אותה). כלומר הרישום הקודם היה שגוי/לא-מדויק — לא ידוע אם מדובר בתיעוד
שנכתב לפני שהקבצים נשמרו בפועל בסבב קודם, או בטעות תיעודית. התשתית
**נבנתה מחדש מאפס** בסבב הנוכחי, ואומתה ישירות מול הדיסק (לא רק מול
התיעוד) לפני שדווחה כאן כמוכנה.

**סיין 1 (`methodica-math-ratio-01-01`) — תשתית הוקמה מחדש (16.08.2026):**
הועתקו/הותאמו רכיבים גלובליים מ-`methodica-science-mass-measure-03-01`
(לא הועתקו מסכי התוכן שלו, רק המנוע/הרכיבים המשותפים — ראו
`ARCHITECTURE.md` לפירוט מלא). `TOTAL_SCREENS=1`, מסך placeholder יחיד.
טוקני Math תוקנו (ראו §0.6 למעלה). נבדק סטטית בלבד — **אין כלי
דפדפן/Playwright זמין בסביבה הנוכחית**: תחביר JS תקין (`node --check`),
איזון תגיות HTML, איזון סוגריים CSS, כל הפניית `assets/` נבדקה מול קבצים
בפועל בדיסק — כל הבדיקות עברו. פירוט מלא, כולל הנחות שטרם אושרו במפורש
(מיפוי גרדיאנט Math לכפתור Continue וכו'):
`methodica-math-ratio-01-01/ARCHITECTURE.md` +
`methodica-math-ratio-01-01/PROJECT_BRIEF.md`.

## 4.1 סיין 1 — מסך 1: בחירת דמות מלווה (TwoOptionSelection) (16.08.2026)

`data-screen="0"`/`id="s0"` הוחלף מ-placeholder למסך אמיתי: בחירת דמות
מלווה, וידאו בכרטיס (לא תמונה סטטית) — לפי בקשת המשתמשת ("בדומה למסך
ראשון ביעד 11"). יעד 11 עצמו לא נמצא בדיסק בסביבה זו; שוכפל בפועל ממסך
1 (`s0`) של `methodica-science-mass-measure-03-01` (720 מדעים, יעד 3),
שמשתמש באותו דפוס וידאו-בכרטיס בדיוק. שני נכסי הוידאו
(`boy-avatar-v-fingers.mp4` = `character-1`,
`yellow-avatar-eats-popcorn.mp4` = `character-2`) סופקו מראש ע"י
המשתמשת ב-`assets/videos/`. תבנית: `720-templates` skill →
TwoOptionSelection (עם וריאנט וידאו, לפי אישור מפורש של המשתמשת מול
SingleChoiceQuestion בשיחה שקדמה לבנייה).

**התאמה מכוונת אחת ביחס למקור:** המקור עדיין משתמש ב-`direction:ltr`
על `.options-row` (הטריק שהפרויקט אוסר); כאן RTL אמיתי — ברירת המחדל
של flexbox תחת `dir="rtl"` בלבד קובעת פיזית ימין/שמאל לפי סדר ה-DOM,
בלי override.

**פתוח, טרם אושר:** מיקום ימין/שמאל של `character-1`/`character-2`
נבחר שרירותית (אין הנחיה קודמת); alt text מומצא לפי שם הקובץ (לא סופק
`Alt text:` מפורש); אין תוויות-שם מתחת לכרטיסים (עקבי עם המקור).
**לא אומת חזותית/בדפדפן** (אין Playwright זמין) — יחס-הממדים האמיתי
של הוידאו מול ה-296:278 שה-CSS מניח לא נבדק בפועל (לא הצלחתי לחלץ
frame width/height, לא `ffprobe` ולא Windows Shell metadata חשפו זאת) —
יש לבדוק ידנית שה-`object-fit:cover` לא חותך חלק מהפעולה בוידאו. בדיקה
סטטית (JS syntax, איזון CSS/HTML, קיום נכסים) — עברה. פירוט מלא:
`methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 1".

**ממתין מהמשתמשת:** אישור/שינוי מיקום ימין/שמאל של שתי הדמויות; בדיקה
חזותית בדפדפן שהוידאו לא נחתך; רשימת מסכי תוכן הבאים לכל אחד מ-6 הסינים
(מקור לשכפול/מספר מסך, או קישור Figma; טווח שקפים בתסריט).

## 4.2 סיין 1 — מסך 2: אולימפיאדת המדעים, מסך גלילה מותאם (16.08.2026)

`TOTAL_SCREENS` עלה מ-1 ל-2. מסך **"לא בתבנית"** (תיוג מפורש של המפיקה
בהערות התסריט, שקפים 4-5) — שתי שאלות חד-ברירה עצמאיות (2 ו-3 מסיחים,
בשורה אופקית אחת — וריאנט מקומי חדש) בתוך אזור גלילה יחיד עם קפיצת-עמוד
(בהשראת מסך 17 ב-`mass-measure-03`, מותאם לעמודים שנחשפים דינמית: Q2
מוסתר עד שנענתה Q1). כותרת ("אולימפיאדת המדעים") + שתי תמונות-משלחת
נשארות קבועות/"צפות" מעל אזור הגלילה (פשוט מחוץ למיכל ה-`overflow:auto`,
לא `position:sticky`). תוכן חולץ בפועל מה-pptx (קובץ היה נעול על ידי
תוכנה אחרת בסיבוב הראשון — המשתמשת סגרה אותו, חולץ בהצלחה בסיבוב השני,
כולל טקסטים מקוננים בתוך קבוצות-צורה).

**שינוי מודע ביחס לרפרנס-הנראות שהמשתמשת ציינה** ("מסך 6 סיין 1 יעד
11", לא נמצא בדיסק בסביבה זו): טקסט הכותרת ישור-ימין, **לא** ממורכז,
לפי בקשה מפורשת; אין טוגל; צבעים = טוקני Math (לא Science).

**⚠️ הנחה יחידה שנותרה פתוחה:** טקסט המשוב לשאלה ב' (התסריט נתן רק את
פידבק שאלה א', שכבר חושף את התשובה "40") נכתב מחדש על סמך אותו
הסבר-יחס שכבר נמסר — לא ציטוט מדויק מהתסריט. יש לאשר. תמונות המשלחות —
placeholder בלבד, לפי בקשה מפורשת.

**לא אומת בדפדפן** (אין Playwright בסביבה): קפיצת-העמוד בפועל, שבירת
שורת-שלושה-המסיחים, מיקום מדויק של אזור הגלילה מתחת לכותרת. בדיקה
סטטית (JS syntax, איזון CSS/HTML, קיום נכסי אייקון) — עברה. פירוט מלא:
`methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2".

**עדכון (16.08.2026):** מיקום שתי תמונות-המשלחת תוקן — מ-שורה ממורכזת
ל-עמודה מוערמת (זו-מעל-זו) בצד שמאל פיזי (`position:absolute;
left:40px`, 150×150px), לפי רפרנס מפורש שהמשתמשת ציינה ("מסך 16 סיין 1
יעד 11") ובדיוק לפי הדפוס המאומת שאותו מסך-רפרנס עצמו התכנס אליו אחרי
כמה סבבי-דחייה (absolute-positioning עם `left` קבוע, לא flex+DOM-order/
centering — ראו `ARCHITECTURE.md` לפירוט המלא כולל הציטוט מהיומן).

**עדכון נוסף (16.08.2026) — שני פערי-תוכן אמיתיים + באג-גלילה, כולם
תוקנו:**
1. פסקת-הקדמה שלמה (נתוני-היחס 1:4/1:8) הייתה חסרה — קיימת בתסריט
   (שקף 4) אך עטופה ב-`<mc:AlternateContent>` ש-python-pptx משמיט
   בשקט מ-`slide.shapes` (אותרה רק בחיפוש XML ישיר — **לקח מתודולוגי
   לכל חילוץ pptx עתידי**, ראו `ARCHITECTURE.md`). נוספה ל-header
   הצף, כולל כיתוב-יחס ליד כל תמונה.
2. שאלה ג' (שקף 6, "חשיבה יחסית") נוספה — לא נבנתה קודם. מסיחים עם
   label+נימוק (וריאנט חדש, `.oly-opt-rich`).
3. תוקן באג ממשי: `preventDefault()` ללא-תנאי על גלגלת-העכבר חסם
   לגמרי גלילה טבעית ("הגלילה לא עובדת בכלל"). הוסר — גלילה טבעית
   עכשיו, קפיצת-עמוד/מקלדת נשארה רק כתוספת. **לא אומת בדפדפן אמיתי**
   (אין Playwright בסביבה).

פירוט מלא: `methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2".

**עדכון נוסף (16.08.2026) — תיקון מהותי לפי משוב חד מהמשתמשת:**
1. פורש מחדש מה "צף" בפועל — **רק עמודת התמונות** נשארת קבועה; הכותרת
   ופסקת-ההקדמה עברו לתוך אזור הגלילה עצמו (זרימה רגילה, מיד לפני
   שאלה א', לא header נפרד).
2. **כל גודלי הפונט הוחלפו לטוקנים מאושרים** — הגדלים הקודמים
   (16/14/13/11/18px) לא היו בטבלת הטוקנים כלל (720-design-guidelines
   → typography.md, טווח מאושר: 20px ומעלה בלבד).
3. אזור הגלילה קיבל שטח אמיתי (604px, לא 166px) — פותר את הצפיפות
   שהמשתמשת ציינה כ"הזייה".

פירוט מלא: `methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2" →
"עדכון (סבב שני)".

**עדכון נוסף (16.08.2026) — שני תיקונים לפי משוב ישיר:**
1. כפתור "צדקתי?" הוחלף מפיל-מתאר מומצא ל-`.btn-continue` הגלובלי
   (כבר מתועד ב-SELF-QA-lomda.md §6 כאותו רכיב בדיוק — לא היה צריך
   לחכות לפיגמה כדי לגלות זאת).
2. המשוב הוחלף מתיבה סטטית/מוטבעת לפופ-אפ `.scq-fb-box` גריר אמיתי —
   הסטייה הקודמת מהכלל הגלובלי לא הייתה מאושרת. קו-הפרדה אפור מומצא
   בין השאלות הוסר גם הוא.

פירוט מלא: `methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2".

**עדכון נוסף (17.08.2026):**
1. משוב-סעיף מוסתר ברגע שנבחר מסיח בסעיף הבא (לא רק בניווט-מסך) —
   "סעיפים" בהערת המפיקה = משוב-אחד-בכל-רגע.
2. מסיחים חזרו לעמודה מוערמת (לא שורה) — עקביות עם שאר שאלות
   חד-הברירה בפרויקט.

**עדכון נוסף (17.08.2026):**
1. עמודת התמונות הקבועה הוגדלה (150→170px).
2. נוסף שקף 7 (פסקת-מעבר + דמות מלווה עם בועית) אחרי שאלה ג', דמות
   תלוית-בחירה ממסך 1, גודל זהה למסך 2 סיין 1 יעד 3 (300×230px).
3. מסיחי שאלה ג' פושטו לריצת-טקסט אחת ("**משלחת X**: נימוק").

פירוט מלא: `methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2".

## 4.3 סיין 1 — Gesture Hint (Cursor Scroll) על מסך 2 + מסך 3 נבנה במלואו (17.08.2026)

**Gesture Hint:** `SELF-QA-lomda.md` §7 מתעד הנחיה מפורשת (יד+שתי
טבעות פועמות, מוצג פעם אחת בכניסה למסך, נעלם בגלילה אמיתית) שלא
יושמה בשני הסבבים הקודמים על מסך 2 — תוקן, ויושם **גם על מסך 3** לפי
בקשה מפורשת "בכל מסך שיש בו גלילה". צבעי-טבעת הוחלפו מ-Science
המוקשח (SELF-QA) ל-`var(--subject-350/400)` (Math).

**מסך 3 ("יהב ויעל שותלים גינה", `data-screen="2"`/`id="s2"`) נבנה
במלואו** — שקפים 8-9,11-14 (שקף 10 דולג לגמרי, מסומן בתסריט עצמו
"מסך לא להפקה"). כולל תצלום אמיתי מהתסריט (לא placeholder), placeholder
מפורש ליישומון GeoGebra (לפי בקשה), שאלת חד-ברירה (סעיף ג', SCQ
עמודה), ווריאנט **DragAndDropQuestion חדש** ("דיאגרמת-משוואה" — 4
משבצות מסביב למשוואה `4/7·70=40`, כל אחת מחוברת בקו לחלק הרלוונטי).

**⚠️ לקח מתודולוגי חוזר:** חילוץ טקסט מ-pptx דרך python-pptx פספס
תוכן אמיתי (פסקאות + נוסחאות `<m:oMath>`) שחי בתוך
`<mc:AlternateContent>` — פעם שנייה+שלישית שזה קורה בפרויקט הזה. מועמד
קבוע ל-skill-maintainer.

**פריסת שאלת-הגרירה שוחזרה מרינדור אמיתי** (PowerPoint COM automation
ליצוא השקף כ-PNG) — לא נמצא node תואם בפיגמה, והוחלט במפורש מול
המשתמשת לא לנחש מקואורדינטות XML גולמיות.

**דמויות-בועה במסך 3 = placeholder בלבד** — אין נכסי וידאו לתוכן הזה
(שונה מהתוכן שכבר סופק למסכים 1-2); אושר במפורש להמשיך כך בינתיים.

פירוט מלא + כל ההנחות/קירובים שדורשים אישור:
`methodica-math-ratio-01-01/ARCHITECTURE.md` → "מסך 2" (Gesture Hint)
ו-"מסך 3" (הכול).

**ממתין מהמשתמשת:** בדיקה חזותית בדפדפן של מסך 3 בשלמותו (גלילה,
gesture hint, גרירה, פריסת-המשוואה); נכסי וידאו-דמות לתוכן מסך 3
(סעיפים א'/ג'/ד'); אישור טקסט המשוב לשאלה ב' במסך 2; המשך מסכי תוכן.

## 4.4 סיין 1 — תוקן באג-זום מבני + נבנו מסכים 4-5 (מאוחד, "מסכים 4-11" בדוקס) (17.08.2026)

**תוקן באג מבני בהגדלת-תמונה (מסכים 2+3):** המשתמשת דיווחה שההגדלה
"מגעילה לגמרי" ושלא בדקתי את הרפרנס בלומדות קודמות — הצדק היה איתה.
`imgZoomOpen()` הגלובלי משכפל (`cloneNode`) את ה-wrapper המיידי של
כפתור-הזום לתוך מודאל 660×660px; ברפרנס (`methodica-science-mass-measure-03-01`,
`.scq-img-inner`) ה-wrapper הזה מוגדר `width/height:100%` (יחסי) כדי
שיתמלא אוטומטית לגודל המודאל. בקוד שכתבתי הוא היה ב-px קבוע (170×170
במסך 2, 400×267 במסך 3) — נשאר קטן במקום להתמלא. תוקן: כל wrapper
פוצל לרמה חיצונית (px קבוע, ללייאאוט הרגיל) + רמה פנימית חדשה (יחסי,
זו שבפועל משוכפלת). תמונות המקור (320×255px) נשארות ברזולוציה נמוכה
יחסית — מגבלת-נכס אמיתית, לא CSS.

**נבנו מסכים 4-5** לפי `פרומפטים לבניית מסכים של סיין 1.docx`:
- **מסך 4** (`data-screen="3"`/`id="s3"`) — מסך-מעבר, שקף 15. דמות-
  מלווה יחידה (לא "שתי דמויות אוחזות יד ביד" כמבוקש בהערת המפיקה —
  אין נכס כזה בפרויקט; הנחה שדורשת אישור/נכס-חדש).
- **מסך 5** (`data-screen="4"`/`id="s4"`, מאוחד — "מסכים 5-11" בדוקס/
  פיגמה, שקפים 16-22) — לפי בקשה מפורשת **"כמסך אחד, לשיקולך"**: נבנה
  כמסך HTML יחיד עם state machine פנימי בן 7 מצבים (פתיחה + 6 שלבי
  תרגול-מונחה על שאלת יחס-הגולות של אורי ודן, 56 גולות ביחס 5:2).
  שני-פאנלים לפי Figma (fileKey `eSbp4bKHgBky0rakDb8l9N`): מחברת-לבנה
  (בעיה מעומעמת+סיכום דינמי+שלב-נוכחי מודגש-לא-מצטבר) + כרטיס "שלב
  מודרך" סגול (#5749C4, לא זהה לטוקן Subject קיים — הוגדר משתנה
  ייעודי). תוכן מדויק חולץ מהתסריט **וגם** מ-8 שקפים שיוצאו כ-PNG
  אמיתי (PowerPoint COM) כדי לפענח סדר-פילים פיזי — תיקן מיפוי שגוי
  קודם בשיחה (שלב ב' הוא 3-way, לא 4-way). דיאגרמת-56-הגולות (שלב 6,
  8 קבוצות של 2 אדום+5 כחול) נבנתה HTML/CSS טהור, לא asset-תמונה.
  `TOTAL_SCREENS` עודכן ל-5.

**⚠️ לקח מתודולוגי:** זו הפעם הרביעית בפרויקט הזה שחילוץ-pptx רגיל
(python-pptx `has_text_frame`) פספס תוכן אמיתי (`<mc:AlternateContent>`)
— ומופע נוסף שבו PowerPoint COM screenshot היה הכרחי לפענוח ויזואלי
חד-משמעי (סדר פיזי של פילי-בחירה) שלא ניתן להסיק מטקסט גולמי בלבד.

פירוט מלא, כולל כל ההנחות שדורשות אישור: `methodica-math-ratio-01-01/ARCHITECTURE.md`
→ סעיפי "באג הגדלת-תמונה", "מסך 4", ו-"מסך 5 (מאוחד)".

**ממתין מהמשתמשת:** בדיקה חזותית בדפדפן שהזום המתוקן אכן נראה טוב
(במיוחד ברזולוציית-המקור הנמוכה של תמונות משלחת א'/ב'); אישור/נכס
לדמות-הזוגית במסך 4; אישור על פרשנות "אין צורך בלחיצת-המשך בין
הקטעים" במסך 5 (מומש כ"אין ניווט-מסך מלא", אבל כן יש כפתור-המשך
פנימי לכל שלב); אייקון-נורה (bulb) אמיתי לכרטיס "שלב מודרך" (כרגע
לא ממומש כלל — אין נכס, ואין ציור-SVG-ביד לפי מדיניות הפרויקט).

---

## עדכון — סיין 1, מסכים 6+7 (17.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 1.docx` (חולץ מה-XML הגולמי של
הקובץ). **מסך 6** (`data-screen="5"`, שקף 23) — מסך מעבר, שכפול מבני
מלא של מסך 4 (אין class חדש), כפתור-המשך מותאם ("קחו אותי לשאלות").
**מסך 7** (`data-screen="6"`, שקפים 24-26) — מסך גלילה, 2 שאלות
ValueInputQuestion (בקבוקים, אז זוויות); יישומון-בקבוקים כפלייסהולדר
ריק בלבד (בקשה מפורשת); דיאגרמת-זווית יוצאה בפועל מהשקף (PowerPoint
COM export + טרימינג-Python, לא צויר ביד); סרגל-התקדמות (qnav)
מצומצם ל-2 פריטים בלבד — לא 6 כמו במוקאפ הגולמי (ספירה גלובלית על
מסכים עתידיים, לא לוקאלית, לפי הבהרת המשתמשת). כפתור "אפשר רמז?"
(`.btn-hint`, מוגדר-CSS משלב-התשתית) קיבל כאן מימוש פעיל **ראשון**
בפרויקט. `TOTAL_SCREENS`: 5→7.

**ממצא-תיעוד תוך-כדי בנייה, תוקן:** `resetScreenState3()` בפועל
טוען וידאו-דמות **דינמית** לפי הבחירה במסך 1 (לא נכס-זוג-דמויות
סטטי כפי שתועד קודם ב-`ARCHITECTURE.md` של הסיין) — הערה ישנה,
לא עודכנה אחרי שינוי-קוד מאוחר יותר. מסך 6 נבנה לפי ההתנהגות
האמיתית.

בדיקה סטטית בלבד (אין Playwright בסביבה): `node --check`/איזון-
סוגריים/איזון-תגיות/כל הפניית-נכס — כולם PASS. `responsiveness`/
`animation-system`/בדיקת-דפדפן בפועל טרם בוצעו על אף מסך בסיין הזה.

פירוט מלא, כולל כל ההנחות שדורשות אישור:
`methodica-math-ratio-01-01/ARCHITECTURE.md` § "מסך 6" ו-§ "מסך 7".

---

## עדכון — סיין 2, תשתית + 5 מסכי תוכן (17.08.2026)

**תשתית:** `methodica-math-ratio-01-02` נמצאה קיימת-מראש כעותק גולמי
של קבצי סיין 1 (כולל כל תוכן-הלימוד הספציפי שלו) — נוקתה לתשתית-בלבד
לפי אישור מפורש ("Strip it down to clean infra"), תוך שמירה על נכסי
מדיה שכבר היו בתיקייה (כולל נכסים חדשים שלא קיימים בסיין 1). מפתח
אחסון-הדמות (`CHARACTER_STORAGE_KEY`) הושאר **זהה** לסיין 1 בכוונה —
משותף ברמת היעד/יחידה, לא ברמת הסיין, כדי שבחירת-הדמות תישמר בין
סינים.

**5 מסכי תוכן** (לפי `פרומפטים לבניית מסכים של סיין 2.docx`, שקפים
28-36): מסך-מעבר, שתי שאלות ValueInputQuestion+פלייסהולדר-תמונה, שני
מסכי-גלילה מרובי-חלקים (מידע+SCQ+נכון/לא-נכון; SCQ+SCQ+VIQ כפול).
אומץ לראשונה בפרויקט הזה הרכיב הקנוני "Progress Question"
מ-`720-templates` skill (BEM, state משותף על פני 4 מסכים, ניווט-
בלחיצה) — במקום לשכפל את ה-`qnav` האד-הוק שנבנה בסיין 1. שני נכסי-
תמונה חדשים חולצו ישירות מה-pptx (picture-shape אמיתי + ייצוא-שקף
לדיאגרמה). `TOTAL_SCREENS`: 1→5.

בדיקה סטטית בלבד (אין Playwright): כל הבדיקות הרגילות PASS, כולל
תיקון-תוך-כדי-בנייה של חוסר `placeholder=" "` בשדות קלט (היה שובר
את מצב ה-CSS "filled-blurred").

פירוט מלא, כולל כל ההנחות שדורשות אישור:
`methodica-math-ratio-01-02/ARCHITECTURE.md` § "מסכי תוכן (17.08.2026)".

---

## תשתית לסינים 3-6 (18.08.2026)

לפי `תבנית-פתיחה-לפרויקט-לומדה-חדש.md` § "פרומפט 1 — הקמת תשתית". נבנו
ארבע תיקיות חדשות: `methodica-math-ratio-01-03`/`-04`/`-05`/`-06` —
אף אחת מהן לא הייתה קיימת קודם (נבדק במפורש לפני יצירה, בניגוד למצב
שהתגלה בסיין 2). כל תיקייה: `TOTAL_SCREENS=1`, מסך placeholder יחיד,
כל הרכיבים הגלובליים מוכנים-לא-מחוברים (בר-ניווט, flag-btn, image
zoom, feedback popup CSS+JS, hint overlay CSS, Companion character
system) — **אין תוכן לימודי בשום תיקייה מהארבע**.

**מקור-מתודולוגיה:** `methodica-math-ratio-01-02` (סיין 2), לא סיין 1
— לפי "לחפש לפני שממציאים": סיין 2 עבר סבבי-QA נוספים אחרי סיין 1
(תיקון-גלילה-RTL, תיקון-צבע Progress Question, מרווח-פס-גלילה מול
Figma) והוא כרגע מקור-האמת המתודולוגי המאומת ביותר בפרויקט. סיין 3
נבנה ראשון כ"תבנית קנונית" (נקרא/נבדק ידנית שורה-שורה מול סיין 2),
וסיינים 4-6 שוכפלו ממנו (`cp -r` + החלפת מספר-סיין), לא נבנו כל אחד
בנפרד מאפס — מבטיח עקביות מוחלטת בין הארבע.

**ממצא אגבי שתוקן תוך-כדי:** בזמן קריאת סיין 2 כמקור-להעתקה התגלה
ש-`.scq-hint-overlay` שם עדיין הכיל צבע Science דלוף
(`rgba(0,32,96,0.35)`) שכבר תוקן באותו רכיב בסיין 1 — ביקורת-העיצוב
שהורצה קודם על סיין 2 החמיצה את זה כי בדקה רק את `.scq-hint-popup`
עצמו, לא את ה-backdrop שמאחוריו. תוקן בו-זמנית גם בסיין 2 (המקור) וגם
בסיינים 3-6 (הועתקו כבר-מתוקנים) — נגזר מ-`var(--subject-900)` של
הפרויקט (`#2F2478`→`rgb(47,36,120)`), לא ערך קבוע.

**נכסי-תוכן שלא הועתקו במכוון:** `icon-bulb-outline-white.svg`,
`icon-check-white.svg`/`icon-x-white.svg`, `gesture-hand-cursor.svg` —
לא הועתקו לסינים 3-6, כי שום CSS/HTML בתשתית-בלבד לא משתמש בהם עדיין
(ה-CSS שמפנה אליהם — `.btn-hint`, תגי נכון/שגוי, Gesture Hint — קיים
אך לא מחובר). יועתקו מסיין 2 כשמסך-תוכן ראשון שבאמת צריך אותם ייבנה
בכל סיין (Prompt 2).

**סקיל חדש, חובה מעכשיו:** `pptx-script-extraction` — נמצא מותקן
(בנתיב פגום, `~/.claude/skills/%USERPROFILE%/.claude/skills/`, כדאי
לבדוק/לתקן את ההתקנה) ונוסף כחובה ב-`MUST-USE-skills.md` §8: כל שליפת
תוכן מהתסריט (pptx) לבניית מסך חדש — בכל סיין, כולל 3-6 — **חייבת**
לעבור דרכו, לא python-pptx רגיל/סקרינשוט. הסיבה: כשל חוזר שכבר קרה
בפועל בפרויקט הזה — python-pptx רגיל מפספס בשקט טקסט העטוף
ב-`<mc:AlternateContent>`.

**חסר ידוע:** לא נמצא קובץ `פרומפטים לבניית מסכים של סיין 6.docx`
בתיקיית הפרויקט (קיימים לסינים 3/4/5 בלבד) — לא חוסם את התשתית עצמה,
אבל נדרש לפני Prompt 2 בסיין 6.

בדיקה סטטית בלבד (אין Playwright בסביבה הזו) על כל ארבע התיקיות:
`node --check script.js` PASS, איזון `{`/`}` ב-CSS (74/74 בכל אחת),
איזון-תגיות ב-HTML (9/9 div, 1/1 section בכל אחת). לא אומת חזותית
בדפדפן — יש לפתוח את `index_dev.html` של כל תיקייה ולוודא טעינה תקינה
לפני Prompt 2.

פירוט מלא לכל סיין: `methodica-math-ratio-01-0{3,4,5,6}/ARCHITECTURE.md`.

---

## תוכן אמיתי — סיין 3, 6 מסכים (18.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 3.docx` (שקפים 38-49, גבולות
אומתו דרך שקפי-מחיצה "רכיב 3"/"רכיב 4"). ⚠️ **הערה מתודולוגית
חשובה:** למרות ש-`pptx-script-extraction` נרשם כחובה ב-`MUST-USE-
skills.md` §8, **הוא לא ניתן להפעלה ישירה כ-Skill** (מותקן בנתיב פגום
— ראו למטה) — הועברה קריאה ידנית שלו במקום (נקרא `SKILL.md`+
`extraction-rules.md` במלואם ויושמו העקרונות שלהם ידנית: זיהוי-שקף
לפי תוכן לא היסט, רינדור-בפועל דרך PowerPoint COM automation
כשטקסט-שטוח לא מספיק, אי-אמון בקווי-הפרדה/רכיב-צבוע כראיה עצמאית).
זו לא הפעלה אמיתית של הסקיל, רק החלה-ידנית של אותה מתודולוגיה.

6 מסכים: מסך-מעבר → מסך-גלילה (משימת יחס-פרחי-שוקולד, 3 חלקים,
**פלייסהולדר ליישומון אינטראקטיבי** — החלטת-לקוח מפורשת לא לבנות
אותו עכשיו) → מסך-סטטי (דיאגרמת-זוויות + 4 נכון/לא-נכון, תמונה
מרונדרת מ-PowerPoint COM, לא מוטמעת כ-picture ב-pptx) → מסך-גלילה
(פרס-פורים ביחס-כספים, 3 חלקים, כולל **MultipleChoiceQuestion אמיתי
ראשון בפרויקט הזה** — 2 תשובות נכונות מתוך 3, רכיב `MCQ_CFG`/
`mcqToggle`/`mcqCheck` חדש) → מסך-מעבר → מסך-גלילה (יחס-שעות-עבודה,
קבוצת-התקדמות **שנייה ונפרדת** על אותו מסך, החלפת-תמונה לפי-חלק).

**ממצא-תוכן אמיתי, לא ניחוש:** הדוקס תיאר את החלק השלישי במסך-הפרס
כ"שאלה חד-ברירה", אבל רינדור בפועל של השקף (לא רק טקסט) הראה 2
תשובות-נכונות מודגשות בירוק מתוך 3 — MCQ אמיתי, לא SCQ. תוקן לפי
הראיה המרונדרת, לא לפי תיאור-הדוקס.

**באג JS אמיתי שנתפס ותוקן:** temporal-dead-zone `ReferenceError` —
`SCQ_CFG`/`VIQ_CFG`/`MCQ_CFG` הוצהרו בסוף הקובץ אבל נקראו (דרך
`*_REGISTER`) הרבה קודם. `node --check` לא היה תופס את זה (רק תחביר,
לא סדר-טעינה) — נתפס ע"י הרצה אמיתית ב-Node עם DOM מדומה, גם ע"י
הסוכן שבנה וגם בבדיקה עצמאית נפרדת אחר-כך.

פירוט מלא: `methodica-math-ratio-01-03/ARCHITECTURE.md`.

## תוכן אמיתי — סיין 4, 3 מסכים (18.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 4.docx` (שקפים 51-53, גבולות
אומתו דרך שקפי-מחיצה "רכיב 4"/"רכיב 5"). סיין קצר בהרבה: מסך-מעבר
(עם בועית-דיבור, תבנית שונה מ-`.s0-content` הפשוט של סיינים 2-3) →
משימת-כיתה פיזית (הוראות בלבד, בלי מחשב, **בלי שום בדיקת נכון/שגוי**
— אומת מול רפרנס אמיתי, הערת-קוד מקורית שם אומרת זאת מפורשות) →
המשך-משימה עם **קלט פתוח של נתוני-אמת אישיים** (6 שדות, בלי תשובה-
נכונה יחידה, בלי משוב/צביעה — רק שער "הכל מלא" לכפתור-המשך). ⚠️ שני
פרויקטי-אחות נבדקו (`methodica-science-mass-measure-03-03` והמקור
שלו `Methodica-science-mass-measure-02-03`) ואף אחד לא הכיל תקדים
לצירוף "הוראות-פיזיות + שדות-קלט-אמיתיים" — נבנה חדש, לא הועתק.

**באג אמיתי שנתפס בבדיקה-עצמית (סבב הזה, לא סבב-QA נפרד):**
`.viq-input`/`.viq-input--sm`/`.viq-coord` נעשה בהם שימוש ב-HTML של
מסך 3 אבל מעולם לא הוגדרו בפועל ב-CSS (התשתית של הסיין הזה לא כללה
CSS של תבניות-שאלה מלכתחילה) — נתפס ע"י סריקה פרוגרמטית של כל class
שמופיע ב-HTML מול קיומו בפועל ב-CSS, לא הסתמכות על הנחה. תוקן.

פירוט מלא: `methodica-math-ratio-01-04/ARCHITECTURE.md`.

## QA צולב על כל 5 הסינים לפני סיין 6 (19.08.2026)

לפי בקשת-משתמש מפורשת: לבדוק/לתקן על **כל** המסכים ב**כל** הסינים
שנבנו עד כה — מרווח פס-גלילה מול Figma, התנהגות-גלילה, מיקום כפתור
"אפשר רמז?" מול "צדקתי?", וצבעים. שני קישורי-Figma שהמשתמש/ת סיפק/ה
לא היו ניתנים-לפתיחה (קישור-גלילה בלי node-id, ונוד `3839:16518` של
כפתור-הרמז — "node not found"; כך גם הנודים החלופיים מ-SELF-QA-lomda.md
§6) — סוכם עם המשתמש/ת: לגלילה, מספיק מה שכבר אומת קודם; לכפתור-הרמז,
נעשה שימוש בנוד-חלופי שכבר אומת בשיחה (`1045:13096`) שמראה את שלושת
כפתורי-הפעולה יחד.

**ממצא אמיתי, מאומת מול קואורדינטות-Figma בפועל (לא ניחוש)**: סדר-
ה-DOM של כפתור-הרמז ביחס לכפתור-הבדיקה/המשך היה **הפוך** מהמקור ב-2
המקומות הממשיים שקיימים בפרויקט — `methodica-math-ratio-01-01`
(`.s6-answer-row-actions`, **מקור-הבאג המקורי**) ו-
`methodica-math-ratio-01-05` (`.btn-hint-row`, שני מופעים — הועתק
משם). תוקן בשלושתם: כפתור-הרמז עכשיו ראשון ב-DOM, כפתור-הבדיקה שני —
כך שתחת מוסכמת ה-RTL-flex של הפרויקט, "אפשר רמז?" מוצג פיזית מימין
ל-"צדקתי?"/"המשך", תואם ל-Figma.

**ממצא אמיתי נוסף**: `methodica-math-ratio-01-04` (מסכים 2+3) היה חסר
לגמרי את טכניקת עוטף-ltr/פנימי-rtl לפס-גלילה (מסכים שנבנו לפני שהדרישה
נבדקה שם במפורש) — סיכון לפס-גלילה בצד שמאל בניגוד למוסכמה. תוקן,
אותו דפוס-בדיוק כמו שאר הסינים.

`methodica-math-ratio-01-03`/`-05` כבר היו נכונים (`padding-right:20px`)
מלכתחילה. צבעים (`--subject-700` ל-`.btn-hint`) כבר נכונים בכל מקום —
אין ממצא. פירוט מלא לפי-סיין ב-`ARCHITECTURE.md` של כל אחד.

## תוכן אמיתי — סיין 6 (האחרון ביעד), 3 מסכים (19.08.2026)

לפי `פרומפטים לבניית מסכים של סיין 6.docx` (שקפים 62-68, תוכן חולץ
ישירות ע"י `python-docx`+PowerPoint COM — לא הסקיל `pptx-script-extraction`,
עדיין תקוע בנתיב-התקנה שבור, ראו `MUST-USE-skills.md` §8). תשתית-בלבד
(Prompt 1) כבר הייתה קיימת מסבב קודם; כאן נבנה Prompt 2 (תוכן אמיתי),
`TOTAL_SCREENS` עודכן מ-1 ל-3. נבנה ע"י סוכן מואצל עם תדריך מפורט
שכלל את כל התוכן שכבר חולץ + הגיאומטריה שנמדדה ישירות מה-pptx (יחסים,
לא ניחוש) עבור שני מסכי-התמונה-ממלאת-מסך; אומת עצמאית אחר-כך (לא רק
לפי דו"ח הסוכן) — `node --check`, טעינה-אמיתית ב-Node/vm, איזון-תגיות/
סוגריים, סריקת-נכסים מול דיסק, וכן בדיקה ממוקדת של שני התיקונים
שבוצעו הרגע בסבב ה-QA למעלה (סדר-DOM כפתור-רמז, מרווח פס-גלילה) —
כולם יושמו נכון במסך החדש הזה מלכתחילה.

**המסכים**: `s0` מסך-מעבר (מבנה מועתק 1:1 מ-`methodica-math-ratio-01-04`
מסך 1, חלוקת-הטקסט בין 4 חלקי-המבנה היא שיקול-דעת מתועד — המקור לא
מחלק בעצמו) → `s1` תמונה ממלאת-מסך (`family-home.jpg`) עם תגית-מידע —
**סוג-מסך חדש, אין תקדים ישיר** → `s2` "שאלת השיא" (4 סעיפים, כפי
שנרמז במסך הקודם): מסך-גלילה עם דיאגרמת-מגרש קבועה (חולצה כקרופ
מדויק מהשקף בעצמי, `land-plot-diagram.png`) המשותפת לארבעה סעיפי
ValueInputQuestion (א/ב/ג/ד — שטחים 216/80/26/28 מ"ר), + חלק-ביניים
תמונתי לא-מנוקד (`engineers.jpg`) שמסתיר זמנית את הדיאגרמה ומרחיב את
עמודת-הגלילה למלוא-הרוחב (טרייד-אוף מתועד — bleed עם margin שלילי
נפסל כי `overflow-x:hidden` של האב היה קוצץ אותו).

**ממצא-נוסח (לא באג בפועל, תוקן)**: הערות-קוד ו-`PROJECT_BRIEF.md` תיארו
בטעות את מיקום תגית-המידע במסך `s1` כ"פינה ימנית-עליונה" ואת צדדי-
הגינה/בנייה בדיאגרמה הפוך — ה-CSS/המיקום-בפועל היו **נכונים** מלכתחילה
(שמאל-עליון, גינה משמאל) לפי המדידה מה-pptx; רק הטקסט-המתאר היה הפוך.
תוקן בשלוש ההפניות (שני קבצי-md + alt-text של הדיאגרמה).

פירוט מלא: `methodica-math-ratio-01-06/ARCHITECTURE.md`. **לא בוצעה
בדיקה חזותית בדפדפן** (אין Playwright זמין) — מומלץ לעבור על `index_dev.html`
לפני מסירה, בפרט מיקום-התגית במסך `s1` והרחבת-העמודה בחלק-הביניים.

## שתי דרישות-משתמש נוספות (19.08.2026)

**1. מיקום כף-היד (Gesture Hint — Cursor Scroll) במסכי-גלילה, מול
Figma node `2915:35184`.** נבדק בפועל (`get_design_context` +
`get_motion_context`) — זהו דף-תיעוד-רכיב, לא מוקאפ-מסך קונקרטי.
הנחיית-השימוש הרשמית שלו: "Use when: the screen contains scrollable
content... **including scrollable feedback areas**. Target: position
over the relevant scroll area." — כלומר "מעל אזור-הגלילה", לא הנחיה
ל"ליד פס-הגלילה עצמו". משך-האנימציה (2000ms) תואם בדיוק את ה-
`2s infinite` הקיים כבר בכל הסינים. **מסקנה: אין ממצא/באג** — המיקום
הקיים (`left:50%; top:62%` בתוך אזור-הגלילה, זהה בכל 6 הסינים) תואם
את כוונת-הרכיב כפי שמתועדת בפועל ב-Figma. זה מתקן/משלים תשובה קודמת
בשיחה שתיארה את המיקום כ"ירושה לא-מאומתת" — עכשיו כן אומת ישירות.

**2. פופאפ-משוב סטטי מתחת למסיחים, במסכי-גלילה עם שאלות בלבד.** ה-
פופאפ (`.scq-fb-box`) היה `position:absolute` קבוע-לקנבס
(`left:16px; bottom:90px` וכו') — בכל 15 מסכי-הגלילה-עם-שאלות שמופו
קודם בשיחה, זה גרם לניתוק חזותי מהשאלה ברגע שגוללים לחלק אחר (ב/ג/ד).
נוספה מחלקה `.scq-fb-box.is-static` (בכל אחד מ-5 הסינים שיש להם
תיבות-משוב: `position:static; width:100%`), וכל תיבת-משוב הרלוונטית
**הועברה פיזית ב-DOM** לתוך השאלה שלה, מיד אחרי כפתור-הבדיקה — כך
שהיא זורמת עם התוכן הגולל במקום לצוף במיקום קבוע. הוסרה מרשימת-
האתחול `scqFbMakeDraggable` (אין יותר גרירה לתיבות האלה).

**סה"כ 31 תיבות-משוב הומרו** על-פני 15 מסכים ב-5 סינים
(01: 7, 02: 7, 03: 9, 05: 4, 06: 4) — **סיין 04 לא נגע כלל** (שני
מסכי-הגלילה שלו לא-מנוקדים, אין להם תיבת-משוב בכלל). **שתי תיבות-משוב
במסכים לא-גוללים נשארו כפי-שהיו במפורש** (סטטיות-לא-גולל, לא "מסכי-
גלילה"), לפי הנחיית-המשתמש המפורשת: `methodica-math-ratio-01-03`
מסך `s2` (זוויות/True-False) ו-`methodica-math-ratio-01-05` מסך `s2`
(יחס-שטחים, "זה לא מסך גלילה" לפי התסריט) — שתיהן עדיין `.scq-fb-box`
רגיל, עדיין רשומות ב-`scqFbMakeDraggable`.

**⚠️ היפוך-החלטה מתועד**: ב-16.08.2026 (סיין 1) התקבלה החלטה הפוכה
במפורש — להזיז תיבות-משוב **החוצה** מאזורי-גלילה, לגרירות מול קנבס-
קבוע, בדיוק כדי "שלא יוטבעו בתוך תוכן שגולל". ההנחיה הנוכחית מבטלת
זאת במפורש (ראו `methodica-math-ratio-01-01/ARCHITECTURE.md` § העדכון
המלא) — לא סתירה בתיעוד, החלטת-מוצר שהשתנתה.

אימות בכל 5 הסינים: איזון `<div>`/`{`/`}`, `node --check`, טעינה-
אמיתית ב-Node/vm — כולם PASS. **לא ניתן לאמת חזותית בסביבה הזו** —
יש לוודא בדפדפן שכל 31 התיבות מופיעות ישירות מתחת למסיחים בפועל,
בלי חפיפה עם עמודות-צד (תמונות קבועות/יישומונים).

## Gesture Hint (Cursor Scroll) — תיקון-המשך אחרי בירור נוסף (19.08.2026)

לאחר התשובה הראשונית (המיקום תואם Figma node `2915:35184` — "position
over the relevant scroll area", בלי מנדט-מיקום מדויק), המשתמשת נתנה
הבהרה קונקרטית שגילתה **באג אמיתי, לא רק העדפת-עיצוב**: במסכים
מסוימים היד מופיעה **בלי שום פס-גלילה גלוי**, ורק אחרי מענה/לחיצת-
המשך (כשתוכן נוסף נחשף ופס-הגלילה בפועל מופיע) מובן למפרע שהיא
הייתה קשורה אליו.

**שורש-הבאג**: בכל 11 המופעים של `sXMaybeShowScrollGesture()` בפרויקט
(6 סינים), הרמז הוצג **ללא-תנאי** בכניסה למסך — בלי לבדוק אם יש
בפועל `scrollHeight>clientHeight` באותו רגע. במסכי-גלילה מרובי-חלקים,
החלק הראשון לבדו לרוב קצר-מדי לגלישה; רק אחרי מענה וחשיפת חלק נוסף
התוכן בפועל גולש.

**תוקן בכל 6 הסינים (11 מופעים: 01×3, 02×2, 03×3, 05×2, 06×1) בשני
מישורים:**
1. **מיקום** — `.gesture-hint-scroll-wrap .gesture-hint { left: 92%; }`
   (סלקטור-ממוקד לרמזי-**גלילה** בלבד — רמזי-**גרירה**, `.gesture-hint-
   drag-wrap` הקיים בסיין 1 בלבד, נשארו במרכז ליד יעד-הגרירה, הקשר
   שונה לגמרי) — במקום `left:50%` (מרכז-האזור), כדי שהיד תופיע קרוב
   בבירור לפס-הגלילה עצמו.
2. **תזמון** — כל 11 הפונקציות בודקות עכשיו `scrollHeight>clientHeight`
   ממש לפני הצגה, ונקראות **שוב** מכל נקודת-חשיפת-תוכן בפועל (בעיקר
   `sXShowPart`, ובסיין 1 גם `olyQ1/2/3Check`/`s1ShowOutro`/`s2CCheck`/
   `s6Finish` — סיין 1 קדם למוסכמת-`ShowPart` האחידה, נקודות-חשיפה
   נמצאו ותוקנו בנפרד לכל אחת). כך אם אין עדיין מה לגלול, הרמז פשוט
   לא מוצג — והוא עדיין יכול להופיע ברגע שגלילה הופכת רלוונטית בפועל.

**אימות**: מעבר ל-`node --check`/איזון-סוגריים/טעינה-ב-Node שכבר
בוצעו בכל 6 הסינים, נוספה **בדיקה התנהגותית ממוקדת**: הרצת
`sXMaybeShowScrollGesture()` מול stub עם `scrollHeight<=clientHeight`
(מוודאת שהרמז נשאר מוסתר), ואז שוב אחרי הגדלת `scrollHeight` (מדמה
חשיפת-חלק, מוודאת שהרמז כן מוצג) — שני המצבים אומתו בפועל בקוד רץ,
לא רק נקרא. **לא ניתן לאמת חזותית בסביבה הזו** — יש לוודא בדפדפן
שהתזמון-החדש אכן מונע את מצב "יד-בלי-פס-גלילה" שדווח, ושהמיקום החדש
לא נחתך ע"י קצה-אזור-הגלילה.

## גודל-דמות במסכי-מעבר, מאוחד מול סיין 6 מסך 1 (19.08.2026)

**בדיקה מקיפה** על כל מסכי-המעבר בפרויקט: 2 משפחות-CSS מקבילות
קיימות — `.s0-content`/`.s0-avatar` (בלי בועית-דיבור, סיינים 2+3, 3
מופעים: s0 של סיין 2, s0+s4 של סיין 3) מול `.transition-content`/
`.transition-avatar` (עם בועית-דיבור, סיינים 4/5/6, מופע אחד כל אחד —
סיין 4 מסך 1 הוא **המקור המקורי**, שסיין 6 מסך 1 הועתק ממנו במפורש
לפי הדוקס). סיין 1 אין לו אף מסך-מעבר (רק מסך-בחירת-דמות, תבנית
שונה).

**ממצא אמיתי**: `.s0-avatar` (סיינים 2+3) היה `320×320px,
object-fit:contain` — שונה-לגמרי מ-`.transition-avatar` (סיינים 4/5/6)
`304×293px, object-fit:cover`, על-אף ששלושתם מציגים **אותם קובצי-
וידאו בדיוק** (זהים byte-for-byte בין הסינים — נבדק ב-md5; מקור-הוידאו
עצמו 1280×720, נבדק ישירות מה-`tkhd` atom של קובץ ה-MP4, לא ניחוש).
אין הבדל תוכני שמצדיק שוני-גודל — זו הייתה סטייה בלתי-מכוונת.

**תוקן**: `.s0-avatar` בשני הסינים (2 ו-3, 3 מופעי-מסך) הותאם בדיוק
לגודל של `.transition-avatar` — `width:304px; aspect-ratio:304/293;
object-fit:cover; border-radius:16px`. `.s0-avatar-wrap` לא שונה (אין
בו בועית-דיבור שדורשת רוחב-עוגן קבוע, בניגוד ל-`.transition-avatar-wrap`).

אימות: איזון `{`/`}` נספר מחדש בשני הקבצים — PASS. **לא ניתן לאמת
חזותית בסביבה הזו** — יש לוודא בדפדפן שה-`object-fit:cover` החדש
(במקום `contain`) לא חותך חלק חשוב מהדמות בשלושת המופעים; פירוט מלא
ב-`ARCHITECTURE.md` של כל אחד משני הסינים.

## משוב נשאר מוצג לצמיתות + תוכן-משוב תוקן + תמונות גדלו (19.08.2026)

**דרישה חדשה, הופכת חלק מהתיקון הקודם**: הפופאפ-הסטטי (מה-QA באותו
יום) חייב להישאר גלוי-על-המסך **גם אחרי מעבר לשאלה הבאה** — לא רק
לזרום נכון ב-DOM. שני שורשי-בעיה נמצאו ותוקנו:

1. **`scrollIntoView` בכל 9 נקודות-חשיפת-חלק בפרויקט** (01/02×2/03×3/
   05×2/06) גלל תמיד אל **החלק החדש עצמו**, מה שדוחף את תיבת-המשוב-
   הסטטית של החלק הקודם (בתחתית אותו חלק) מעל שולי-אזור-הראייה.
   **תוקן בכולן**: גוללים עכשיו אל תיבת-המשוב-הגלויה של החלק הקודם
   (`el.previousElementSibling.querySelector('.scq-fb-box.visible')`)
   אם קיימת — כך שהמשוב מתמקם בראש אזור-הראייה, והחלק החדש ממשיך
   מיד מתחתיו. אומת בבדיקה התנהגותית ממוקדת (לא רק `node --check`):
   סימולציה עם stub-מבנה `part1`+`feedbox-גלוי`+`part2` מוודאת בפועל
   שהגלילה מתמקדת בתיבת-המשוב.
2. **שני מנגנוני-הסתרה ייעודיים בסיין 1 בלבד** (קדם למוסכמת-
   `sXShowPart` האחידה, לא קיימים בשום סיין אחר — נבדק): `olyHideFeedback`
   (מסך 2, מדיניות-17.08.2026 "כל שאלה מציגה רק את המשוב שלה" —
   הופכת עכשיו) ו-`s2InitFeedbackAutoHide` (מסך 3, `IntersectionObserver`
   שהיה נחוץ כשהמשוב היה עדיין פופאפ קנבס-קבוע — התייתר לגמרי אחרי
   שהפך ל-`.is-static`). שניהם הוסרו לגמרי, כולל קריאותיהם ב-`resetScreenState`.

**ממצא-תוכן אמיתי, בעקבות בירור על סיין 1 מסך 2 (אולימפיאדה)**: משוב
שאלה א' (`S1_Q1_FEEDBACK`) הכיל בפועל **את תשובת שאלה ב' גם** ("א.
[הסבר]... ב. [40 משתתפות — התשובה של שאלה ב']"), עם תוויות-אות
מיותרות. שורש-הדבר: בתסריט-המקור הייתה פסקת-הסבר אחת משותפת לשתי
השאלות (תיבת-משוב אחת בלבד שם); כשנבנו כאן שתי תיבות נפרדות, הטקסט
המשותף הועתק **במלואו** לתוך שתיהן — ל-Q2 כבר תוקן-נכון בזמנו, אבל
Q1 נשאר. **תוקן**: הושאר רק החלק הרלוונטי לשאלה א' עצמה, בלי תוויות-
אות ובלי חשיפת-תשובת-Q2 מוקדמת — עכשיו כל שאלה מציגה רק את המשוב
שלה-עצמה, בלי צורך בתוויות כלל (כל תיבה כבר תלויה מיקומית בשאלה
שלה).

**תמונות-המשלחות** (סיין 1 מסך 2): הוגדלו לפי בקשה מפורשת,
`200×200px` → `260×260px` (עמודת-התמונות ואזור-הגלילה/Gesture-Hint
המצומדים אליה הוזזו בהתאם, `left:272px`→`left:332px`).

אימות מלא בכל 5 הסינים (`node --check`, בדיקות התנהגותיות ממוקדות,
איזון `<div>`/`{`/`}`) — PASS. **לא ניתן לאמת חזותית בסביבה הזו**;
פירוט מלא לפי-סיין ב-`ARCHITECTURE.md` (הפירוט המקיף ביותר —
`methodica-math-ratio-01-01`, שם רוב הממצאים נמצאו).

## סבב-תיקונים נוסף, סיין 1 מסך 3 + Gesture Hint פרויקטלי (20.08.2026)

**Gesture Hint — תוקן שוב, בכל 5 הסינים**: `left:92%` (מ-19.08.2026)
דווח כלא-קרוב-מספיק בפועל לפס-הגלילה, "150 פעם" לפי המשתמשת. הוחלף
ב-`right:6px` **קבוע-בפיקסלים** (לא אחוז, שמתרחק מהקצה ככל שהעמודה
רחבה יותר) — עוגן יציב שלא תלוי ברוחב-עמודת-הגלילה של כל מסך. נשמר
כזיכרון-קבוע כדי שלא יידרש תיקון שלישי.

**סיין 1 מסך 3 (גינה) — שלוש בעיות אמיתיות, נבדקו ותוקנו:**

1. **תמונה+שורה-סגולה בסעיף א'** — נבדק בפועל מול Figma node
   `525:9109`. השורה הסגולה כבר תואמת; לתמונה חסרו `padding:8px`
   ו-`border:1px solid rgba(174,174,174,0.5)` (הוסרו בטעות ב-17.08
   בהסתמך על רפרנס-אנלוגי מפרויקט אחר, לא על הצומת האמיתי) — תוקן.
2. **גרירת-המשוואה בסעיף ה'** — דווח "שום דבר לא נראה תקין". נמצאו
   4 באגים אמיתיים: משבצות-שחרור בגודל-קבוע קטן-מדי (170×58, לא
   מכיל את הכרטיסים הארוכים בפועל); `white-space:nowrap` שעבר
   בירושה מההורה ומנע כל אפשרות-גלישה לשורה שנייה (כנראה שורש-הבאג
   המרכזי); קונטיינר בגובה-קבוע (280px) עם מיקום-absolute שהיה חורג/
   חופף ברגע שמשבצת גדלה; ו-border-radius/גודל-badge שלא תאמו את
   הצומת הקנוני (1249:32691, "Drag&Drop State") שנבדק בפועל — 12px
   לא 10px, 26px לא 22px. כל הארבעה תוקנו. **ממצא פתוח, לא תוקן**:
   אותו צומת מראה תג-סימון פיזית-שמאל, לא ימין כמו המוסכמה שהקוד
   מניח כאן — לא תוקן (רדיוס-פגיעה רחב על מוסכמה משותפת ברחבי
   הפרויקט, דורש אימות נפרד לפני שינוי).
3. **חשיפה-הדרגתית בוטלה** — לפי בקשה מפורשת, סעיפים ד'/ה' גלויים
   תמיד עכשיו (לא עוד `hidden` עד מענה על סעיף קודם); הלומד/ת פשוט
   גולל/ת. שונה רק החזותיות — שום סעיף לא היה תלוי-פונקציונלית
   בסעיף קודם, כך שהלוגיקה עצמה לא שונתה.

**זיכרון-משוב חדש נשמר**: החלטות font-size/weight חייבות להיגזר
מ-Figma-חי/מסך-רפרנס-אחות/טבלת-הסקיל — לא מניחוש — לפי תלונה
מפורשת וחוזרת של המשתמשת.

אימות: איזון `<div>`/`{`/`}` בכל הקבצים שנגעו בהם — PASS, `node --check`
PASS. **לא ניתן לאמת חזותית בסביבה הזו** — פירוט מלא ב-
`methodica-math-ratio-01-01/ARCHITECTURE.md`.
