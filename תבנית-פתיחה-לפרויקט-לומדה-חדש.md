# תבנית פתיחה — פרויקט לומדה חדש (720)

מסמך זה הוא **תבנית משותפת** לכל מי שמפיק/ה לומדה חדשה בתכנית 720
(מקצוע/יעד כלשהו — מדעים, מתמטיקה וכו'), כתוב אחרי שכמה יעדים (מדעים
יעד 2, יעד 3) כבר הופקו ונאספו מהם לקחים בפועל. הוא **מעדכן** את
`First Prompt.docx` (הפרומפט המקורי שפתח את הפרויקט הראשון,
`Methodica-science-mass-measure-02`) — לא מחליף אותו; ההבדלים העיקריים
מתועדים בסעיף "מה השתנה מאז" בסוף.

**איך להשתמש בזה:** זה לא פרומפט אחד מוכן-להעתקה, אלא **שני פרומפטים**
(תשתית + הפקת מסך) עם דגשים על אילו חלקים קבועים בין כל הלומדות ואילו
משתנים לפי היעד הספציפי שלך. תמלא/י את השדות ב-`< >`, תשמור/י את
הגרסה המלאה שלך (עם הפרטים של היעד שלך) בקובץ הסיכום של הפרויקט שלך —
לא כאן, כדי שהתבנית הזו תישאר נקייה לפרויקט הבא.

---

## מה קבוע בכל לומדות 720 (לא ממציאים מחדש בכל יעד)

- **שבעת הסקילים**, לאורך כל הבנייה — לא רק פעם אחת בהתחלה:
  - `720-templates` — שולף תבניות קוד קיימות של מסכי 720 ומטמיע אותן
    בפרויקט (התבנית הרלוונטית לכל מסך: TwoOptionSelection /
    SingleChoiceQuestion / MultipleChoiceQuestion / FlipCardsReveal /
    DragAndDropQuestion / ValueInputQuestion / MemoryGame), בלי לעבור
    דרך Figma כשלא נדרש.
  - `figma-lomda-builder` — בונה מסכים חדשים מתוך קובצי Figma, שומר על
    מבנה הפרויקט ומממש רספונסיבית לפי מוסכמות הפיתוח.
  - `720-design-guidelines` — מקור האמת העיצובי: צבעים, טיפוגרפיה, RTL,
    רכיבים, מצבי תבניות, שמות ובדיקות עיצוב.
  - `animation-system` — מוסיף/מתחזק מערכת אנימציות אחידה (כניסה, reveal,
    popups, מעברים) **אחרי** שתוכן/layout/אינטראקציות של מסך כבר סגורים —
    לא בהתחלה, ולא לפני שהמסך עצמו יציב.
  - `responsiveness` — ניתוח (לא עריכה) של ארכיטקטורת viewport/scaling של
    מסך/סיין; להריץ **לפני** נגיעה ב-`scaleApp()`/`#app`/`.screen`
    sizing, ולפני כל מסירת QA.
  - `lomda-qa-publish` — בדיקת לומדה לפני מסירה (תוכן/עיצוב/אינטראקציות/
    ניווט/ביצועים), איתור קבצים/קוד לא בשימוש, הכנת הריפו למסירה — דורש
    דו"ח טרי מ-`responsiveness` כקלט.
  - `skill-maintainer` — מעבר ניתוח בלבד; מחליט אילו ממצאים/לקחים כדאי
    להוסיף/לעדכן בספריית הסקילים, בלי לערוך סקילים ללא אישור מפורש.
- HTML5+CSS3+JS טהור בלבד — בלי React/framework/build tools/CDN חיצוני.
  קבצים: `index.html`/`styles.css`/`script.js`/`index_dev.html` (לא
  תת-תיקיות `css/`/`js/`).
- קנבס קבוע 1280×710, `scaleApp()` להגדלה דינמית, ניווט `goTo(n)`/
  `resetScreenState(n)`, `TOTAL_SCREENS`.
- RTL **אמיתי** מהשורש — סדר DOM + `flex-start`/`flex-end`, לא טריק
  `direction:ltr` (אלא במקרים חריגים שיתועדו במפורש למה).
- כל תמונת/כפתור-Figma מיוצא **בלי** טקסט לומד חי (`upload_assets`
  מרכיב את כל הילדים כולל TEXT — צריך לוודא שה-node שמייצאים נקי מטקסט
  לפני הייצוא). כל טקסט פונה-ללומד = HTML חי, לא raster.
- `index_dev.html` — כלי ניווט חופשי בין מסכים לבדיקות, מתעדכן אוטומטית
  (`DEV_READY`/`DEV_GOTO` דרך `postMessage`).
- כל תיקיית סיין/סצנה **עצמאית לחלוטין** — `assets/fonts/`
  `assets/images/` `assets/videos/` מקומיים בתוכה, לא תיקיית `assets`
  משותפת ברמת הפרויקט. כל סיין = חבילה נפרדת שניתן להעלות ל-LMS בפני
  עצמה.
- לפני כל התאמה למסך מועתק ("בלי תמונה", "פחות אפשרויות" וכו') —
  לחפש בכל פרויקט-המקור אם כבר קיים וריאנט מאומת שפותר את אותה
  התאמה, ולהעתיק 1:1 לפני שממציאים פתרון חדש.
- כל מסך/רצף מסכים נבדק בפועל ב-Playwright (זרימה מלאה ורציפה, כולל
  מסלולי הצלחה **וגם** כשלון אם התוצאה תלוית-ביצוע), אפס שגיאות JS.
- לכל תיקיית סיין: `PROJECT_BRIEF.md` + `ARCHITECTURE.md`. ברמת
  הפרויקט הראשי: קובץ `סיכום-תהליך-בניית-הלומדה.md` יחיד, מתעדכן
  אחרי כל מסך/סבב — כללי עבודה, החלטות, קונפליקטים בתסריט שנפתרו,
  באגים ולקחים.
- ניקוי: אין להשאיר screenshots/סקריפטים זמניים בתיקיות הפרויקט —
  רק ב-scratchpad.

## מה משתנה בכל יעד (למלא לפי הפרויקט הספציפי שלך)

| שדה | הערה |
|---|---|
| נושא/יעד | למשל "יעד 8 במתמטיקה" — קובע גם את נושא הלימוד וגם את טוקן הצבע (`--subject-500`/`--subject-700`) אם שונה מהיעדים הקודמים |
| נתיב לתסריט ההפקה (pptx) | קובץ ה-slides הספציפי ליעד הזה |
| קישור/י Figma | קובץ הייחוס הרשמי לכל הפרויקטים: **"720 - UI Templates"** — `https://www.figma.com/design/eSbp4bKHgBky0rakDb8l9N/720---UI-Templates?node-id=196-3196&m=dev`. ("לומדת התנסות", fileKey `Z9sD0kksYj1Ph1kUcs71sw`, המוזכר במסמכי יעד 2/3 — זה עותק אישי של אותו קובץ, לא קובץ אחר.) node-ים ספציפיים משתנים לפי מסך |
| פרויקט-מקור למתודולוגיה | הפרויקט/יעד האחרון שהושלם (למשל יעד 3 עבור יעד 8) — מצביעים עליו כדי ש-Claude יקרא את `סיכום-תהליך-בניית-הלומדה.md` שלו וימשיך באותן מוסכמות בדיוק |
| תבנית שם תיקיות | למשל `methodica-<תחום>-<נושא>-<NN>` |
| מספר סינים/סצנות | אם ידוע מראש; אחרת מתעדכן תוך כדי |

---

## פרומפט 1 — הקמת תשתית

> תכין/י תשתית ללומדה חדשה בעברית (RTL), תכנית 720. נושא היחידה:
> `<נושא/יעד>`. תסריט ההפקה: `<נתיב לקובץ ה-pptx>`. `<אם רלוונטי:
> קישור/י Figma של המסכים>`.
>
> המשכיות מתודולוגית: תקרא/י את `<נתיב לקובץ סיכום-תהליך-בניית-הלומדה.md
> של הפרויקט-מקור הקודם>` ותפעל/י לפי אותן מוסכמות בדיוק (מבנה קבצים,
> מנוע ניווט, כללי RTL, מוסכמות תיעוד) — זה לא פרויקט מאפס מבחינת
> שיטת עבודה, רק מבחינת תוכן.
>
> דרישות מחייבות (ראו "מה קבוע" למעלה) — כל בניית מסך עוברת דרך שבעת
> הסקילים (`720-templates`, `figma-lomda-builder`, `720-design-guidelines`,
> `animation-system`, `responsiveness`, `lomda-qa-publish`,
> `skill-maintainer`, כל אחד בשלב שבו הוא רלוונטי), קנבס 1280×710 עם מנוע
> `goTo`/`resetScreenState`, כל סיין תיקייה עצמאית לחלוטין, RTL אמיתי בלי
> טריק direction:ltr, בדיקת Playwright לכל מסך, תיעוד
> `PROJECT_BRIEF.md`+`ARCHITECTURE.md` לכל סיין + קובץ סיכום ראשי.
>
> בשלב הזה **אל תבנה/י תוכן** — רק תשתית: מבנה תיקיות לסינים
> (`<תבנית שם>`), שלד טכני מלא בכל תיקייה (מסך placeholder יחיד אם עוד
> אין תוכן), וקובץ הסיכום הראשי. תעדכן/י אותי כשזה מוכן.

⚠️ **אם המסך הראשון מבוסס על Figma חדש לגמרי** (לא המשך של לומדה קיימת) —
לפי `First Prompt.docx` המקורי, כדאי להוסיף לפרומפט הזה: *"לפני כתיבת
קוד, בצע רק שלב תכנון: עדכן/י PROJECT_BRIEF.md+ARCHITECTURE.md, הצג/י
לי Plan מפורט (מיפוי Figma frames למסכים/states, מבנה קבצים, רשימת
assets מתוכננת, מה יהיה live HTML לעומת raster, class/state names
מרכזיים, סיכונים אפשריים, סדר בנייה) — וחכה/י לאישור שלי לפני קוד/assets
בפועל."* זה מונע בזבוז עבודה אם המיפוי-Figma לא נכון.

## פרומפט 2 — הפקת מסך חדש

יש שני מצבים למסך חדש — תלוי אם יש לו "מקור" (מסך קיים ללומדה קודמת)
או שהוא חדש-מ-Figma:

**(א) שכפול ממסך קיים בלומדה אחרת:**

> תבנה/י מסך `<N>` בסיין `<X>`. שכפל/י את מסך `<מספר/שם המסך במקור>`
> מתוך: `<נתיב לתיקיית הלומדה/סיין המקור>`. תוכן: שקפים `<טווח>`
> בתסריט. התאמות מהמקור (אם יש): `<מה שונה>`. `<נתיב לתמונה/נכס אם
> סופק>`.

**(ב) מסך חדש לגמרי מ-Figma** (בלי מקור בלומדה קודמת):

> תבנה/י מסך `<N>` בסיין `<X>` לפי הפריים הבא בפיגמה: `<קישור עם
> node-id>`. השתמש/י בתבנית `<שם התבנית מ-720-templates, למשל
> SingleChoiceQuestion>`. תוכן: שקפים `<טווח>` בתסריט. מצבים
> (states) שצריך למפות: `<לפני בחירה / אחרי בחירה / תשובה נכונה /
> ניסיון שגוי 1 / משוב סופי, עם קישורי node-id לכל אחד אם יש>`.
> הקפד/י: טקסט לומד = HTML חי, לא raster; כפתור "אפשר רמז?" ופופ-אפ
> הרמז לפי הפריים הרלוונטי.

**דגשים שכדאי לזכור בכל מקרה (מהלקחים שנצברו):**
- ברגע שתוכן/layout/אינטראקציות של המסך סגורים ויציבים — סקיל
  `animation-system` מוסיף את שכבת האנימציה (כניסה/reveal/popups/מעברים),
  כולל בדיקת קדם-טיסה (transform-conflict, layout-shift, replay לא רצוי).
  לא לפני שהמסך יציב.
- וריאנט מבוקש ("בלי X", "עם Y") שכבר קיים כווריאנט מאומת במקור —
  מעתיקים, לא ממציאים.
- קונפליקט בין הערת המפיק בתסריט לתוכן המשוב עצמו (`correctId` וכו') —
  נפתר לפי **תוכן המשוב בפועל**; שואלים את המשתמש/ת רק כשבאמת אין
  הכרעה ברורה מהתוכן.
- שגיאת כתיב חוזרת ידועה בתסריטים: "התשבה" → מתקנים בשקט ל"התשובה".
- נכסים חסרים (תמונה/גיף שלא סופקו) — לא ממציאים ולא חוסמים; בונים
  placeholder מתועד וממתינים לנכס בפועל.
- כל מסך/זרימה חדשים נבדקים ב-Playwright ומתועדים ב-`ARCHITECTURE.md`
  + בקובץ הסיכום הראשי.

---

## צ'ק-ליסט למפיק/ה — מהתחלה ועד סיין מוכן

### לפני שפותחים Claude Code
- [ ] לוודא שקובץ תסריט ההפקה (pptx) נגיש, ולדעת את הנתיב המלא אליו.
- [ ] קישור Figma הרשמי: `https://www.figma.com/design/eSbp4bKHgBky0rakDb8l9N/720---UI-Templates?node-id=196-3196&m=dev`
      ("720 - UI Templates"). "לומדת התנסות" (fileKey
      `Z9sD0kksYj1Ph1kUcs71sw`) המוזכר במסמכי יעד 2/3 הוא עותק אישי
      של אותו קובץ עצמו — לאסוף node-id-ים ספציפיים למסכים הראשונים.
- [ ] להחליט על נושא/יעד (למשל "יעד 8 במתמטיקה") ותבנית שם תיקיות
      (`methodica-<תחום>-<נושא>-NN`).
- [ ] להחליט אם יש פרויקט-מקור למתודולוגיה (היעד האחרון שהושלם) ולאתר
      את קובץ ה-`סיכום-תהליך-בניית-הלומדה.md` שלו.
- [ ] ליצור/לבחור את תיקיית העבודה הראשית של הפרויקט/היעד החדש.

### שלב 1 — הקמת תשתית (פרומפט 1, פעם אחת לכל סיין חדש)
⚠️ פרומפט 1 לא רץ פעם אחת בלבד לכל הפרויקט — הוא חוזר **לכל סיין
חדש** (גם בתוך אותו יעד), כי כל סיין הוא תשתית טכנית עצמאית משלו.
- [ ] לפתוח את Claude Code בתוך תיקיית העבודה של הסיין החדש.
- [ ] למלא את השדות בפרומפט 1 (נושא, נתיב תסריט, נתיב לפרויקט-מקור,
      קישורי Figma אם רלוונטי).
- [ ] אם המסך הראשון בסיין הוא Figma חדש לגמרי (לא המשך של לומדה
      קיימת) — להוסיף את קטע "שלב תכנון בלבד, חכה לאישור" (ראו למעלה).
- [ ] לשלוח את הפרומפט, ולענות לשאלות הבהרה אם יש.
- [ ] לבדוק שנוצרו: מבנה תיקיות + שלד טכני (`index.html`/`styles.css`/
      `script.js`/`index_dev.html`) + `PROJECT_BRIEF.md`+`ARCHITECTURE.md`
      + קובץ סיכום ראשי (אם עוד לא קיים).
- [ ] לפתוח את `index_dev.html` בדפדפן ולוודא שהקנבס נטען תקין
      (placeholder ריק, בלי שגיאות בקונסול).

### שלב 2 — הפקת מסכים (פרומפט 2, חוזר על עצמו לכל מסך)
- [ ] לכל מסך: להחליט אם יש לו מקור (מסך קיים בלומדה אחרת) או שהוא
      Figma-חדש — ולבחור את הווריאנט המתאים (א'/ב').
- [ ] לאסוף פרטים מדויקים: מספר/שם המסך במקור + נתיב מלא, טווח שקפים
      בתסריט, ומה בדיוק שונה מהמקור — **או**, אם Figma-חדש: קישור
      עם node-id, שם התבנית מ-`720-templates`, ומיפוי ה-states
      (לפני בחירה/אחרי/תשובה נכונה/ניסיון שגוי/משוב סופי).
- [ ] לשלוח את פרומפט 2.
- [ ] לעבור על התוצאה בעין דרך `index_dev.html`.
- [ ] לוודא ש-Claude הריץ בדיקת Playwright (הצלחה + כשלון אם רלוונטי)
      ודיווח על אפס שגיאות JS.
- [ ] לוודא ש-Claude הפעיל את `animation-system` על המסך אחרי שהתוכן/
      layout/אינטראקציות סגורים (לא לפני).
- [ ] לוודא עדכון `ARCHITECTURE.md`/`PROJECT_BRIEF.md` + קובץ הסיכום
      הראשי.
- [ ] לחזור על השלב הזה עד שכל מסכי הסיין מוכנים.

### שלב 3 — סיום סיין
- [ ] לוודא ניקוי — אין screenshots/סקריפטי בדיקה שנשארו בתיקיית
      הפרויקט (רק ב-scratchpad).
- [ ] לבדוק זרימה מלאה ורציפה של **כל** הסיין (לא רק מסכים בודדים
      במבודד).
- [ ] להריץ `responsiveness` על הסיין (ניתוח viewport/scaling; לא עורך
      קוד) — לוודא דו"ח טרי לפני מעבר ל-QA.
- [ ] להריץ `lomda-qa-publish` (דורש את דו"ח ה-`responsiveness` הטרי
      כקלט) — תוכן/עיצוב/אינטראקציות/ניווט/ביצועים, איתור קבצים/קוד לא
      בשימוש, הכנת הריפו למסירה.
- [ ] אם יש סיין נוסף בפרויקט — לחזור לשלב 1, עם התייחסות לתיקיית
      הסיין הקודם כ"מקור להעתקת מנוע/רכיבים גלובליים".

---

## מה השתנה מאז `First Prompt.docx` (הפרומפט המקורי)

הפרומפט המקורי (ששימש להתחלת `Methodica-science-mass-measure-02`)
התמקד במסך בודד מ-Figma, עם שלב תכנון-וחיכוי-לאישור לפני קוד. מאז,
דרך יעד 2 ויעד 3, נוספו המוסכמות הבאות (כולן בתבנית הזו למעלה):

- מבנה **סיין עצמאי לחלוטין** (assets מקומיים, לא משותפים) — בפרויקט
  המקורי `assets/` היה מתוכנן כתיקיית-על משותפת; בפועל זה לא מה
  שנבנה, ותוקן במפורש.
- מנגנון **המשכיות דמות בין סינים** דרך `localStorage`
  (`lomda_selectedCharacter`) — כי כל סיין הוא מסמך HTML נפרד לגמרי.
- דרישת **ארבעת הסקילים** לאורך כל הבנייה (לא רק `figma-lomda-builder`
  בהתחלה).
- העיקרון **"לחפש לפני שממציאים"** — נולד מתיקון בפועל אחרי שהומצא
  מבנה חדש למרות שוריאנט מתאים כבר היה קיים במקור.
- דרישת **בדיקת Playwright** לכל מסך/זרימה (לא הייתה מוזכרת במקור).
- דרישת **RTL אמיתי** ללא טריק `direction:ltr` — נולדה מתיקון בפועל
  בפס הפעולה התחתון.
- מוסכמת תיעוד **PROJECT_BRIEF+ARCHITECTURE לכל סיין, וקובץ סיכום
  ראשי אחד** שמצטבר לאורך כל הפרויקט (המקור דיבר רק על PROJECT_BRIEF/
  ARCHITECTURE ברמת מסך בודד).

